{
  "properties": {
    "matrix_id": {
      "type": "string",
      "description": "Unique identifier"
    },
    "action": {
      "type": "string",
      "description": "CREATE|READ|UPDATE|DELETE|EXECUTE"
    },
    "approval_required": {
      "type": "string",
      "description": "true|false"
    },
    "resource_type": {
      "type": "string",
      "description": "Data model layer or service"
    },
    "approval_role": {
      "type": "string",
      "description": "Who must approve"
    },
    "allowed": {
      "type": "string",
      "description": "true|false"
    },
    "effective_date": {
      "type": "string",
      "description": "ISO 8601 when RBAC applies"
    },
    "role": {
      "type": "string",
      "description": "User personas (L2) or service principal"
    },
    "conditional": {
      "type": "string",
      "description": "Rule if conditional access required"
    },
    "audit_required": {
      "type": "string",
      "description": "true|false"
    },
    "data_classification": {
      "type": "string",
      "description": "Classification level accessible"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema/access_control_matrix.schema.json",
  "required": [
    "matrix_id",
    "action",
    "approval_required",
    "effective_date",
    "resource_type",
    "approval_role",
    "allowed",
    "audit_required",
    "conditional",
    "role",
    "data_classification"
  ],
  "description": "Role-Based Access Control (RBAC) configuration per resource type and user role",
  "type": "object",
  "title": "access_control_matrix"
}
