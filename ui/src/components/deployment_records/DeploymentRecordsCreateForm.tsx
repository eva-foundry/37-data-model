/**
 * DeploymentRecordsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L84 (deployment_records)
 * Generated: 2026-03-12T09:01:31Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new deployment_records records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createDeploymentRecordsRecord } from '@api/deployment_recordsApi';
import type { DeploymentRecordsRecord, CreateDeploymentRecordsRecordInput } from '@/types/deployment_records';

interface DeploymentRecordsCreateFormProps {
  onSuccess: (record: DeploymentRecordsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const DeploymentRecordsCreateForm: React.FC<DeploymentRecordsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('deployment_records.create_form');

  const [formData, setFormData] = useState<Partial<CreateDeploymentRecordsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createDeploymentRecordsRecord(formData as CreateDeploymentRecordsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="deployment_records-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="deployment_records-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="deployment_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Id *
          </label>
          <input
            type="text"
            id="deployment_id"
            data-testid="deployment_records-field-deployment_id"
            value={(formData.deployment_id as string) || ''}
            onChange={(e) => handleChange('deployment_id', e.target.value)}
            aria-invalid={!!errors.deployment_id}
            aria-describedby={errors.deployment_id ? 'deployment_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deployment_id && (
            <div
              id="deployment_id-error"
              role="alert"
              data-testid="deployment_records-field-deployment_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Timestamp *
          </label>
          <input
            type="datetime-local"
            id="timestamp"
            data-testid="deployment_records-field-timestamp"
            value={(formData.timestamp as string) || ''}
            onChange={(e) => handleChange('timestamp', e.target.value)}
            aria-invalid={!!errors.timestamp}
            aria-describedby={errors.timestamp ? 'timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.timestamp && (
            <div
              id="timestamp-error"
              role="alert"
              data-testid="deployment_records-field-timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="session"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Session *
          </label>
          <input
            type="text"
            id="session"
            data-testid="deployment_records-field-session"
            value={(formData.session as string) || ''}
            onChange={(e) => handleChange('session', e.target.value)}
            aria-invalid={!!errors.session}
            aria-describedby={errors.session ? 'session-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.session ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.session && (
            <div
              id="session-error"
              role="alert"
              data-testid="deployment_records-field-session-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.session}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="phase"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Phase *
          </label>
          <input
            type="text"
            id="phase"
            data-testid="deployment_records-field-phase"
            value={(formData.phase as string) || ''}
            onChange={(e) => handleChange('phase', e.target.value)}
            aria-invalid={!!errors.phase}
            aria-describedby={errors.phase ? 'phase-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.phase ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.phase && (
            <div
              id="phase-error"
              role="alert"
              data-testid="deployment_records-field-phase-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.phase}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Id *
          </label>
          <input
            type="text"
            id="agent_id"
            data-testid="deployment_records-field-agent_id"
            value={(formData.agent_id as string) || ''}
            onChange={(e) => handleChange('agent_id', e.target.value)}
            aria-invalid={!!errors.agent_id}
            aria-describedby={errors.agent_id ? 'agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_id && (
            <div
              id="agent_id-error"
              role="alert"
              data-testid="deployment_records-field-agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Name *
          </label>
          <input
            type="text"
            id="agent_name"
            data-testid="deployment_records-field-agent_name"
            value={(formData.agent_name as string) || ''}
            onChange={(e) => handleChange('agent_name', e.target.value)}
            aria-invalid={!!errors.agent_name}
            aria-describedby={errors.agent_name ? 'agent_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_name && (
            <div
              id="agent_name-error"
              role="alert"
              data-testid="deployment_records-field-agent_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="project_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Id *
          </label>
          <input
            type="text"
            id="project_id"
            data-testid="deployment_records-field-project_id"
            value={(formData.project_id as string) || ''}
            onChange={(e) => handleChange('project_id', e.target.value)}
            aria-invalid={!!errors.project_id}
            aria-describedby={errors.project_id ? 'project_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.project_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_id && (
            <div
              id="project_id-error"
              role="alert"
              data-testid="deployment_records-field-project_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="service"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service *
          </label>
          <input
            type="text"
            id="service"
            data-testid="deployment_records-field-service"
            value={(formData.service as string) || ''}
            onChange={(e) => handleChange('service', e.target.value)}
            aria-invalid={!!errors.service}
            aria-describedby={errors.service ? 'service-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service && (
            <div
              id="service-error"
              role="alert"
              data-testid="deployment_records-field-service-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deployment_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Type *
          </label>
          <input
            type="text"
            id="deployment_type"
            data-testid="deployment_records-field-deployment_type"
            value={(formData.deployment_type as string) || ''}
            onChange={(e) => handleChange('deployment_type', e.target.value)}
            aria-invalid={!!errors.deployment_type}
            aria-describedby={errors.deployment_type ? 'deployment_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deployment_type && (
            <div
              id="deployment_type-error"
              role="alert"
              data-testid="deployment_records-field-deployment_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="change_summary"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Change Summary *
          </label>
          <input
            type="text"
            id="change_summary"
            data-testid="deployment_records-field-change_summary"
            value={(formData.change_summary as string) || ''}
            onChange={(e) => handleChange('change_summary', e.target.value)}
            aria-invalid={!!errors.change_summary}
            aria-describedby={errors.change_summary ? 'change_summary-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.change_summary ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.change_summary && (
            <div
              id="change_summary-error"
              role="alert"
              data-testid="deployment_records-field-change_summary-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.change_summary}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="before_state"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Before State *
          </label>
          <textarea
            id="before_state"
            data-testid="deployment_records-field-before_state"
            value={(formData.before_state as string) || ''}
            onChange={(e) => handleChange('before_state', e.target.value)}
            aria-invalid={!!errors.before_state}
            aria-describedby={errors.before_state ? 'before_state-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.before_state ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.before_state && (
            <div
              id="before_state-error"
              role="alert"
              data-testid="deployment_records-field-before_state-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.before_state}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="after_state"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            After State *
          </label>
          <textarea
            id="after_state"
            data-testid="deployment_records-field-after_state"
            value={(formData.after_state as string) || ''}
            onChange={(e) => handleChange('after_state', e.target.value)}
            aria-invalid={!!errors.after_state}
            aria-describedby={errors.after_state ? 'after_state-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.after_state ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.after_state && (
            <div
              id="after_state-error"
              role="alert"
              data-testid="deployment_records-field-after_state-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.after_state}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="validation_result"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Validation Result *
          </label>
          <textarea
            id="validation_result"
            data-testid="deployment_records-field-validation_result"
            value={(formData.validation_result as string) || ''}
            onChange={(e) => handleChange('validation_result', e.target.value)}
            aria-invalid={!!errors.validation_result}
            aria-describedby={errors.validation_result ? 'validation_result-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.validation_result ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.validation_result && (
            <div
              id="validation_result-error"
              role="alert"
              data-testid="deployment_records-field-validation_result-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.validation_result}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="artifacts"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Artifacts *
          </label>
          <textarea
            id="artifacts"
            data-testid="deployment_records-field-artifacts"
            value={(formData.artifacts as string) || ''}
            onChange={(e) => handleChange('artifacts', e.target.value)}
            aria-invalid={!!errors.artifacts}
            aria-describedby={errors.artifacts ? 'artifacts-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.artifacts ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.artifacts && (
            <div
              id="artifacts-error"
              role="alert"
              data-testid="deployment_records-field-artifacts-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.artifacts}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="evidence_correlation_ids"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Correlation Ids *
          </label>
          <textarea
            id="evidence_correlation_ids"
            data-testid="deployment_records-field-evidence_correlation_ids"
            value={(formData.evidence_correlation_ids as string) || ''}
            onChange={(e) => handleChange('evidence_correlation_ids', e.target.value)}
            aria-invalid={!!errors.evidence_correlation_ids}
            aria-describedby={errors.evidence_correlation_ids ? 'evidence_correlation_ids-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_correlation_ids ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.evidence_correlation_ids && (
            <div
              id="evidence_correlation_ids-error"
              role="alert"
              data-testid="deployment_records-field-evidence_correlation_ids-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_correlation_ids}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="duration_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Duration Seconds *
          </label>
          <input
            type="number"
            id="duration_seconds"
            data-testid="deployment_records-field-duration_seconds"
            value={(formData.duration_seconds as number) || 0}
            onChange={(e) => handleChange('duration_seconds', Number(e.target.value))}
            aria-invalid={!!errors.duration_seconds}
            aria-describedby={errors.duration_seconds ? 'duration_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.duration_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.duration_seconds && (
            <div
              id="duration_seconds-error"
              role="alert"
              data-testid="deployment_records-field-duration_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.duration_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_available"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Available *
          </label>
          <input
            type="checkbox"
            id="rollback_available"
            data-testid="deployment_records-field-rollback_available"
            checked={(formData.rollback_available as boolean) || false}
            onChange={(e) => handleChange('rollback_available', e.target.checked)}
            aria-invalid={!!errors.rollback_available}
            aria-describedby={errors.rollback_available ? 'rollback_available-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.rollback_available && (
            <div
              id="rollback_available-error"
              role="alert"
              data-testid="deployment_records-field-rollback_available-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_available}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_command"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Command *
          </label>
          <input
            type="text"
            id="rollback_command"
            data-testid="deployment_records-field-rollback_command"
            value={(formData.rollback_command as string) || ''}
            onChange={(e) => handleChange('rollback_command', e.target.value)}
            aria-invalid={!!errors.rollback_command}
            aria-describedby={errors.rollback_command ? 'rollback_command-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rollback_command ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rollback_command && (
            <div
              id="rollback_command-error"
              role="alert"
              data-testid="deployment_records-field-rollback_command-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_command}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="approver"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Approver *
          </label>
          <input
            type="text"
            id="approver"
            data-testid="deployment_records-field-approver"
            value={(formData.approver as string) || ''}
            onChange={(e) => handleChange('approver', e.target.value)}
            aria-invalid={!!errors.approver}
            aria-describedby={errors.approver ? 'approver-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.approver ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.approver && (
            <div
              id="approver-error"
              role="alert"
              data-testid="deployment_records-field-approver-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.approver}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="approval_timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Approval Timestamp *
          </label>
          <input
            type="datetime-local"
            id="approval_timestamp"
            data-testid="deployment_records-field-approval_timestamp"
            value={(formData.approval_timestamp as string) || ''}
            onChange={(e) => handleChange('approval_timestamp', e.target.value)}
            aria-invalid={!!errors.approval_timestamp}
            aria-describedby={errors.approval_timestamp ? 'approval_timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.approval_timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.approval_timestamp && (
            <div
              id="approval_timestamp-error"
              role="alert"
              data-testid="deployment_records-field-approval_timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.approval_timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="compliance_gaited"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Compliance Gaited *
          </label>
          <input
            type="checkbox"
            id="compliance_gaited"
            data-testid="deployment_records-field-compliance_gaited"
            checked={(formData.compliance_gaited as boolean) || false}
            onChange={(e) => handleChange('compliance_gaited', e.target.checked)}
            aria-invalid={!!errors.compliance_gaited}
            aria-describedby={errors.compliance_gaited ? 'compliance_gaited-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.compliance_gaited && (
            <div
              id="compliance_gaited-error"
              role="alert"
              data-testid="deployment_records-field-compliance_gaited-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.compliance_gaited}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="mtI_score"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Mti Score *
          </label>
          <input
            type="text"
            id="mtI_score"
            data-testid="deployment_records-field-mtI_score"
            value={(formData.mtI_score as string) || ''}
            onChange={(e) => handleChange('mtI_score', e.target.value)}
            aria-invalid={!!errors.mtI_score}
            aria-describedby={errors.mtI_score ? 'mtI_score-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.mtI_score ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.mtI_score && (
            <div
              id="mtI_score-error"
              role="alert"
              data-testid="deployment_records-field-mtI_score-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.mtI_score}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes *
          </label>
          <textarea
            id="notes"
            data-testid="deployment_records-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <div
              id="notes-error"
              role="alert"
              data-testid="deployment_records-field-notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="deployment_records-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="deployment_records-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="deployment_records-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="deployment_records-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

