/**
 * AgentPerformanceMetricsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L40 (agent_performance_metrics)
 * Generated: 2026-03-12T09:01:01Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new agent_performance_metrics records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createAgentPerformanceMetricsRecord } from '@api/agent_performance_metricsApi';
import type { AgentPerformanceMetricsRecord, CreateAgentPerformanceMetricsRecordInput } from '@/types/agent_performance_metrics';

interface AgentPerformanceMetricsCreateFormProps {
  onSuccess: (record: AgentPerformanceMetricsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const AgentPerformanceMetricsCreateForm: React.FC<AgentPerformanceMetricsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('agent_performance_metrics.create_form');

  const [formData, setFormData] = useState<Partial<CreateAgentPerformanceMetricsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createAgentPerformanceMetricsRecord(formData as CreateAgentPerformanceMetricsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="agent_performance_metrics-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="agent_performance_metrics-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Id *
          </label>
          <input
            type="text"
            id="agent_id"
            data-testid="agent_performance_metrics-field-agent_id"
            value={(formData.agent_id as string) || ''}
            onChange={(e) => handleChange('agent_id', e.target.value)}
            aria-invalid={!!errors.agent_id}
            aria-describedby={errors.agent_id ? 'agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_id && (
            <div
              id="agent_id-error"
              role="alert"
              data-testid="agent_performance_metrics-field-agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Name *
          </label>
          <input
            type="text"
            id="agent_name"
            data-testid="agent_performance_metrics-field-agent_name"
            value={(formData.agent_name as string) || ''}
            onChange={(e) => handleChange('agent_name', e.target.value)}
            aria-invalid={!!errors.agent_name}
            aria-describedby={errors.agent_name ? 'agent_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_name && (
            <div
              id="agent_name-error"
              role="alert"
              data-testid="agent_performance_metrics-field-agent_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Type *
          </label>
          <input
            type="text"
            id="agent_type"
            data-testid="agent_performance_metrics-field-agent_type"
            value={(formData.agent_type as string) || ''}
            onChange={(e) => handleChange('agent_type', e.target.value)}
            aria-invalid={!!errors.agent_type}
            aria-describedby={errors.agent_type ? 'agent_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_type && (
            <div
              id="agent_type-error"
              role="alert"
              data-testid="agent_performance_metrics-field-agent_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="successful_deployments"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Successful Deployments *
          </label>
          <input
            type="number"
            id="successful_deployments"
            data-testid="agent_performance_metrics-field-successful_deployments"
            value={(formData.successful_deployments as number) || 0}
            onChange={(e) => handleChange('successful_deployments', Number(e.target.value))}
            aria-invalid={!!errors.successful_deployments}
            aria-describedby={errors.successful_deployments ? 'successful_deployments-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.successful_deployments ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.successful_deployments && (
            <div
              id="successful_deployments-error"
              role="alert"
              data-testid="agent_performance_metrics-field-successful_deployments-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.successful_deployments}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="failed_deployments"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Failed Deployments *
          </label>
          <input
            type="number"
            id="failed_deployments"
            data-testid="agent_performance_metrics-field-failed_deployments"
            value={(formData.failed_deployments as number) || 0}
            onChange={(e) => handleChange('failed_deployments', Number(e.target.value))}
            aria-invalid={!!errors.failed_deployments}
            aria-describedby={errors.failed_deployments ? 'failed_deployments-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.failed_deployments ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.failed_deployments && (
            <div
              id="failed_deployments-error"
              role="alert"
              data-testid="agent_performance_metrics-field-failed_deployments-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.failed_deployments}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="avg_deployment_time_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Avg Deployment Time Seconds *
          </label>
          <input
            type="number"
            id="avg_deployment_time_seconds"
            data-testid="agent_performance_metrics-field-avg_deployment_time_seconds"
            value={(formData.avg_deployment_time_seconds as number) || 0}
            onChange={(e) => handleChange('avg_deployment_time_seconds', Number(e.target.value))}
            aria-invalid={!!errors.avg_deployment_time_seconds}
            aria-describedby={errors.avg_deployment_time_seconds ? 'avg_deployment_time_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.avg_deployment_time_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.avg_deployment_time_seconds && (
            <div
              id="avg_deployment_time_seconds-error"
              role="alert"
              data-testid="agent_performance_metrics-field-avg_deployment_time_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.avg_deployment_time_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="avg_cost_per_deployment"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Avg Cost Per Deployment *
          </label>
          <input
            type="number"
            id="avg_cost_per_deployment"
            data-testid="agent_performance_metrics-field-avg_cost_per_deployment"
            value={(formData.avg_cost_per_deployment as number) || 0}
            onChange={(e) => handleChange('avg_cost_per_deployment', Number(e.target.value))}
            aria-invalid={!!errors.avg_cost_per_deployment}
            aria-describedby={errors.avg_cost_per_deployment ? 'avg_cost_per_deployment-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.avg_cost_per_deployment ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.avg_cost_per_deployment && (
            <div
              id="avg_cost_per_deployment-error"
              role="alert"
              data-testid="agent_performance_metrics-field-avg_cost_per_deployment-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.avg_cost_per_deployment}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="reliability_score"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Reliability Score *
          </label>
          <input
            type="number"
            id="reliability_score"
            data-testid="agent_performance_metrics-field-reliability_score"
            value={(formData.reliability_score as number) || 0}
            onChange={(e) => handleChange('reliability_score', Number(e.target.value))}
            aria-invalid={!!errors.reliability_score}
            aria-describedby={errors.reliability_score ? 'reliability_score-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.reliability_score ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.reliability_score && (
            <div
              id="reliability_score-error"
              role="alert"
              data-testid="agent_performance_metrics-field-reliability_score-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.reliability_score}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="speed_percentile"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Speed Percentile *
          </label>
          <input
            type="number"
            id="speed_percentile"
            data-testid="agent_performance_metrics-field-speed_percentile"
            value={(formData.speed_percentile as number) || 0}
            onChange={(e) => handleChange('speed_percentile', Number(e.target.value))}
            aria-invalid={!!errors.speed_percentile}
            aria-describedby={errors.speed_percentile ? 'speed_percentile-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.speed_percentile ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.speed_percentile && (
            <div
              id="speed_percentile-error"
              role="alert"
              data-testid="agent_performance_metrics-field-speed_percentile-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.speed_percentile}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_efficiency_percentile"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Efficiency Percentile *
          </label>
          <input
            type="number"
            id="cost_efficiency_percentile"
            data-testid="agent_performance_metrics-field-cost_efficiency_percentile"
            value={(formData.cost_efficiency_percentile as number) || 0}
            onChange={(e) => handleChange('cost_efficiency_percentile', Number(e.target.value))}
            aria-invalid={!!errors.cost_efficiency_percentile}
            aria-describedby={errors.cost_efficiency_percentile ? 'cost_efficiency_percentile-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_efficiency_percentile ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cost_efficiency_percentile && (
            <div
              id="cost_efficiency_percentile-error"
              role="alert"
              data-testid="agent_performance_metrics-field-cost_efficiency_percentile-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_efficiency_percentile}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="safety_incidents"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Safety Incidents *
          </label>
          <input
            type="number"
            id="safety_incidents"
            data-testid="agent_performance_metrics-field-safety_incidents"
            value={(formData.safety_incidents as number) || 0}
            onChange={(e) => handleChange('safety_incidents', Number(e.target.value))}
            aria-invalid={!!errors.safety_incidents}
            aria-describedby={errors.safety_incidents ? 'safety_incidents-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.safety_incidents ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.safety_incidents && (
            <div
              id="safety_incidents-error"
              role="alert"
              data-testid="agent_performance_metrics-field-safety_incidents-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.safety_incidents}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_rate"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Rate *
          </label>
          <input
            type="number"
            id="rollback_rate"
            data-testid="agent_performance_metrics-field-rollback_rate"
            value={(formData.rollback_rate as number) || 0}
            onChange={(e) => handleChange('rollback_rate', Number(e.target.value))}
            aria-invalid={!!errors.rollback_rate}
            aria-describedby={errors.rollback_rate ? 'rollback_rate-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rollback_rate ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rollback_rate && (
            <div
              id="rollback_rate-error"
              role="alert"
              data-testid="agent_performance_metrics-field-rollback_rate-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_rate}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="compliance_score"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Compliance Score *
          </label>
          <input
            type="number"
            id="compliance_score"
            data-testid="agent_performance_metrics-field-compliance_score"
            value={(formData.compliance_score as number) || 0}
            onChange={(e) => handleChange('compliance_score', Number(e.target.value))}
            aria-invalid={!!errors.compliance_score}
            aria-describedby={errors.compliance_score ? 'compliance_score-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.compliance_score ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.compliance_score && (
            <div
              id="compliance_score-error"
              role="alert"
              data-testid="agent_performance_metrics-field-compliance_score-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.compliance_score}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="agent_performance_metrics-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="agent_performance_metrics-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="agent_performance_metrics-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="agent_performance_metrics-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

