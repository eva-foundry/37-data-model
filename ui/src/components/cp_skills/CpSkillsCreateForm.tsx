/**
 * CpSkillsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L64 (cp_skills)
 * Generated: 2026-03-12T09:01:18Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new cp_skills records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createCpSkillsRecord } from '@api/cp_skillsApi';
import type { CpSkillsRecord, CreateCpSkillsRecordInput } from '@/types/cp_skills';

interface CpSkillsCreateFormProps {
  onSuccess: (record: CpSkillsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const CpSkillsCreateForm: React.FC<CpSkillsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('cp_skills.create_form');

  const [formData, setFormData] = useState<Partial<CreateCpSkillsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createCpSkillsRecord(formData as CreateCpSkillsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="cp_skills-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="cp_skills-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="cp_skills-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.label ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <div
              id="label-error"
              role="alert"
              data-testid="cp_skills-field-label-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="category"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Category *
          </label>
          <input
            type="text"
            id="category"
            data-testid="cp_skills-field-category"
            value={(formData.category as string) || ''}
            onChange={(e) => handleChange('category', e.target.value)}
            aria-invalid={!!errors.category}
            aria-describedby={errors.category ? 'category-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.category ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.category && (
            <div
              id="category-error"
              role="alert"
              data-testid="cp_skills-field-category-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.category}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="plane"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Plane *
          </label>
          <input
            type="text"
            id="plane"
            data-testid="cp_skills-field-plane"
            value={(formData.plane as string) || ''}
            onChange={(e) => handleChange('plane', e.target.value)}
            aria-invalid={!!errors.plane}
            aria-describedby={errors.plane ? 'plane-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.plane ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.plane && (
            <div
              id="plane-error"
              role="alert"
              data-testid="cp_skills-field-plane-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.plane}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="input_schema"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Input Schema *
          </label>
          <textarea
            id="input_schema"
            data-testid="cp_skills-field-input_schema"
            value={(formData.input_schema as string) || ''}
            onChange={(e) => handleChange('input_schema', e.target.value)}
            aria-invalid={!!errors.input_schema}
            aria-describedby={errors.input_schema ? 'input_schema-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.input_schema ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.input_schema && (
            <div
              id="input_schema-error"
              role="alert"
              data-testid="cp_skills-field-input_schema-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.input_schema}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="output_schema"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Output Schema *
          </label>
          <textarea
            id="output_schema"
            data-testid="cp_skills-field-output_schema"
            value={(formData.output_schema as string) || ''}
            onChange={(e) => handleChange('output_schema', e.target.value)}
            aria-invalid={!!errors.output_schema}
            aria-describedby={errors.output_schema ? 'output_schema-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.output_schema ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.output_schema && (
            <div
              id="output_schema-error"
              role="alert"
              data-testid="cp_skills-field-output_schema-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.output_schema}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="used_by_agents"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Used By Agents *
          </label>
          <textarea
            id="used_by_agents"
            data-testid="cp_skills-field-used_by_agents"
            value={(formData.used_by_agents as string) || ''}
            onChange={(e) => handleChange('used_by_agents', e.target.value)}
            aria-invalid={!!errors.used_by_agents}
            aria-describedby={errors.used_by_agents ? 'used_by_agents-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.used_by_agents ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.used_by_agents && (
            <div
              id="used_by_agents-error"
              role="alert"
              data-testid="cp_skills-field-used_by_agents-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.used_by_agents}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="cp_skills-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="cp_skills-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes *
          </label>
          <textarea
            id="notes"
            data-testid="cp_skills-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <div
              id="notes-error"
              role="alert"
              data-testid="cp_skills-field-notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="cp_skills-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="cp_skills-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="cp_skills-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="cp_skills-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

