/**
 * EvidenceCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L5 (evidence)
 * Generated: 2026-03-11T18:59:04Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new evidence records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createEvidenceRecord } from '@api/evidenceApi';
import type { EvidenceRecord, CreateEvidenceRecordInput } from '@/types/evidence';

interface EvidenceCreateFormProps {
  onSuccess: (record: EvidenceRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const EvidenceCreateForm: React.FC<EvidenceCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('evidence.create_form');

  const [formData, setFormData] = useState<Partial<CreateEvidenceRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
        if (!formData.sprint_id) {
      newErrors.sprint_id = t('errors.required');
    }
    if (!formData.story_id) {
      newErrors.story_id = t('errors.required');
    }
    if (!formData.phase) {
      newErrors.phase = t('errors.required');
    }
    if (!formData.validation) {
      newErrors.validation = t('errors.required');
    }
    if (!formData.metrics) {
      newErrors.metrics = t('errors.required');
    }
    if (!formData.artifacts) {
      newErrors.artifacts = t('errors.required');
    }
    if (!formData.commits) {
      newErrors.commits = t('errors.required');
    }
    if (!formData.completed_at) {
      newErrors.completed_at = t('errors.required');
    }
    if (!formData.is_active) {
      newErrors.is_active = t('errors.required');
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createEvidenceRecord(formData as CreateEvidenceRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="evidence-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="evidence-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="sprint_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint Id *
          </label>
          <input
            type="text"
            id="sprint_id"
            data-testid="evidence-field-sprint_id"
            value={(formData.sprint_id as string) || ''}
            onChange={(e) => handleChange('sprint_id', e.target.value)}
            aria-invalid={!!errors.sprint_id}
            aria-describedby={errors.sprint_id ? 'sprint_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprint_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint_id && (
            <div
              id="sprint_id-error"
              role="alert"
              data-testid="evidence-field-sprint_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="story_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Story Id *
          </label>
          <input
            type="text"
            id="story_id"
            data-testid="evidence-field-story_id"
            value={(formData.story_id as string) || ''}
            onChange={(e) => handleChange('story_id', e.target.value)}
            aria-invalid={!!errors.story_id}
            aria-describedby={errors.story_id ? 'story_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.story_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.story_id && (
            <div
              id="story_id-error"
              role="alert"
              data-testid="evidence-field-story_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.story_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="phase"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Phase *
          </label>
          <input
            type="text"
            id="phase"
            data-testid="evidence-field-phase"
            value={(formData.phase as string) || ''}
            onChange={(e) => handleChange('phase', e.target.value)}
            aria-invalid={!!errors.phase}
            aria-describedby={errors.phase ? 'phase-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.phase ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.phase && (
            <div
              id="phase-error"
              role="alert"
              data-testid="evidence-field-phase-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.phase}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="validation"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Validation *
          </label>
          <textarea
            id="validation"
            data-testid="evidence-field-validation"
            value={(formData.validation as string) || ''}
            onChange={(e) => handleChange('validation', e.target.value)}
            aria-invalid={!!errors.validation}
            aria-describedby={errors.validation ? 'validation-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.validation ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.validation && (
            <div
              id="validation-error"
              role="alert"
              data-testid="evidence-field-validation-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.validation}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="metrics"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Metrics *
          </label>
          <textarea
            id="metrics"
            data-testid="evidence-field-metrics"
            value={(formData.metrics as string) || ''}
            onChange={(e) => handleChange('metrics', e.target.value)}
            aria-invalid={!!errors.metrics}
            aria-describedby={errors.metrics ? 'metrics-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.metrics ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.metrics && (
            <div
              id="metrics-error"
              role="alert"
              data-testid="evidence-field-metrics-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.metrics}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="artifacts"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Artifacts *
          </label>
          <textarea
            id="artifacts"
            data-testid="evidence-field-artifacts"
            value={(formData.artifacts as string) || ''}
            onChange={(e) => handleChange('artifacts', e.target.value)}
            aria-invalid={!!errors.artifacts}
            aria-describedby={errors.artifacts ? 'artifacts-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.artifacts ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.artifacts && (
            <div
              id="artifacts-error"
              role="alert"
              data-testid="evidence-field-artifacts-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.artifacts}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="commits"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Commits *
          </label>
          <textarea
            id="commits"
            data-testid="evidence-field-commits"
            value={(formData.commits as string) || ''}
            onChange={(e) => handleChange('commits', e.target.value)}
            aria-invalid={!!errors.commits}
            aria-describedby={errors.commits ? 'commits-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.commits ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.commits && (
            <div
              id="commits-error"
              role="alert"
              data-testid="evidence-field-commits-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.commits}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="completed_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Completed At *
          </label>
          <input
            type="datetime-local"
            id="completed_at"
            data-testid="evidence-field-completed_at"
            value={(formData.completed_at as string) || ''}
            onChange={(e) => handleChange('completed_at', e.target.value)}
            aria-invalid={!!errors.completed_at}
            aria-describedby={errors.completed_at ? 'completed_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.completed_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.completed_at && (
            <div
              id="completed_at-error"
              role="alert"
              data-testid="evidence-field-completed_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.completed_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="evidence-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="evidence-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="evidence-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="evidence-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

