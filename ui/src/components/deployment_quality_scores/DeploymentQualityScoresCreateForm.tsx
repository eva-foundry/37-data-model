/**
 * DeploymentQualityScoresCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L83 (deployment_quality_scores)
 * Generated: 2026-03-12T09:01:30Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new deployment_quality_scores records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createDeploymentQualityScoresRecord } from '@api/deployment_quality_scoresApi';
import type { DeploymentQualityScoresRecord, CreateDeploymentQualityScoresRecordInput } from '@/types/deployment_quality_scores';

interface DeploymentQualityScoresCreateFormProps {
  onSuccess: (record: DeploymentQualityScoresRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const DeploymentQualityScoresCreateForm: React.FC<DeploymentQualityScoresCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('deployment_quality_scores.create_form');

  const [formData, setFormData] = useState<Partial<CreateDeploymentQualityScoresRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createDeploymentQualityScoresRecord(formData as CreateDeploymentQualityScoresRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="deployment_quality_scores-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="deployment_quality_scores-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="deployment_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Id *
          </label>
          <input
            type="text"
            id="deployment_id"
            data-testid="deployment_quality_scores-field-deployment_id"
            value={(formData.deployment_id as string) || ''}
            onChange={(e) => handleChange('deployment_id', e.target.value)}
            aria-invalid={!!errors.deployment_id}
            aria-describedby={errors.deployment_id ? 'deployment_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deployment_id && (
            <div
              id="deployment_id-error"
              role="alert"
              data-testid="deployment_quality_scores-field-deployment_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deployment_timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Timestamp *
          </label>
          <input
            type="datetime-local"
            id="deployment_timestamp"
            data-testid="deployment_quality_scores-field-deployment_timestamp"
            value={(formData.deployment_timestamp as string) || ''}
            onChange={(e) => handleChange('deployment_timestamp', e.target.value)}
            aria-invalid={!!errors.deployment_timestamp}
            aria-describedby={errors.deployment_timestamp ? 'deployment_timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deployment_timestamp && (
            <div
              id="deployment_timestamp-error"
              role="alert"
              data-testid="deployment_quality_scores-field-deployment_timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="environment"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Environment *
          </label>
          <input
            type="text"
            id="environment"
            data-testid="deployment_quality_scores-field-environment"
            value={(formData.environment as string) || ''}
            onChange={(e) => handleChange('environment', e.target.value)}
            aria-invalid={!!errors.environment}
            aria-describedby={errors.environment ? 'environment-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.environment ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.environment && (
            <div
              id="environment-error"
              role="alert"
              data-testid="deployment_quality_scores-field-environment-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.environment}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Id *
          </label>
          <input
            type="text"
            id="agent_id"
            data-testid="deployment_quality_scores-field-agent_id"
            value={(formData.agent_id as string) || ''}
            onChange={(e) => handleChange('agent_id', e.target.value)}
            aria-invalid={!!errors.agent_id}
            aria-describedby={errors.agent_id ? 'agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_id && (
            <div
              id="agent_id-error"
              role="alert"
              data-testid="deployment_quality_scores-field-agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resources_deployed"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resources Deployed *
          </label>
          <textarea
            id="resources_deployed"
            data-testid="deployment_quality_scores-field-resources_deployed"
            value={(formData.resources_deployed as string) || ''}
            onChange={(e) => handleChange('resources_deployed', e.target.value)}
            aria-invalid={!!errors.resources_deployed}
            aria-describedby={errors.resources_deployed ? 'resources_deployed-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resources_deployed ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.resources_deployed && (
            <div
              id="resources_deployed-error"
              role="alert"
              data-testid="deployment_quality_scores-field-resources_deployed-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resources_deployed}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="quality_dimensions"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Quality Dimensions *
          </label>
          <textarea
            id="quality_dimensions"
            data-testid="deployment_quality_scores-field-quality_dimensions"
            value={(formData.quality_dimensions as string) || ''}
            onChange={(e) => handleChange('quality_dimensions', e.target.value)}
            aria-invalid={!!errors.quality_dimensions}
            aria-describedby={errors.quality_dimensions ? 'quality_dimensions-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.quality_dimensions ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.quality_dimensions && (
            <div
              id="quality_dimensions-error"
              role="alert"
              data-testid="deployment_quality_scores-field-quality_dimensions-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.quality_dimensions}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="overall_quality_score"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Overall Quality Score *
          </label>
          <input
            type="number"
            id="overall_quality_score"
            data-testid="deployment_quality_scores-field-overall_quality_score"
            value={(formData.overall_quality_score as number) || 0}
            onChange={(e) => handleChange('overall_quality_score', Number(e.target.value))}
            aria-invalid={!!errors.overall_quality_score}
            aria-describedby={errors.overall_quality_score ? 'overall_quality_score-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.overall_quality_score ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.overall_quality_score && (
            <div
              id="overall_quality_score-error"
              role="alert"
              data-testid="deployment_quality_scores-field-overall_quality_score-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.overall_quality_score}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="quality_grade"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Quality Grade *
          </label>
          <input
            type="text"
            id="quality_grade"
            data-testid="deployment_quality_scores-field-quality_grade"
            value={(formData.quality_grade as string) || ''}
            onChange={(e) => handleChange('quality_grade', e.target.value)}
            aria-invalid={!!errors.quality_grade}
            aria-describedby={errors.quality_grade ? 'quality_grade-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.quality_grade ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.quality_grade && (
            <div
              id="quality_grade-error"
              role="alert"
              data-testid="deployment_quality_scores-field-quality_grade-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.quality_grade}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="quality_breakdown"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Quality Breakdown *
          </label>
          <textarea
            id="quality_breakdown"
            data-testid="deployment_quality_scores-field-quality_breakdown"
            value={(formData.quality_breakdown as string) || ''}
            onChange={(e) => handleChange('quality_breakdown', e.target.value)}
            aria-invalid={!!errors.quality_breakdown}
            aria-describedby={errors.quality_breakdown ? 'quality_breakdown-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.quality_breakdown ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.quality_breakdown && (
            <div
              id="quality_breakdown-error"
              role="alert"
              data-testid="deployment_quality_scores-field-quality_breakdown-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.quality_breakdown}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="anomalies_detected"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Anomalies Detected *
          </label>
          <textarea
            id="anomalies_detected"
            data-testid="deployment_quality_scores-field-anomalies_detected"
            value={(formData.anomalies_detected as string) || ''}
            onChange={(e) => handleChange('anomalies_detected', e.target.value)}
            aria-invalid={!!errors.anomalies_detected}
            aria-describedby={errors.anomalies_detected ? 'anomalies_detected-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.anomalies_detected ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.anomalies_detected && (
            <div
              id="anomalies_detected-error"
              role="alert"
              data-testid="deployment_quality_scores-field-anomalies_detected-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.anomalies_detected}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="recommendations"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Recommendations *
          </label>
          <textarea
            id="recommendations"
            data-testid="deployment_quality_scores-field-recommendations"
            value={(formData.recommendations as string) || ''}
            onChange={(e) => handleChange('recommendations', e.target.value)}
            aria-invalid={!!errors.recommendations}
            aria-describedby={errors.recommendations ? 'recommendations-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.recommendations ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.recommendations && (
            <div
              id="recommendations-error"
              role="alert"
              data-testid="deployment_quality_scores-field-recommendations-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.recommendations}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deployment_insights"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Insights *
          </label>
          <textarea
            id="deployment_insights"
            data-testid="deployment_quality_scores-field-deployment_insights"
            value={(formData.deployment_insights as string) || ''}
            onChange={(e) => handleChange('deployment_insights', e.target.value)}
            aria-invalid={!!errors.deployment_insights}
            aria-describedby={errors.deployment_insights ? 'deployment_insights-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_insights ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.deployment_insights && (
            <div
              id="deployment_insights-error"
              role="alert"
              data-testid="deployment_quality_scores-field-deployment_insights-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_insights}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="calculated_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Calculated At *
          </label>
          <input
            type="datetime-local"
            id="calculated_at"
            data-testid="deployment_quality_scores-field-calculated_at"
            value={(formData.calculated_at as string) || ''}
            onChange={(e) => handleChange('calculated_at', e.target.value)}
            aria-invalid={!!errors.calculated_at}
            aria-describedby={errors.calculated_at ? 'calculated_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.calculated_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.calculated_at && (
            <div
              id="calculated_at-error"
              role="alert"
              data-testid="deployment_quality_scores-field-calculated_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.calculated_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="deployment_quality_scores-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="deployment_quality_scores-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="deployment_quality_scores-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="deployment_quality_scores-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

