/**
 * SessionTranscriptsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L25 (session_transcripts)
 * Generated: 2026-03-12T09:00:51Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new session_transcripts records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createSessionTranscriptsRecord } from '@api/session_transcriptsApi';
import type { SessionTranscriptsRecord, CreateSessionTranscriptsRecordInput } from '@/types/session_transcripts';

interface SessionTranscriptsCreateFormProps {
  onSuccess: (record: SessionTranscriptsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const SessionTranscriptsCreateForm: React.FC<SessionTranscriptsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('session_transcripts.create_form');

  const [formData, setFormData] = useState<Partial<CreateSessionTranscriptsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createSessionTranscriptsRecord(formData as CreateSessionTranscriptsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="session_transcripts-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="session_transcripts-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="workflow_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Workflow Id *
          </label>
          <input
            type="text"
            id="workflow_id"
            data-testid="session_transcripts-field-workflow_id"
            value={(formData.workflow_id as string) || ''}
            onChange={(e) => handleChange('workflow_id', e.target.value)}
            aria-invalid={!!errors.workflow_id}
            aria-describedby={errors.workflow_id ? 'workflow_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.workflow_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.workflow_id && (
            <div
              id="workflow_id-error"
              role="alert"
              data-testid="session_transcripts-field-workflow_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.workflow_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="persona_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Persona Id *
          </label>
          <input
            type="text"
            id="persona_id"
            data-testid="session_transcripts-field-persona_id"
            value={(formData.persona_id as string) || ''}
            onChange={(e) => handleChange('persona_id', e.target.value)}
            aria-invalid={!!errors.persona_id}
            aria-describedby={errors.persona_id ? 'persona_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.persona_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.persona_id && (
            <div
              id="persona_id-error"
              role="alert"
              data-testid="session_transcripts-field-persona_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.persona_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="session_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Session Start *
          </label>
          <input
            type="datetime-local"
            id="session_start"
            data-testid="session_transcripts-field-session_start"
            value={(formData.session_start as string) || ''}
            onChange={(e) => handleChange('session_start', e.target.value)}
            aria-invalid={!!errors.session_start}
            aria-describedby={errors.session_start ? 'session_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.session_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.session_start && (
            <div
              id="session_start-error"
              role="alert"
              data-testid="session_transcripts-field-session_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.session_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="session_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Session End *
          </label>
          <input
            type="datetime-local"
            id="session_end"
            data-testid="session_transcripts-field-session_end"
            value={(formData.session_end as string) || ''}
            onChange={(e) => handleChange('session_end', e.target.value)}
            aria-invalid={!!errors.session_end}
            aria-describedby={errors.session_end ? 'session_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.session_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.session_end && (
            <div
              id="session_end-error"
              role="alert"
              data-testid="session_transcripts-field-session_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.session_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="duration_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Duration Seconds *
          </label>
          <input
            type="number"
            id="duration_seconds"
            data-testid="session_transcripts-field-duration_seconds"
            value={(formData.duration_seconds as number) || 0}
            onChange={(e) => handleChange('duration_seconds', Number(e.target.value))}
            aria-invalid={!!errors.duration_seconds}
            aria-describedby={errors.duration_seconds ? 'duration_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.duration_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.duration_seconds && (
            <div
              id="duration_seconds-error"
              role="alert"
              data-testid="session_transcripts-field-duration_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.duration_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="turn_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Turn Count *
          </label>
          <input
            type="number"
            id="turn_count"
            data-testid="session_transcripts-field-turn_count"
            value={(formData.turn_count as number) || 0}
            onChange={(e) => handleChange('turn_count', Number(e.target.value))}
            aria-invalid={!!errors.turn_count}
            aria-describedby={errors.turn_count ? 'turn_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.turn_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.turn_count && (
            <div
              id="turn_count-error"
              role="alert"
              data-testid="session_transcripts-field-turn_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.turn_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="transcript"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Transcript *
          </label>
          <textarea
            id="transcript"
            data-testid="session_transcripts-field-transcript"
            value={(formData.transcript as string) || ''}
            onChange={(e) => handleChange('transcript', e.target.value)}
            aria-invalid={!!errors.transcript}
            aria-describedby={errors.transcript ? 'transcript-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.transcript ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.transcript && (
            <div
              id="transcript-error"
              role="alert"
              data-testid="session_transcripts-field-transcript-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.transcript}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="token_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Token Count *
          </label>
          <input
            type="number"
            id="token_count"
            data-testid="session_transcripts-field-token_count"
            value={(formData.token_count as number) || 0}
            onChange={(e) => handleChange('token_count', Number(e.target.value))}
            aria-invalid={!!errors.token_count}
            aria-describedby={errors.token_count ? 'token_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.token_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.token_count && (
            <div
              id="token_count-error"
              role="alert"
              data-testid="session_transcripts-field-token_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.token_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="session_transcripts-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="session_transcripts-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="session_transcripts-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="session_transcripts-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

