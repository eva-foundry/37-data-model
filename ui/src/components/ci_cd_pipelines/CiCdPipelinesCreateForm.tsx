/**
 * CiCdPipelinesCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L15 (ci_cd_pipelines)
 * Generated: 2026-03-12T09:00:44Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new ci_cd_pipelines records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createCiCdPipelinesRecord } from '@api/ci_cd_pipelinesApi';
import type { CiCdPipelinesRecord, CreateCiCdPipelinesRecordInput } from '@/types/ci_cd_pipelines';

interface CiCdPipelinesCreateFormProps {
  onSuccess: (record: CiCdPipelinesRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const CiCdPipelinesCreateForm: React.FC<CiCdPipelinesCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('ci_cd_pipelines.create_form');

  const [formData, setFormData] = useState<Partial<CreateCiCdPipelinesRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createCiCdPipelinesRecord(formData as CreateCiCdPipelinesRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="ci_cd_pipelines-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="ci_cd_pipelines-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="repo_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Repo Id *
          </label>
          <input
            type="text"
            id="repo_id"
            data-testid="ci_cd_pipelines-field-repo_id"
            value={(formData.repo_id as string) || ''}
            onChange={(e) => handleChange('repo_id', e.target.value)}
            aria-invalid={!!errors.repo_id}
            aria-describedby={errors.repo_id ? 'repo_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.repo_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.repo_id && (
            <div
              id="repo_id-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-repo_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.repo_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="ci_cd_pipelines-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="pipeline_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Pipeline Type *
          </label>
          <input
            type="text"
            id="pipeline_type"
            data-testid="ci_cd_pipelines-field-pipeline_type"
            value={(formData.pipeline_type as string) || ''}
            onChange={(e) => handleChange('pipeline_type', e.target.value)}
            aria-invalid={!!errors.pipeline_type}
            aria-describedby={errors.pipeline_type ? 'pipeline_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.pipeline_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.pipeline_type && (
            <div
              id="pipeline_type-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-pipeline_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.pipeline_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="trigger_on"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Trigger On *
          </label>
          <textarea
            id="trigger_on"
            data-testid="ci_cd_pipelines-field-trigger_on"
            value={(formData.trigger_on as string) || ''}
            onChange={(e) => handleChange('trigger_on', e.target.value)}
            aria-invalid={!!errors.trigger_on}
            aria-describedby={errors.trigger_on ? 'trigger_on-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.trigger_on ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.trigger_on && (
            <div
              id="trigger_on-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-trigger_on-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.trigger_on}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="stages"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Stages *
          </label>
          <textarea
            id="stages"
            data-testid="ci_cd_pipelines-field-stages"
            value={(formData.stages as string) || ''}
            onChange={(e) => handleChange('stages', e.target.value)}
            aria-invalid={!!errors.stages}
            aria-describedby={errors.stages ? 'stages-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.stages ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.stages && (
            <div
              id="stages-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-stages-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.stages}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="build_command"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Build Command *
          </label>
          <input
            type="text"
            id="build_command"
            data-testid="ci_cd_pipelines-field-build_command"
            value={(formData.build_command as string) || ''}
            onChange={(e) => handleChange('build_command', e.target.value)}
            aria-invalid={!!errors.build_command}
            aria-describedby={errors.build_command ? 'build_command-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.build_command ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.build_command && (
            <div
              id="build_command-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-build_command-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.build_command}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deploy_target"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deploy Target *
          </label>
          <input
            type="text"
            id="deploy_target"
            data-testid="ci_cd_pipelines-field-deploy_target"
            value={(formData.deploy_target as string) || ''}
            onChange={(e) => handleChange('deploy_target', e.target.value)}
            aria-invalid={!!errors.deploy_target}
            aria-describedby={errors.deploy_target ? 'deploy_target-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deploy_target ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deploy_target && (
            <div
              id="deploy_target-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-deploy_target-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deploy_target}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="success_rate_percent"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Success Rate Percent *
          </label>
          <input
            type="number"
            id="success_rate_percent"
            data-testid="ci_cd_pipelines-field-success_rate_percent"
            value={(formData.success_rate_percent as number) || 0}
            onChange={(e) => handleChange('success_rate_percent', Number(e.target.value))}
            aria-invalid={!!errors.success_rate_percent}
            aria-describedby={errors.success_rate_percent ? 'success_rate_percent-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.success_rate_percent ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.success_rate_percent && (
            <div
              id="success_rate_percent-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-success_rate_percent-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.success_rate_percent}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="avg_duration_minutes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Avg Duration Minutes *
          </label>
          <input
            type="number"
            id="avg_duration_minutes"
            data-testid="ci_cd_pipelines-field-avg_duration_minutes"
            value={(formData.avg_duration_minutes as number) || 0}
            onChange={(e) => handleChange('avg_duration_minutes', Number(e.target.value))}
            aria-invalid={!!errors.avg_duration_minutes}
            aria-describedby={errors.avg_duration_minutes ? 'avg_duration_minutes-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.avg_duration_minutes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.avg_duration_minutes && (
            <div
              id="avg_duration_minutes-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-avg_duration_minutes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.avg_duration_minutes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_run"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Run *
          </label>
          <input
            type="datetime-local"
            id="last_run"
            data-testid="ci_cd_pipelines-field-last_run"
            value={(formData.last_run as string) || ''}
            onChange={(e) => handleChange('last_run', e.target.value)}
            aria-invalid={!!errors.last_run}
            aria-describedby={errors.last_run ? 'last_run-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_run ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_run && (
            <div
              id="last_run-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-last_run-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_run}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_run_status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Run Status *
          </label>
          <input
            type="text"
            id="last_run_status"
            data-testid="ci_cd_pipelines-field-last_run_status"
            value={(formData.last_run_status as string) || ''}
            onChange={(e) => handleChange('last_run_status', e.target.value)}
            aria-invalid={!!errors.last_run_status}
            aria-describedby={errors.last_run_status ? 'last_run_status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_run_status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_run_status && (
            <div
              id="last_run_status-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-last_run_status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_run_status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="ci_cd_pipelines-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="ci_cd_pipelines-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="ci_cd_pipelines-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="ci_cd_pipelines-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

