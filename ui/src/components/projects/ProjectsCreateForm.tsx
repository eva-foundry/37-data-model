/**
 * ProjectsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L25 (projects)
 * Generated: 2026-03-11T00:49:10Z
 * Generator: screens-machine-v2.0.0
 * 
 * Form for creating new projects records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLang } from '@context/LangContext';
import { createProjectsRecord } from '@api/projectsApi';
import type { ProjectsRecord, CreateProjectsRecordInput } from '@/types/projects';

interface ProjectsCreateFormProps {
  onSuccess: (record: ProjectsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const ProjectsCreateForm: React.FC<ProjectsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const { lang } = useLang();

  const t = {
    title:       lang === 'fr' ? 'Créer un nouvel enregistrement' : 'Create new record',
    submit:      lang === 'fr' ? 'Créer' : 'Create',
    cancel:      lang === 'fr' ? 'Annuler' : 'Cancel',
    required:    lang === 'fr' ? 'Champ obligatoire' : 'This field is required',
    submitting:  lang === 'fr' ? 'Création en cours...' : 'Creating...',
    error:       lang === 'fr' ? 'Erreur lors de la création' : 'Failed to create record',
  };

  const [formData, setFormData] = useState<Partial<CreateProjectsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
        if (!formData.id) {
      newErrors.id = t.required;
    }
    if (!formData.goal) {
      newErrors.goal = t.required;
    }
    if (!formData.maturity) {
      newErrors.maturity = t.required;
    }
    if (!formData.label_fr) {
      newErrors.label_fr = t.required;
    }
    if (!formData.folder) {
      newErrors.folder = t.required;
    }
    if (!formData.status) {
      newErrors.status = t.required;
    }
    if (!formData.category) {
      newErrors.category = t.required;
    }
    if (!formData.label) {
      newErrors.label = t.required;
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createProjectsRecord(formData as CreateProjectsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="projects-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t.title}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="projects-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t.error}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
              <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
        <div>
          <label
            htmlFor="id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Id *
          </label>
          <input
            type="text"
            id="id"
            data-testid="projects-field-id"
            value={(formData.id as string) || ''}
            onChange={(e) => handleChange('id', e.target.value)}
            aria-invalid={!!errors.id}
            aria-describedby={errors.id ? 'id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.id ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.id && (
            <p
              id="id-error"
              data-testid="projects-error-id"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.id}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="phase"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Phase
          </label>
          <textarea
            id="phase"
            data-testid="projects-field-phase"
            value={(formData.phase as string) || ''}
            onChange={(e) => handleChange('phase', e.target.value)}
            aria-invalid={!!errors.phase}
            aria-describedby={errors.phase ? 'phase-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.phase ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.phase && (
            <p
              id="phase-error"
              data-testid="projects-error-phase"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.phase}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="goal"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Goal *
          </label>
          <textarea
            id="goal"
            data-testid="projects-field-goal"
            value={(formData.goal as string) || ''}
            onChange={(e) => handleChange('goal', e.target.value)}
            aria-invalid={!!errors.goal}
            aria-describedby={errors.goal ? 'goal-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.goal ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.goal && (
            <p
              id="goal-error"
              data-testid="projects-error-goal"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.goal}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="pbi_total"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Pbi Total
          </label>
          <input
            type="text"
            id="pbi_total"
            data-testid="projects-field-pbi_total"
            value={(formData.pbi_total as string) || ''}
            onChange={(e) => handleChange('pbi_total', e.target.value)}
            aria-invalid={!!errors.pbi_total}
            aria-describedby={errors.pbi_total ? 'pbi_total-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.pbi_total ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.pbi_total && (
            <p
              id="pbi_total-error"
              data-testid="projects-error-pbi_total"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.pbi_total}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="maturity"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Maturity *
          </label>
          <input
            type="text"
            id="maturity"
            data-testid="projects-field-maturity"
            value={(formData.maturity as string) || ''}
            onChange={(e) => handleChange('maturity', e.target.value)}
            aria-invalid={!!errors.maturity}
            aria-describedby={errors.maturity ? 'maturity-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.maturity ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.maturity && (
            <p
              id="maturity-error"
              data-testid="projects-error-maturity"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.maturity}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="sprint_context"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint Context
          </label>
          <input
            type="text"
            id="sprint_context"
            data-testid="projects-field-sprint_context"
            value={(formData.sprint_context as string) || ''}
            onChange={(e) => handleChange('sprint_context', e.target.value)}
            aria-invalid={!!errors.sprint_context}
            aria-describedby={errors.sprint_context ? 'sprint_context-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.sprint_context ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint_context && (
            <p
              id="sprint_context-error"
              data-testid="projects-error-sprint_context"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint_context}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="label_fr"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label Fr *
          </label>
          <input
            type="text"
            id="label_fr"
            data-testid="projects-field-label_fr"
            value={(formData.label_fr as string) || ''}
            onChange={(e) => handleChange('label_fr', e.target.value)}
            aria-invalid={!!errors.label_fr}
            aria-describedby={errors.label_fr ? 'label_fr-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.label_fr ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label_fr && (
            <p
              id="label_fr-error"
              data-testid="projects-error-label_fr"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label_fr}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="ado_epic_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Ado Epic Id
          </label>
          <input
            type="text"
            id="ado_epic_id"
            data-testid="projects-field-ado_epic_id"
            value={(formData.ado_epic_id as string) || ''}
            onChange={(e) => handleChange('ado_epic_id', e.target.value)}
            aria-invalid={!!errors.ado_epic_id}
            aria-describedby={errors.ado_epic_id ? 'ado_epic_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.ado_epic_id ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ado_epic_id && (
            <p
              id="ado_epic_id-error"
              data-testid="projects-error-ado_epic_id"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ado_epic_id}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="ado_project"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Ado Project
          </label>
          <input
            type="text"
            id="ado_project"
            data-testid="projects-field-ado_project"
            value={(formData.ado_project as string) || ''}
            onChange={(e) => handleChange('ado_project', e.target.value)}
            aria-invalid={!!errors.ado_project}
            aria-describedby={errors.ado_project ? 'ado_project-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.ado_project ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ado_project && (
            <p
              id="ado_project-error"
              data-testid="projects-error-ado_project"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ado_project}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="folder"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Folder *
          </label>
          <input
            type="text"
            id="folder"
            data-testid="projects-field-folder"
            value={(formData.folder as string) || ''}
            onChange={(e) => handleChange('folder', e.target.value)}
            aria-invalid={!!errors.folder}
            aria-describedby={errors.folder ? 'folder-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.folder ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.folder && (
            <p
              id="folder-error"
              data-testid="projects-error-folder"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.folder}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes
          </label>
          <textarea
            id="notes"
            data-testid="projects-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.notes ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <p
              id="notes-error"
              data-testid="projects-error-notes"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="depends_on"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Depends On
          </label>
          <textarea
            id="depends_on"
            data-testid="projects-field-depends_on"
            value={(formData.depends_on as string) || ''}
            onChange={(e) => handleChange('depends_on', e.target.value)}
            aria-invalid={!!errors.depends_on}
            aria-describedby={errors.depends_on ? 'depends_on-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.depends_on ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.depends_on && (
            <p
              id="depends_on-error"
              data-testid="projects-error-depends_on"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.depends_on}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="projects-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.status ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <p
              id="status-error"
              data-testid="projects-error-status"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="github_repo"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Github Repo
          </label>
          <input
            type="text"
            id="github_repo"
            data-testid="projects-field-github_repo"
            value={(formData.github_repo as string) || ''}
            onChange={(e) => handleChange('github_repo', e.target.value)}
            aria-invalid={!!errors.github_repo}
            aria-describedby={errors.github_repo ? 'github_repo-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.github_repo ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.github_repo && (
            <p
              id="github_repo-error"
              data-testid="projects-error-github_repo"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.github_repo}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="category"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Category *
          </label>
          <input
            type="text"
            id="category"
            data-testid="projects-field-category"
            value={(formData.category as string) || ''}
            onChange={(e) => handleChange('category', e.target.value)}
            aria-invalid={!!errors.category}
            aria-describedby={errors.category ? 'category-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.category ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.category && (
            <p
              id="category-error"
              data-testid="projects-error-category"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.category}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="pbi_done"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Pbi Done
          </label>
          <input
            type="text"
            id="pbi_done"
            data-testid="projects-field-pbi_done"
            value={(formData.pbi_done as string) || ''}
            onChange={(e) => handleChange('pbi_done', e.target.value)}
            aria-invalid={!!errors.pbi_done}
            aria-describedby={errors.pbi_done ? 'pbi_done-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.pbi_done ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.pbi_done && (
            <p
              id="pbi_done-error"
              data-testid="projects-error-pbi_done"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.pbi_done}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="projects-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.label ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <p
              id="label-error"
              data-testid="projects-error-label"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="wbs_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Wbs Id
          </label>
          <input
            type="text"
            id="wbs_id"
            data-testid="projects-field-wbs_id"
            value={(formData.wbs_id as string) || ''}
            onChange={(e) => handleChange('wbs_id', e.target.value)}
            aria-invalid={!!errors.wbs_id}
            aria-describedby={errors.wbs_id ? 'wbs_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.wbs_id ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.wbs_id && (
            <p
              id="wbs_id-error"
              data-testid="projects-error-wbs_id"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.wbs_id}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="blocked_by"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Blocked By
          </label>
          <textarea
            id="blocked_by"
            data-testid="projects-field-blocked_by"
            value={(formData.blocked_by as string) || ''}
            onChange={(e) => handleChange('blocked_by', e.target.value)}
            aria-invalid={!!errors.blocked_by}
            aria-describedby={errors.blocked_by ? 'blocked_by-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.blocked_by ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.blocked_by && (
            <p
              id="blocked_by-error"
              data-testid="projects-error-blocked_by"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.blocked_by}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="services"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Services
          </label>
          <textarea
            id="services"
            data-testid="projects-field-services"
            value={(formData.services as string) || ''}
            onChange={(e) => handleChange('services', e.target.value)}
            aria-invalid={!!errors.services}
            aria-describedby={errors.services ? 'services-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.services ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.services && (
            <p
              id="services-error"
              data-testid="projects-error-services"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.services}
            </p>
          )}
        </div>
      </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="projects-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t.cancel}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="projects-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t.submitting : t.submit}
        </button>
      </div>
    </form>
  );
};

