/**
 * AgenticWorkflowsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L24 (agentic_workflows)
 * Generated: 2026-03-12T09:00:50Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new agentic_workflows records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createAgenticWorkflowsRecord } from '@api/agentic_workflowsApi';
import type { AgenticWorkflowsRecord, CreateAgenticWorkflowsRecordInput } from '@/types/agentic_workflows';

interface AgenticWorkflowsCreateFormProps {
  onSuccess: (record: AgenticWorkflowsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const AgenticWorkflowsCreateForm: React.FC<AgenticWorkflowsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('agentic_workflows.create_form');

  const [formData, setFormData] = useState<Partial<CreateAgenticWorkflowsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createAgenticWorkflowsRecord(formData as CreateAgenticWorkflowsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="agentic_workflows-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="agentic_workflows-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="prompt_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Prompt Id *
          </label>
          <input
            type="text"
            id="prompt_id"
            data-testid="agentic_workflows-field-prompt_id"
            value={(formData.prompt_id as string) || ''}
            onChange={(e) => handleChange('prompt_id', e.target.value)}
            aria-invalid={!!errors.prompt_id}
            aria-describedby={errors.prompt_id ? 'prompt_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.prompt_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.prompt_id && (
            <div
              id="prompt_id-error"
              role="alert"
              data-testid="agentic_workflows-field-prompt_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.prompt_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="persona_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Persona Id *
          </label>
          <input
            type="text"
            id="persona_id"
            data-testid="agentic_workflows-field-persona_id"
            value={(formData.persona_id as string) || ''}
            onChange={(e) => handleChange('persona_id', e.target.value)}
            aria-invalid={!!errors.persona_id}
            aria-describedby={errors.persona_id ? 'persona_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.persona_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.persona_id && (
            <div
              id="persona_id-error"
              role="alert"
              data-testid="agentic_workflows-field-persona_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.persona_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="workflow_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Workflow Name *
          </label>
          <input
            type="text"
            id="workflow_name"
            data-testid="agentic_workflows-field-workflow_name"
            value={(formData.workflow_name as string) || ''}
            onChange={(e) => handleChange('workflow_name', e.target.value)}
            aria-invalid={!!errors.workflow_name}
            aria-describedby={errors.workflow_name ? 'workflow_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.workflow_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.workflow_name && (
            <div
              id="workflow_name-error"
              role="alert"
              data-testid="agentic_workflows-field-workflow_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.workflow_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="workflow_description"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Workflow Description *
          </label>
          <input
            type="text"
            id="workflow_description"
            data-testid="agentic_workflows-field-workflow_description"
            value={(formData.workflow_description as string) || ''}
            onChange={(e) => handleChange('workflow_description', e.target.value)}
            aria-invalid={!!errors.workflow_description}
            aria-describedby={errors.workflow_description ? 'workflow_description-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.workflow_description ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.workflow_description && (
            <div
              id="workflow_description-error"
              role="alert"
              data-testid="agentic_workflows-field-workflow_description-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.workflow_description}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="agentic_workflows-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="agentic_workflows-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="steps"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Steps *
          </label>
          <textarea
            id="steps"
            data-testid="agentic_workflows-field-steps"
            value={(formData.steps as string) || ''}
            onChange={(e) => handleChange('steps', e.target.value)}
            aria-invalid={!!errors.steps}
            aria-describedby={errors.steps ? 'steps-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.steps ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.steps && (
            <div
              id="steps-error"
              role="alert"
              data-testid="agentic_workflows-field-steps-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.steps}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="error_handling"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Error Handling *
          </label>
          <input
            type="text"
            id="error_handling"
            data-testid="agentic_workflows-field-error_handling"
            value={(formData.error_handling as string) || ''}
            onChange={(e) => handleChange('error_handling', e.target.value)}
            aria-invalid={!!errors.error_handling}
            aria-describedby={errors.error_handling ? 'error_handling-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.error_handling ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.error_handling && (
            <div
              id="error_handling-error"
              role="alert"
              data-testid="agentic_workflows-field-error_handling-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.error_handling}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="retry_max"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Retry Max *
          </label>
          <input
            type="number"
            id="retry_max"
            data-testid="agentic_workflows-field-retry_max"
            value={(formData.retry_max as number) || 0}
            onChange={(e) => handleChange('retry_max', Number(e.target.value))}
            aria-invalid={!!errors.retry_max}
            aria-describedby={errors.retry_max ? 'retry_max-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.retry_max ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.retry_max && (
            <div
              id="retry_max-error"
              role="alert"
              data-testid="agentic_workflows-field-retry_max-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.retry_max}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="timeout_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Timeout Seconds *
          </label>
          <input
            type="number"
            id="timeout_seconds"
            data-testid="agentic_workflows-field-timeout_seconds"
            value={(formData.timeout_seconds as number) || 0}
            onChange={(e) => handleChange('timeout_seconds', Number(e.target.value))}
            aria-invalid={!!errors.timeout_seconds}
            aria-describedby={errors.timeout_seconds ? 'timeout_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.timeout_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.timeout_seconds && (
            <div
              id="timeout_seconds-error"
              role="alert"
              data-testid="agentic_workflows-field-timeout_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.timeout_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="agentic_workflows-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="agentic_workflows-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="agentic_workflows-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="agentic_workflows-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

