/**
 * ReposCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L8 (repos)
 * Generated: 2026-03-12T09:00:39Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new repos records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createReposRecord } from '@api/reposApi';
import type { ReposRecord, CreateReposRecordInput } from '@/types/repos';

interface ReposCreateFormProps {
  onSuccess: (record: ReposRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const ReposCreateForm: React.FC<ReposCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('repos.create_form');

  const [formData, setFormData] = useState<Partial<CreateReposRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createReposRecord(formData as CreateReposRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="repos-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="repos-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="repos-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="repos-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Name *
          </label>
          <input
            type="text"
            id="name"
            data-testid="repos-field-name"
            value={(formData.name as string) || ''}
            onChange={(e) => handleChange('name', e.target.value)}
            aria-invalid={!!errors.name}
            aria-describedby={errors.name ? 'name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.name && (
            <div
              id="name-error"
              role="alert"
              data-testid="repos-field-name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="owner"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Owner *
          </label>
          <input
            type="text"
            id="owner"
            data-testid="repos-field-owner"
            value={(formData.owner as string) || ''}
            onChange={(e) => handleChange('owner', e.target.value)}
            aria-invalid={!!errors.owner}
            aria-describedby={errors.owner ? 'owner-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.owner ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.owner && (
            <div
              id="owner-error"
              role="alert"
              data-testid="repos-field-owner-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.owner}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="url"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            URL *
          </label>
          <input
            type="text"
            id="url"
            data-testid="repos-field-url"
            value={(formData.url as string) || ''}
            onChange={(e) => handleChange('url', e.target.value)}
            aria-invalid={!!errors.url}
            aria-describedby={errors.url ? 'url-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.url ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.url && (
            <div
              id="url-error"
              role="alert"
              data-testid="repos-field-url-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.url}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="branch_main"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Branch Main *
          </label>
          <input
            type="text"
            id="branch_main"
            data-testid="repos-field-branch_main"
            value={(formData.branch_main as string) || ''}
            onChange={(e) => handleChange('branch_main', e.target.value)}
            aria-invalid={!!errors.branch_main}
            aria-describedby={errors.branch_main ? 'branch_main-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.branch_main ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.branch_main && (
            <div
              id="branch_main-error"
              role="alert"
              data-testid="repos-field-branch_main-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.branch_main}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="branch_staging"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Branch Staging *
          </label>
          <input
            type="text"
            id="branch_staging"
            data-testid="repos-field-branch_staging"
            value={(formData.branch_staging as string) || ''}
            onChange={(e) => handleChange('branch_staging', e.target.value)}
            aria-invalid={!!errors.branch_staging}
            aria-describedby={errors.branch_staging ? 'branch_staging-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.branch_staging ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.branch_staging && (
            <div
              id="branch_staging-error"
              role="alert"
              data-testid="repos-field-branch_staging-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.branch_staging}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="language"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Language *
          </label>
          <input
            type="text"
            id="language"
            data-testid="repos-field-language"
            value={(formData.language as string) || ''}
            onChange={(e) => handleChange('language', e.target.value)}
            aria-invalid={!!errors.language}
            aria-describedby={errors.language ? 'language-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.language ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.language && (
            <div
              id="language-error"
              role="alert"
              data-testid="repos-field-language-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.language}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="repo_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Repo Type *
          </label>
          <input
            type="text"
            id="repo_type"
            data-testid="repos-field-repo_type"
            value={(formData.repo_type as string) || ''}
            onChange={(e) => handleChange('repo_type', e.target.value)}
            aria-invalid={!!errors.repo_type}
            aria-describedby={errors.repo_type ? 'repo_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.repo_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.repo_type && (
            <div
              id="repo_type-error"
              role="alert"
              data-testid="repos-field-repo_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.repo_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="version"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Version *
          </label>
          <input
            type="text"
            id="version"
            data-testid="repos-field-version"
            value={(formData.version as string) || ''}
            onChange={(e) => handleChange('version', e.target.value)}
            aria-invalid={!!errors.version}
            aria-describedby={errors.version ? 'version-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.version ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.version && (
            <div
              id="version-error"
              role="alert"
              data-testid="repos-field-version-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.version}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_commit"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Commit *
          </label>
          <input
            type="datetime-local"
            id="last_commit"
            data-testid="repos-field-last_commit"
            value={(formData.last_commit as string) || ''}
            onChange={(e) => handleChange('last_commit', e.target.value)}
            aria-invalid={!!errors.last_commit}
            aria-describedby={errors.last_commit ? 'last_commit-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_commit ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_commit && (
            <div
              id="last_commit-error"
              role="alert"
              data-testid="repos-field-last_commit-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_commit}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_commit_hash"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Commit Hash *
          </label>
          <input
            type="text"
            id="last_commit_hash"
            data-testid="repos-field-last_commit_hash"
            value={(formData.last_commit_hash as string) || ''}
            onChange={(e) => handleChange('last_commit_hash', e.target.value)}
            aria-invalid={!!errors.last_commit_hash}
            aria-describedby={errors.last_commit_hash ? 'last_commit_hash-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_commit_hash ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_commit_hash && (
            <div
              id="last_commit_hash-error"
              role="alert"
              data-testid="repos-field-last_commit_hash-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_commit_hash}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="webhook_enabled"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Webhook Enabled *
          </label>
          <input
            type="checkbox"
            id="webhook_enabled"
            data-testid="repos-field-webhook_enabled"
            checked={(formData.webhook_enabled as boolean) || false}
            onChange={(e) => handleChange('webhook_enabled', e.target.checked)}
            aria-invalid={!!errors.webhook_enabled}
            aria-describedby={errors.webhook_enabled ? 'webhook_enabled-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.webhook_enabled && (
            <div
              id="webhook_enabled-error"
              role="alert"
              data-testid="repos-field-webhook_enabled-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.webhook_enabled}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="repos-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="repos-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="repos-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="repos-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

