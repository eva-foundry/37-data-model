/**
 * AgentExecutionHistoryCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L46 (agent_execution_history)
 * Generated: 2026-03-12T09:01:05Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new agent_execution_history records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createAgentExecutionHistoryRecord } from '@api/agent_execution_historyApi';
import type { AgentExecutionHistoryRecord, CreateAgentExecutionHistoryRecordInput } from '@/types/agent_execution_history';

interface AgentExecutionHistoryCreateFormProps {
  onSuccess: (record: AgentExecutionHistoryRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const AgentExecutionHistoryCreateForm: React.FC<AgentExecutionHistoryCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('agent_execution_history.create_form');

  const [formData, setFormData] = useState<Partial<CreateAgentExecutionHistoryRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createAgentExecutionHistoryRecord(formData as CreateAgentExecutionHistoryRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="agent_execution_history-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="agent_execution_history-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="execution_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Id *
          </label>
          <input
            type="text"
            id="execution_id"
            data-testid="agent_execution_history-field-execution_id"
            value={(formData.execution_id as string) || ''}
            onChange={(e) => handleChange('execution_id', e.target.value)}
            aria-invalid={!!errors.execution_id}
            aria-describedby={errors.execution_id ? 'execution_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_id && (
            <div
              id="execution_id-error"
              role="alert"
              data-testid="agent_execution_history-field-execution_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="execution_sequence"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Sequence *
          </label>
          <input
            type="number"
            id="execution_sequence"
            data-testid="agent_execution_history-field-execution_sequence"
            value={(formData.execution_sequence as number) || 0}
            onChange={(e) => handleChange('execution_sequence', Number(e.target.value))}
            aria-invalid={!!errors.execution_sequence}
            aria-describedby={errors.execution_sequence ? 'execution_sequence-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_sequence ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_sequence && (
            <div
              id="execution_sequence-error"
              role="alert"
              data-testid="agent_execution_history-field-execution_sequence-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_sequence}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Id *
          </label>
          <input
            type="text"
            id="agent_id"
            data-testid="agent_execution_history-field-agent_id"
            value={(formData.agent_id as string) || ''}
            onChange={(e) => handleChange('agent_id', e.target.value)}
            aria-invalid={!!errors.agent_id}
            aria-describedby={errors.agent_id ? 'agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_id && (
            <div
              id="agent_id-error"
              role="alert"
              data-testid="agent_execution_history-field-agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Name *
          </label>
          <input
            type="text"
            id="agent_name"
            data-testid="agent_execution_history-field-agent_name"
            value={(formData.agent_name as string) || ''}
            onChange={(e) => handleChange('agent_name', e.target.value)}
            aria-invalid={!!errors.agent_name}
            aria-describedby={errors.agent_name ? 'agent_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_name && (
            <div
              id="agent_name-error"
              role="alert"
              data-testid="agent_execution_history-field-agent_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="action_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Action Type *
          </label>
          <input
            type="text"
            id="action_type"
            data-testid="agent_execution_history-field-action_type"
            value={(formData.action_type as string) || ''}
            onChange={(e) => handleChange('action_type', e.target.value)}
            aria-invalid={!!errors.action_type}
            aria-describedby={errors.action_type ? 'action_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.action_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.action_type && (
            <div
              id="action_type-error"
              role="alert"
              data-testid="agent_execution_history-field-action_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.action_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="action_subtype"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Action Subtype *
          </label>
          <input
            type="text"
            id="action_subtype"
            data-testid="agent_execution_history-field-action_subtype"
            value={(formData.action_subtype as string) || ''}
            onChange={(e) => handleChange('action_subtype', e.target.value)}
            aria-invalid={!!errors.action_subtype}
            aria-describedby={errors.action_subtype ? 'action_subtype-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.action_subtype ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.action_subtype && (
            <div
              id="action_subtype-error"
              role="alert"
              data-testid="agent_execution_history-field-action_subtype-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.action_subtype}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Timestamp *
          </label>
          <input
            type="datetime-local"
            id="timestamp"
            data-testid="agent_execution_history-field-timestamp"
            value={(formData.timestamp as string) || ''}
            onChange={(e) => handleChange('timestamp', e.target.value)}
            aria-invalid={!!errors.timestamp}
            aria-describedby={errors.timestamp ? 'timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.timestamp && (
            <div
              id="timestamp-error"
              role="alert"
              data-testid="agent_execution_history-field-timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="environment"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Environment *
          </label>
          <input
            type="text"
            id="environment"
            data-testid="agent_execution_history-field-environment"
            value={(formData.environment as string) || ''}
            onChange={(e) => handleChange('environment', e.target.value)}
            aria-invalid={!!errors.environment}
            aria-describedby={errors.environment ? 'environment-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.environment ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.environment && (
            <div
              id="environment-error"
              role="alert"
              data-testid="agent_execution_history-field-environment-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.environment}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="context"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Context *
          </label>
          <textarea
            id="context"
            data-testid="agent_execution_history-field-context"
            value={(formData.context as string) || ''}
            onChange={(e) => handleChange('context', e.target.value)}
            aria-invalid={!!errors.context}
            aria-describedby={errors.context ? 'context-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.context ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.context && (
            <div
              id="context-error"
              role="alert"
              data-testid="agent_execution_history-field-context-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.context}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="phase"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Phase *
          </label>
          <input
            type="text"
            id="phase"
            data-testid="agent_execution_history-field-phase"
            value={(formData.phase as string) || ''}
            onChange={(e) => handleChange('phase', e.target.value)}
            aria-invalid={!!errors.phase}
            aria-describedby={errors.phase ? 'phase-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.phase ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.phase && (
            <div
              id="phase-error"
              role="alert"
              data-testid="agent_execution_history-field-phase-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.phase}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="outcome"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Outcome *
          </label>
          <input
            type="text"
            id="outcome"
            data-testid="agent_execution_history-field-outcome"
            value={(formData.outcome as string) || ''}
            onChange={(e) => handleChange('outcome', e.target.value)}
            aria-invalid={!!errors.outcome}
            aria-describedby={errors.outcome ? 'outcome-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.outcome ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.outcome && (
            <div
              id="outcome-error"
              role="alert"
              data-testid="agent_execution_history-field-outcome-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.outcome}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="duration_ms"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Duration Ms *
          </label>
          <input
            type="number"
            id="duration_ms"
            data-testid="agent_execution_history-field-duration_ms"
            value={(formData.duration_ms as number) || 0}
            onChange={(e) => handleChange('duration_ms', Number(e.target.value))}
            aria-invalid={!!errors.duration_ms}
            aria-describedby={errors.duration_ms ? 'duration_ms-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.duration_ms ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.duration_ms && (
            <div
              id="duration_ms-error"
              role="alert"
              data-testid="agent_execution_history-field-duration_ms-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.duration_ms}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_impact_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Impact Usd *
          </label>
          <input
            type="number"
            id="cost_impact_usd"
            data-testid="agent_execution_history-field-cost_impact_usd"
            value={(formData.cost_impact_usd as number) || 0}
            onChange={(e) => handleChange('cost_impact_usd', Number(e.target.value))}
            aria-invalid={!!errors.cost_impact_usd}
            aria-describedby={errors.cost_impact_usd ? 'cost_impact_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_impact_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cost_impact_usd && (
            <div
              id="cost_impact_usd-error"
              role="alert"
              data-testid="agent_execution_history-field-cost_impact_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_impact_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="details"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Details *
          </label>
          <textarea
            id="details"
            data-testid="agent_execution_history-field-details"
            value={(formData.details as string) || ''}
            onChange={(e) => handleChange('details', e.target.value)}
            aria-invalid={!!errors.details}
            aria-describedby={errors.details ? 'details-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.details ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.details && (
            <div
              id="details-error"
              role="alert"
              data-testid="agent_execution_history-field-details-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.details}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="policy_evaluation"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Policy Evaluation
          </label>
          <textarea
            id="policy_evaluation"
            data-testid="agent_execution_history-field-policy_evaluation"
            value={(formData.policy_evaluation as string) || ''}
            onChange={(e) => handleChange('policy_evaluation', e.target.value)}
            aria-invalid={!!errors.policy_evaluation}
            aria-describedby={errors.policy_evaluation ? 'policy_evaluation-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.policy_evaluation ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.policy_evaluation && (
            <div
              id="policy_evaluation-error"
              role="alert"
              data-testid="agent_execution_history-field-policy_evaluation-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.policy_evaluation}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="decisions_made"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Decisions Made *
          </label>
          <textarea
            id="decisions_made"
            data-testid="agent_execution_history-field-decisions_made"
            value={(formData.decisions_made as string) || ''}
            onChange={(e) => handleChange('decisions_made', e.target.value)}
            aria-invalid={!!errors.decisions_made}
            aria-describedby={errors.decisions_made ? 'decisions_made-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.decisions_made ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.decisions_made && (
            <div
              id="decisions_made-error"
              role="alert"
              data-testid="agent_execution_history-field-decisions_made-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.decisions_made}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="evidence_trail"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Trail *
          </label>
          <textarea
            id="evidence_trail"
            data-testid="agent_execution_history-field-evidence_trail"
            value={(formData.evidence_trail as string) || ''}
            onChange={(e) => handleChange('evidence_trail', e.target.value)}
            aria-invalid={!!errors.evidence_trail}
            aria-describedby={errors.evidence_trail ? 'evidence_trail-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_trail ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.evidence_trail && (
            <div
              id="evidence_trail-error"
              role="alert"
              data-testid="agent_execution_history-field-evidence_trail-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_trail}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="error_log"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Error Log *
          </label>
          <textarea
            id="error_log"
            data-testid="agent_execution_history-field-error_log"
            value={(formData.error_log as string) || ''}
            onChange={(e) => handleChange('error_log', e.target.value)}
            aria-invalid={!!errors.error_log}
            aria-describedby={errors.error_log ? 'error_log-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.error_log ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.error_log && (
            <div
              id="error_log-error"
              role="alert"
              data-testid="agent_execution_history-field-error_log-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.error_log}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="warnings"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Warnings *
          </label>
          <textarea
            id="warnings"
            data-testid="agent_execution_history-field-warnings"
            value={(formData.warnings as string) || ''}
            onChange={(e) => handleChange('warnings', e.target.value)}
            aria-invalid={!!errors.warnings}
            aria-describedby={errors.warnings ? 'warnings-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.warnings ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.warnings && (
            <div
              id="warnings-error"
              role="alert"
              data-testid="agent_execution_history-field-warnings-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.warnings}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="performance_notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Performance Notes *
          </label>
          <input
            type="text"
            id="performance_notes"
            data-testid="agent_execution_history-field-performance_notes"
            value={(formData.performance_notes as string) || ''}
            onChange={(e) => handleChange('performance_notes', e.target.value)}
            aria-invalid={!!errors.performance_notes}
            aria-describedby={errors.performance_notes ? 'performance_notes-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.performance_notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.performance_notes && (
            <div
              id="performance_notes-error"
              role="alert"
              data-testid="agent_execution_history-field-performance_notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.performance_notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="agent_execution_history-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="agent_execution_history-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="agent_execution_history-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="agent_execution_history-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

