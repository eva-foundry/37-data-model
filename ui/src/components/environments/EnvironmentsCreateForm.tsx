/**
 * EnvironmentsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L63 (environments)
 * Generated: 2026-03-12T09:01:17Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new environments records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createEnvironmentsRecord } from '@api/environmentsApi';
import type { EnvironmentsRecord, CreateEnvironmentsRecordInput } from '@/types/environments';

interface EnvironmentsCreateFormProps {
  onSuccess: (record: EnvironmentsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const EnvironmentsCreateForm: React.FC<EnvironmentsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('environments.create_form');

  const [formData, setFormData] = useState<Partial<CreateEnvironmentsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createEnvironmentsRecord(formData as CreateEnvironmentsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="environments-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="environments-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="environments-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.label ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <div
              id="label-error"
              role="alert"
              data-testid="environments-field-label-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="short_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Short Name *
          </label>
          <input
            type="text"
            id="short_name"
            data-testid="environments-field-short_name"
            value={(formData.short_name as string) || ''}
            onChange={(e) => handleChange('short_name', e.target.value)}
            aria-invalid={!!errors.short_name}
            aria-describedby={errors.short_name ? 'short_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.short_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.short_name && (
            <div
              id="short_name-error"
              role="alert"
              data-testid="environments-field-short_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.short_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="classification"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Classification *
          </label>
          <input
            type="text"
            id="classification"
            data-testid="environments-field-classification"
            value={(formData.classification as string) || ''}
            onChange={(e) => handleChange('classification', e.target.value)}
            aria-invalid={!!errors.classification}
            aria-describedby={errors.classification ? 'classification-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.classification ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.classification && (
            <div
              id="classification-error"
              role="alert"
              data-testid="environments-field-classification-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.classification}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="purpose"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Purpose *
          </label>
          <input
            type="text"
            id="purpose"
            data-testid="environments-field-purpose"
            value={(formData.purpose as string) || ''}
            onChange={(e) => handleChange('purpose', e.target.value)}
            aria-invalid={!!errors.purpose}
            aria-describedby={errors.purpose ? 'purpose-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.purpose ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.purpose && (
            <div
              id="purpose-error"
              role="alert"
              data-testid="environments-field-purpose-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.purpose}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="approval_gate_required"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Approval Gate Required *
          </label>
          <input
            type="checkbox"
            id="approval_gate_required"
            data-testid="environments-field-approval_gate_required"
            checked={(formData.approval_gate_required as boolean) || false}
            onChange={(e) => handleChange('approval_gate_required', e.target.checked)}
            aria-invalid={!!errors.approval_gate_required}
            aria-describedby={errors.approval_gate_required ? 'approval_gate_required-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.approval_gate_required && (
            <div
              id="approval_gate_required-error"
              role="alert"
              data-testid="environments-field-approval_gate_required-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.approval_gate_required}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="approval_roles"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Approval Roles
          </label>
          <textarea
            id="approval_roles"
            data-testid="environments-field-approval_roles"
            value={(formData.approval_roles as string) || ''}
            onChange={(e) => handleChange('approval_roles', e.target.value)}
            aria-invalid={!!errors.approval_roles}
            aria-describedby={errors.approval_roles ? 'approval_roles-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.approval_roles ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.approval_roles && (
            <div
              id="approval_roles-error"
              role="alert"
              data-testid="environments-field-approval_roles-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.approval_roles}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="azure_subscription_placeholder"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Azure Subscription Placeholder *
          </label>
          <input
            type="text"
            id="azure_subscription_placeholder"
            data-testid="environments-field-azure_subscription_placeholder"
            value={(formData.azure_subscription_placeholder as string) || ''}
            onChange={(e) => handleChange('azure_subscription_placeholder', e.target.value)}
            aria-invalid={!!errors.azure_subscription_placeholder}
            aria-describedby={errors.azure_subscription_placeholder ? 'azure_subscription_placeholder-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.azure_subscription_placeholder ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.azure_subscription_placeholder && (
            <div
              id="azure_subscription_placeholder-error"
              role="alert"
              data-testid="environments-field-azure_subscription_placeholder-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.azure_subscription_placeholder}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_group_placeholder"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Group Placeholder *
          </label>
          <input
            type="text"
            id="resource_group_placeholder"
            data-testid="environments-field-resource_group_placeholder"
            value={(formData.resource_group_placeholder as string) || ''}
            onChange={(e) => handleChange('resource_group_placeholder', e.target.value)}
            aria-invalid={!!errors.resource_group_placeholder}
            aria-describedby={errors.resource_group_placeholder ? 'resource_group_placeholder-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_group_placeholder ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_group_placeholder && (
            <div
              id="resource_group_placeholder-error"
              role="alert"
              data-testid="environments-field-resource_group_placeholder-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_group_placeholder}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="telemetry_workspace_placeholder"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Telemetry Workspace Placeholder *
          </label>
          <input
            type="text"
            id="telemetry_workspace_placeholder"
            data-testid="environments-field-telemetry_workspace_placeholder"
            value={(formData.telemetry_workspace_placeholder as string) || ''}
            onChange={(e) => handleChange('telemetry_workspace_placeholder', e.target.value)}
            aria-invalid={!!errors.telemetry_workspace_placeholder}
            aria-describedby={errors.telemetry_workspace_placeholder ? 'telemetry_workspace_placeholder-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.telemetry_workspace_placeholder ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.telemetry_workspace_placeholder && (
            <div
              id="telemetry_workspace_placeholder-error"
              role="alert"
              data-testid="environments-field-telemetry_workspace_placeholder-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.telemetry_workspace_placeholder}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="auto_deploy"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Auto Deploy *
          </label>
          <input
            type="checkbox"
            id="auto_deploy"
            data-testid="environments-field-auto_deploy"
            checked={(formData.auto_deploy as boolean) || false}
            onChange={(e) => handleChange('auto_deploy', e.target.checked)}
            aria-invalid={!!errors.auto_deploy}
            aria-describedby={errors.auto_deploy ? 'auto_deploy-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.auto_deploy && (
            <div
              id="auto_deploy-error"
              role="alert"
              data-testid="environments-field-auto_deploy-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.auto_deploy}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="environments-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="environments-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes *
          </label>
          <textarea
            id="notes"
            data-testid="environments-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <div
              id="notes-error"
              role="alert"
              data-testid="environments-field-notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="environments-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="environments-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="environments-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="environments-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

