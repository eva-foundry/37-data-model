/**
 * TsTypesCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L72 (ts_types)
 * Generated: 2026-03-12T09:01:23Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new ts_types records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createTsTypesRecord } from '@api/ts_typesApi';
import type { TsTypesRecord, CreateTsTypesRecordInput } from '@/types/ts_types';

interface TsTypesCreateFormProps {
  onSuccess: (record: TsTypesRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const TsTypesCreateForm: React.FC<TsTypesCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('ts_types.create_form');

  const [formData, setFormData] = useState<Partial<CreateTsTypesRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createTsTypesRecord(formData as CreateTsTypesRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="ts_types-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="ts_types-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="ts_types-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.label ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <div
              id="label-error"
              role="alert"
              data-testid="ts_types-field-label-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="app"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            App
          </label>
          <input
            type="text"
            id="app"
            data-testid="ts_types-field-app"
            value={(formData.app as string) || ''}
            onChange={(e) => handleChange('app', e.target.value)}
            aria-invalid={!!errors.app}
            aria-describedby={errors.app ? 'app-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.app ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.app && (
            <div
              id="app-error"
              role="alert"
              data-testid="ts_types-field-app-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.app}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="kind"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Kind
          </label>
          <input
            type="text"
            id="kind"
            data-testid="ts_types-field-kind"
            value={(formData.kind as string) || ''}
            onChange={(e) => handleChange('kind', e.target.value)}
            aria-invalid={!!errors.kind}
            aria-describedby={errors.kind ? 'kind-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.kind ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.kind && (
            <div
              id="kind-error"
              role="alert"
              data-testid="ts_types-field-kind-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.kind}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="defined_in_service"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Defined In Service *
          </label>
          <input
            type="text"
            id="defined_in_service"
            data-testid="ts_types-field-defined_in_service"
            value={(formData.defined_in_service as string) || ''}
            onChange={(e) => handleChange('defined_in_service', e.target.value)}
            aria-invalid={!!errors.defined_in_service}
            aria-describedby={errors.defined_in_service ? 'defined_in_service-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.defined_in_service ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.defined_in_service && (
            <div
              id="defined_in_service-error"
              role="alert"
              data-testid="ts_types-field-defined_in_service-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.defined_in_service}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="repo_path"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Repo Path *
          </label>
          <input
            type="text"
            id="repo_path"
            data-testid="ts_types-field-repo_path"
            value={(formData.repo_path as string) || ''}
            onChange={(e) => handleChange('repo_path', e.target.value)}
            aria-invalid={!!errors.repo_path}
            aria-describedby={errors.repo_path ? 'repo_path-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.repo_path ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.repo_path && (
            <div
              id="repo_path-error"
              role="alert"
              data-testid="ts_types-field-repo_path-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.repo_path}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="repo_line"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Repo Line
          </label>
          <input
            type="number"
            id="repo_line"
            data-testid="ts_types-field-repo_line"
            value={(formData.repo_line as number) || 0}
            onChange={(e) => handleChange('repo_line', Number(e.target.value))}
            aria-invalid={!!errors.repo_line}
            aria-describedby={errors.repo_line ? 'repo_line-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.repo_line ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.repo_line && (
            <div
              id="repo_line-error"
              role="alert"
              data-testid="ts_types-field-repo_line-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.repo_line}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="fields"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Fields *
          </label>
          <textarea
            id="fields"
            data-testid="ts_types-field-fields"
            value={(formData.fields as string) || ''}
            onChange={(e) => handleChange('fields', e.target.value)}
            aria-invalid={!!errors.fields}
            aria-describedby={errors.fields ? 'fields-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.fields ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.fields && (
            <div
              id="fields-error"
              role="alert"
              data-testid="ts_types-field-fields-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.fields}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="used_by_screens"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Used By Screens *
          </label>
          <textarea
            id="used_by_screens"
            data-testid="ts_types-field-used_by_screens"
            value={(formData.used_by_screens as string) || ''}
            onChange={(e) => handleChange('used_by_screens', e.target.value)}
            aria-invalid={!!errors.used_by_screens}
            aria-describedby={errors.used_by_screens ? 'used_by_screens-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.used_by_screens ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.used_by_screens && (
            <div
              id="used_by_screens-error"
              role="alert"
              data-testid="ts_types-field-used_by_screens-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.used_by_screens}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="used_by_hooks"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Used By Hooks *
          </label>
          <textarea
            id="used_by_hooks"
            data-testid="ts_types-field-used_by_hooks"
            value={(formData.used_by_hooks as string) || ''}
            onChange={(e) => handleChange('used_by_hooks', e.target.value)}
            aria-invalid={!!errors.used_by_hooks}
            aria-describedby={errors.used_by_hooks ? 'used_by_hooks-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.used_by_hooks ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.used_by_hooks && (
            <div
              id="used_by_hooks-error"
              role="alert"
              data-testid="ts_types-field-used_by_hooks-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.used_by_hooks}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="ts_types-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="ts_types-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="endpoint_response"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Endpoint Response
          </label>
          <input
            type="text"
            id="endpoint_response"
            data-testid="ts_types-field-endpoint_response"
            value={(formData.endpoint_response as string) || ''}
            onChange={(e) => handleChange('endpoint_response', e.target.value)}
            aria-invalid={!!errors.endpoint_response}
            aria-describedby={errors.endpoint_response ? 'endpoint_response-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.endpoint_response ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.endpoint_response && (
            <div
              id="endpoint_response-error"
              role="alert"
              data-testid="ts_types-field-endpoint_response-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.endpoint_response}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="ts_types-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="ts_types-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="ts_types-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="ts_types-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

