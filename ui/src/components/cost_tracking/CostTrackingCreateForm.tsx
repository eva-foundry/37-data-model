/**
 * CostTrackingCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L29 (cost_tracking)
 * Generated: 2026-03-12T09:00:53Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new cost_tracking records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createCostTrackingRecord } from '@api/cost_trackingApi';
import type { CostTrackingRecord, CreateCostTrackingRecordInput } from '@/types/cost_tracking';

interface CostTrackingCreateFormProps {
  onSuccess: (record: CostTrackingRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const CostTrackingCreateForm: React.FC<CostTrackingCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('cost_tracking.create_form');

  const [formData, setFormData] = useState<Partial<CreateCostTrackingRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createCostTrackingRecord(formData as CreateCostTrackingRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="cost_tracking-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="cost_tracking-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="persona_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Persona Id *
          </label>
          <input
            type="text"
            id="persona_id"
            data-testid="cost_tracking-field-persona_id"
            value={(formData.persona_id as string) || ''}
            onChange={(e) => handleChange('persona_id', e.target.value)}
            aria-invalid={!!errors.persona_id}
            aria-describedby={errors.persona_id ? 'persona_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.persona_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.persona_id && (
            <div
              id="persona_id-error"
              role="alert"
              data-testid="cost_tracking-field-persona_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.persona_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="workflow_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Workflow Id *
          </label>
          <input
            type="text"
            id="workflow_id"
            data-testid="cost_tracking-field-workflow_id"
            value={(formData.workflow_id as string) || ''}
            onChange={(e) => handleChange('workflow_id', e.target.value)}
            aria-invalid={!!errors.workflow_id}
            aria-describedby={errors.workflow_id ? 'workflow_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.workflow_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.workflow_id && (
            <div
              id="workflow_id-error"
              role="alert"
              data-testid="cost_tracking-field-workflow_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.workflow_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Date *
          </label>
          <input
            type="datetime-local"
            id="date"
            data-testid="cost_tracking-field-date"
            value={(formData.date as string) || ''}
            onChange={(e) => handleChange('date', e.target.value)}
            aria-invalid={!!errors.date}
            aria-describedby={errors.date ? 'date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.date ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.date && (
            <div
              id="date-error"
              role="alert"
              data-testid="cost_tracking-field-date-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.date}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="llm_api_cost_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Llm Api Cost Usd *
          </label>
          <input
            type="number"
            id="llm_api_cost_usd"
            data-testid="cost_tracking-field-llm_api_cost_usd"
            value={(formData.llm_api_cost_usd as number) || 0}
            onChange={(e) => handleChange('llm_api_cost_usd', Number(e.target.value))}
            aria-invalid={!!errors.llm_api_cost_usd}
            aria-describedby={errors.llm_api_cost_usd ? 'llm_api_cost_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.llm_api_cost_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.llm_api_cost_usd && (
            <div
              id="llm_api_cost_usd-error"
              role="alert"
              data-testid="cost_tracking-field-llm_api_cost_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.llm_api_cost_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="compute_cost_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Compute Cost Usd *
          </label>
          <input
            type="number"
            id="compute_cost_usd"
            data-testid="cost_tracking-field-compute_cost_usd"
            value={(formData.compute_cost_usd as number) || 0}
            onChange={(e) => handleChange('compute_cost_usd', Number(e.target.value))}
            aria-invalid={!!errors.compute_cost_usd}
            aria-describedby={errors.compute_cost_usd ? 'compute_cost_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.compute_cost_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.compute_cost_usd && (
            <div
              id="compute_cost_usd-error"
              role="alert"
              data-testid="cost_tracking-field-compute_cost_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.compute_cost_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="storage_cost_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Storage Cost Usd *
          </label>
          <input
            type="number"
            id="storage_cost_usd"
            data-testid="cost_tracking-field-storage_cost_usd"
            value={(formData.storage_cost_usd as number) || 0}
            onChange={(e) => handleChange('storage_cost_usd', Number(e.target.value))}
            aria-invalid={!!errors.storage_cost_usd}
            aria-describedby={errors.storage_cost_usd ? 'storage_cost_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.storage_cost_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.storage_cost_usd && (
            <div
              id="storage_cost_usd-error"
              role="alert"
              data-testid="cost_tracking-field-storage_cost_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.storage_cost_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="total_cost_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Total Cost Usd *
          </label>
          <input
            type="number"
            id="total_cost_usd"
            data-testid="cost_tracking-field-total_cost_usd"
            value={(formData.total_cost_usd as number) || 0}
            onChange={(e) => handleChange('total_cost_usd', Number(e.target.value))}
            aria-invalid={!!errors.total_cost_usd}
            aria-describedby={errors.total_cost_usd ? 'total_cost_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.total_cost_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.total_cost_usd && (
            <div
              id="total_cost_usd-error"
              role="alert"
              data-testid="cost_tracking-field-total_cost_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.total_cost_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_per_execution"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Per Execution *
          </label>
          <input
            type="number"
            id="cost_per_execution"
            data-testid="cost_tracking-field-cost_per_execution"
            value={(formData.cost_per_execution as number) || 0}
            onChange={(e) => handleChange('cost_per_execution', Number(e.target.value))}
            aria-invalid={!!errors.cost_per_execution}
            aria-describedby={errors.cost_per_execution ? 'cost_per_execution-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_per_execution ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cost_per_execution && (
            <div
              id="cost_per_execution-error"
              role="alert"
              data-testid="cost_tracking-field-cost_per_execution-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_per_execution}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="cost_tracking-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="cost_tracking-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="cost_tracking-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="cost_tracking-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

