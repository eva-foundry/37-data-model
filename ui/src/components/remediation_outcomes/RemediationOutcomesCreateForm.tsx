/**
 * RemediationOutcomesCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L50 (remediation_outcomes)
 * Generated: 2026-03-12T09:01:08Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new remediation_outcomes records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createRemediationOutcomesRecord } from '@api/remediation_outcomesApi';
import type { RemediationOutcomesRecord, CreateRemediationOutcomesRecordInput } from '@/types/remediation_outcomes';

interface RemediationOutcomesCreateFormProps {
  onSuccess: (record: RemediationOutcomesRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const RemediationOutcomesCreateForm: React.FC<RemediationOutcomesCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('remediation_outcomes.create_form');

  const [formData, setFormData] = useState<Partial<CreateRemediationOutcomesRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createRemediationOutcomesRecord(formData as CreateRemediationOutcomesRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="remediation_outcomes-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="remediation_outcomes-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="outcome_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Outcome Id *
          </label>
          <input
            type="text"
            id="outcome_id"
            data-testid="remediation_outcomes-field-outcome_id"
            value={(formData.outcome_id as string) || ''}
            onChange={(e) => handleChange('outcome_id', e.target.value)}
            aria-invalid={!!errors.outcome_id}
            aria-describedby={errors.outcome_id ? 'outcome_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.outcome_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.outcome_id && (
            <div
              id="outcome_id-error"
              role="alert"
              data-testid="remediation_outcomes-field-outcome_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.outcome_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="execution_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Id *
          </label>
          <input
            type="text"
            id="execution_id"
            data-testid="remediation_outcomes-field-execution_id"
            value={(formData.execution_id as string) || ''}
            onChange={(e) => handleChange('execution_id', e.target.value)}
            aria-invalid={!!errors.execution_id}
            aria-describedby={errors.execution_id ? 'execution_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_id && (
            <div
              id="execution_id-error"
              role="alert"
              data-testid="remediation_outcomes-field-execution_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="issue_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Issue Id *
          </label>
          <input
            type="text"
            id="issue_id"
            data-testid="remediation_outcomes-field-issue_id"
            value={(formData.issue_id as string) || ''}
            onChange={(e) => handleChange('issue_id', e.target.value)}
            aria-invalid={!!errors.issue_id}
            aria-describedby={errors.issue_id ? 'issue_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.issue_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.issue_id && (
            <div
              id="issue_id-error"
              role="alert"
              data-testid="remediation_outcomes-field-issue_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.issue_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resolution_status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resolution Status *
          </label>
          <input
            type="text"
            id="resolution_status"
            data-testid="remediation_outcomes-field-resolution_status"
            value={(formData.resolution_status as string) || ''}
            onChange={(e) => handleChange('resolution_status', e.target.value)}
            aria-invalid={!!errors.resolution_status}
            aria-describedby={errors.resolution_status ? 'resolution_status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resolution_status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resolution_status && (
            <div
              id="resolution_status-error"
              role="alert"
              data-testid="remediation_outcomes-field-resolution_status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resolution_status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="mttr_minutes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Mttr Minutes *
          </label>
          <input
            type="number"
            id="mttr_minutes"
            data-testid="remediation_outcomes-field-mttr_minutes"
            value={(formData.mttr_minutes as number) || 0}
            onChange={(e) => handleChange('mttr_minutes', Number(e.target.value))}
            aria-invalid={!!errors.mttr_minutes}
            aria-describedby={errors.mttr_minutes ? 'mttr_minutes-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.mttr_minutes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.mttr_minutes && (
            <div
              id="mttr_minutes-error"
              role="alert"
              data-testid="remediation_outcomes-field-mttr_minutes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.mttr_minutes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="root_cause_fixed"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Root Cause Fixed *
          </label>
          <input
            type="checkbox"
            id="root_cause_fixed"
            data-testid="remediation_outcomes-field-root_cause_fixed"
            checked={(formData.root_cause_fixed as boolean) || false}
            onChange={(e) => handleChange('root_cause_fixed', e.target.checked)}
            aria-invalid={!!errors.root_cause_fixed}
            aria-describedby={errors.root_cause_fixed ? 'root_cause_fixed-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.root_cause_fixed && (
            <div
              id="root_cause_fixed-error"
              role="alert"
              data-testid="remediation_outcomes-field-root_cause_fixed-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.root_cause_fixed}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="side_effects"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Side Effects *
          </label>
          <textarea
            id="side_effects"
            data-testid="remediation_outcomes-field-side_effects"
            value={(formData.side_effects as string) || ''}
            onChange={(e) => handleChange('side_effects', e.target.value)}
            aria-invalid={!!errors.side_effects}
            aria-describedby={errors.side_effects ? 'side_effects-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.side_effects ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.side_effects && (
            <div
              id="side_effects-error"
              role="alert"
              data-testid="remediation_outcomes-field-side_effects-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.side_effects}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="safety_violations"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Safety Violations *
          </label>
          <textarea
            id="safety_violations"
            data-testid="remediation_outcomes-field-safety_violations"
            value={(formData.safety_violations as string) || ''}
            onChange={(e) => handleChange('safety_violations', e.target.value)}
            aria-invalid={!!errors.safety_violations}
            aria-describedby={errors.safety_violations ? 'safety_violations-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.safety_violations ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.safety_violations && (
            <div
              id="safety_violations-error"
              role="alert"
              data-testid="remediation_outcomes-field-safety_violations-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.safety_violations}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="customer_impact"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Customer Impact *
          </label>
          <textarea
            id="customer_impact"
            data-testid="remediation_outcomes-field-customer_impact"
            value={(formData.customer_impact as string) || ''}
            onChange={(e) => handleChange('customer_impact', e.target.value)}
            aria-invalid={!!errors.customer_impact}
            aria-describedby={errors.customer_impact ? 'customer_impact-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.customer_impact ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.customer_impact && (
            <div
              id="customer_impact-error"
              role="alert"
              data-testid="remediation_outcomes-field-customer_impact-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.customer_impact}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_savings_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Savings Usd *
          </label>
          <input
            type="number"
            id="cost_savings_usd"
            data-testid="remediation_outcomes-field-cost_savings_usd"
            value={(formData.cost_savings_usd as number) || 0}
            onChange={(e) => handleChange('cost_savings_usd', Number(e.target.value))}
            aria-invalid={!!errors.cost_savings_usd}
            aria-describedby={errors.cost_savings_usd ? 'cost_savings_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_savings_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cost_savings_usd && (
            <div
              id="cost_savings_usd-error"
              role="alert"
              data-testid="remediation_outcomes-field-cost_savings_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_savings_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="performance_improvement"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Performance Improvement *
          </label>
          <textarea
            id="performance_improvement"
            data-testid="remediation_outcomes-field-performance_improvement"
            value={(formData.performance_improvement as string) || ''}
            onChange={(e) => handleChange('performance_improvement', e.target.value)}
            aria-invalid={!!errors.performance_improvement}
            aria-describedby={errors.performance_improvement ? 'performance_improvement-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.performance_improvement ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.performance_improvement && (
            <div
              id="performance_improvement-error"
              role="alert"
              data-testid="remediation_outcomes-field-performance_improvement-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.performance_improvement}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="false_positive"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            False Positive *
          </label>
          <input
            type="checkbox"
            id="false_positive"
            data-testid="remediation_outcomes-field-false_positive"
            checked={(formData.false_positive as boolean) || false}
            onChange={(e) => handleChange('false_positive', e.target.checked)}
            aria-invalid={!!errors.false_positive}
            aria-describedby={errors.false_positive ? 'false_positive-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.false_positive && (
            <div
              id="false_positive-error"
              role="alert"
              data-testid="remediation_outcomes-field-false_positive-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.false_positive}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="escalation_required"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Escalation Required *
          </label>
          <input
            type="checkbox"
            id="escalation_required"
            data-testid="remediation_outcomes-field-escalation_required"
            checked={(formData.escalation_required as boolean) || false}
            onChange={(e) => handleChange('escalation_required', e.target.checked)}
            aria-invalid={!!errors.escalation_required}
            aria-describedby={errors.escalation_required ? 'escalation_required-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.escalation_required && (
            <div
              id="escalation_required-error"
              role="alert"
              data-testid="remediation_outcomes-field-escalation_required-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.escalation_required}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="lessons_learned"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Lessons Learned *
          </label>
          <input
            type="text"
            id="lessons_learned"
            data-testid="remediation_outcomes-field-lessons_learned"
            value={(formData.lessons_learned as string) || ''}
            onChange={(e) => handleChange('lessons_learned', e.target.value)}
            aria-invalid={!!errors.lessons_learned}
            aria-describedby={errors.lessons_learned ? 'lessons_learned-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.lessons_learned ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.lessons_learned && (
            <div
              id="lessons_learned-error"
              role="alert"
              data-testid="remediation_outcomes-field-lessons_learned-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.lessons_learned}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="analyzed_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Analyzed At *
          </label>
          <input
            type="datetime-local"
            id="analyzed_at"
            data-testid="remediation_outcomes-field-analyzed_at"
            value={(formData.analyzed_at as string) || ''}
            onChange={(e) => handleChange('analyzed_at', e.target.value)}
            aria-invalid={!!errors.analyzed_at}
            aria-describedby={errors.analyzed_at ? 'analyzed_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.analyzed_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.analyzed_at && (
            <div
              id="analyzed_at-error"
              role="alert"
              data-testid="remediation_outcomes-field-analyzed_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.analyzed_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="analyst"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Analyst *
          </label>
          <input
            type="text"
            id="analyst"
            data-testid="remediation_outcomes-field-analyst"
            value={(formData.analyst as string) || ''}
            onChange={(e) => handleChange('analyst', e.target.value)}
            aria-invalid={!!errors.analyst}
            aria-describedby={errors.analyst ? 'analyst-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.analyst ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.analyst && (
            <div
              id="analyst-error"
              role="alert"
              data-testid="remediation_outcomes-field-analyst-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.analyst}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="remediation_outcomes-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="remediation_outcomes-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="remediation_outcomes-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="remediation_outcomes-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

