/**
 * AzureInfrastructureCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L81 (azure_infrastructure)
 * Generated: 2026-03-12T09:01:29Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new azure_infrastructure records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createAzureInfrastructureRecord } from '@api/azure_infrastructureApi';
import type { AzureInfrastructureRecord, CreateAzureInfrastructureRecordInput } from '@/types/azure_infrastructure';

interface AzureInfrastructureCreateFormProps {
  onSuccess: (record: AzureInfrastructureRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const AzureInfrastructureCreateForm: React.FC<AzureInfrastructureCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('azure_infrastructure.create_form');

  const [formData, setFormData] = useState<Partial<CreateAzureInfrastructureRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createAzureInfrastructureRecord(formData as CreateAzureInfrastructureRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="azure_infrastructure-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="azure_infrastructure-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="subscription_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Subscription Id *
          </label>
          <input
            type="text"
            id="subscription_id"
            data-testid="azure_infrastructure-field-subscription_id"
            value={(formData.subscription_id as string) || ''}
            onChange={(e) => handleChange('subscription_id', e.target.value)}
            aria-invalid={!!errors.subscription_id}
            aria-describedby={errors.subscription_id ? 'subscription_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.subscription_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.subscription_id && (
            <div
              id="subscription_id-error"
              role="alert"
              data-testid="azure_infrastructure-field-subscription_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.subscription_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Id *
          </label>
          <input
            type="text"
            id="resource_id"
            data-testid="azure_infrastructure-field-resource_id"
            value={(formData.resource_id as string) || ''}
            onChange={(e) => handleChange('resource_id', e.target.value)}
            aria-invalid={!!errors.resource_id}
            aria-describedby={errors.resource_id ? 'resource_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_id && (
            <div
              id="resource_id-error"
              role="alert"
              data-testid="azure_infrastructure-field-resource_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Name *
          </label>
          <input
            type="text"
            id="resource_name"
            data-testid="azure_infrastructure-field-resource_name"
            value={(formData.resource_name as string) || ''}
            onChange={(e) => handleChange('resource_name', e.target.value)}
            aria-invalid={!!errors.resource_name}
            aria-describedby={errors.resource_name ? 'resource_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_name && (
            <div
              id="resource_name-error"
              role="alert"
              data-testid="azure_infrastructure-field-resource_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Type *
          </label>
          <input
            type="text"
            id="resource_type"
            data-testid="azure_infrastructure-field-resource_type"
            value={(formData.resource_type as string) || ''}
            onChange={(e) => handleChange('resource_type', e.target.value)}
            aria-invalid={!!errors.resource_type}
            aria-describedby={errors.resource_type ? 'resource_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_type && (
            <div
              id="resource_type-error"
              role="alert"
              data-testid="azure_infrastructure-field-resource_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_group"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Group *
          </label>
          <input
            type="text"
            id="resource_group"
            data-testid="azure_infrastructure-field-resource_group"
            value={(formData.resource_group as string) || ''}
            onChange={(e) => handleChange('resource_group', e.target.value)}
            aria-invalid={!!errors.resource_group}
            aria-describedby={errors.resource_group ? 'resource_group-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_group ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_group && (
            <div
              id="resource_group-error"
              role="alert"
              data-testid="azure_infrastructure-field-resource_group-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_group}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="location"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Location *
          </label>
          <input
            type="text"
            id="location"
            data-testid="azure_infrastructure-field-location"
            value={(formData.location as string) || ''}
            onChange={(e) => handleChange('location', e.target.value)}
            aria-invalid={!!errors.location}
            aria-describedby={errors.location ? 'location-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.location ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.location && (
            <div
              id="location-error"
              role="alert"
              data-testid="azure_infrastructure-field-location-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.location}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="azure_infrastructure-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="azure_infrastructure-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="configuration"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Configuration *
          </label>
          <textarea
            id="configuration"
            data-testid="azure_infrastructure-field-configuration"
            value={(formData.configuration as string) || ''}
            onChange={(e) => handleChange('configuration', e.target.value)}
            aria-invalid={!!errors.configuration}
            aria-describedby={errors.configuration ? 'configuration-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.configuration ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.configuration && (
            <div
              id="configuration-error"
              role="alert"
              data-testid="azure_infrastructure-field-configuration-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.configuration}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="security_config"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Security Config *
          </label>
          <textarea
            id="security_config"
            data-testid="azure_infrastructure-field-security_config"
            value={(formData.security_config as string) || ''}
            onChange={(e) => handleChange('security_config', e.target.value)}
            aria-invalid={!!errors.security_config}
            aria-describedby={errors.security_config ? 'security_config-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.security_config ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.security_config && (
            <div
              id="security_config-error"
              role="alert"
              data-testid="azure_infrastructure-field-security_config-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.security_config}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_tracking"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Tracking *
          </label>
          <textarea
            id="cost_tracking"
            data-testid="azure_infrastructure-field-cost_tracking"
            value={(formData.cost_tracking as string) || ''}
            onChange={(e) => handleChange('cost_tracking', e.target.value)}
            aria-invalid={!!errors.cost_tracking}
            aria-describedby={errors.cost_tracking ? 'cost_tracking-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_tracking ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.cost_tracking && (
            <div
              id="cost_tracking-error"
              role="alert"
              data-testid="azure_infrastructure-field-cost_tracking-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_tracking}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_synced"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Synced *
          </label>
          <input
            type="datetime-local"
            id="last_synced"
            data-testid="azure_infrastructure-field-last_synced"
            value={(formData.last_synced as string) || ''}
            onChange={(e) => handleChange('last_synced', e.target.value)}
            aria-invalid={!!errors.last_synced}
            aria-describedby={errors.last_synced ? 'last_synced-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_synced ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_synced && (
            <div
              id="last_synced-error"
              role="alert"
              data-testid="azure_infrastructure-field-last_synced-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_synced}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sync_agent"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sync Agent *
          </label>
          <input
            type="text"
            id="sync_agent"
            data-testid="azure_infrastructure-field-sync_agent"
            value={(formData.sync_agent as string) || ''}
            onChange={(e) => handleChange('sync_agent', e.target.value)}
            aria-invalid={!!errors.sync_agent}
            aria-describedby={errors.sync_agent ? 'sync_agent-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sync_agent ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sync_agent && (
            <div
              id="sync_agent-error"
              role="alert"
              data-testid="azure_infrastructure-field-sync_agent-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sync_agent}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="azure_infrastructure-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="azure_infrastructure-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="azure_infrastructure-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="azure_infrastructure-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

