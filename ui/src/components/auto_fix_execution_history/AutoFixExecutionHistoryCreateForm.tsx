/**
 * AutoFixExecutionHistoryCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L49 (auto_fix_execution_history)
 * Generated: 2026-03-12T09:01:07Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new auto_fix_execution_history records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createAutoFixExecutionHistoryRecord } from '@api/auto_fix_execution_historyApi';
import type { AutoFixExecutionHistoryRecord, CreateAutoFixExecutionHistoryRecordInput } from '@/types/auto_fix_execution_history';

interface AutoFixExecutionHistoryCreateFormProps {
  onSuccess: (record: AutoFixExecutionHistoryRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const AutoFixExecutionHistoryCreateForm: React.FC<AutoFixExecutionHistoryCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('auto_fix_execution_history.create_form');

  const [formData, setFormData] = useState<Partial<CreateAutoFixExecutionHistoryRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createAutoFixExecutionHistoryRecord(formData as CreateAutoFixExecutionHistoryRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="auto_fix_execution_history-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="auto_fix_execution_history-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="execution_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Id *
          </label>
          <input
            type="text"
            id="execution_id"
            data-testid="auto_fix_execution_history-field-execution_id"
            value={(formData.execution_id as string) || ''}
            onChange={(e) => handleChange('execution_id', e.target.value)}
            aria-invalid={!!errors.execution_id}
            aria-describedby={errors.execution_id ? 'execution_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_id && (
            <div
              id="execution_id-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-execution_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="policy_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Policy Id *
          </label>
          <input
            type="text"
            id="policy_id"
            data-testid="auto_fix_execution_history-field-policy_id"
            value={(formData.policy_id as string) || ''}
            onChange={(e) => handleChange('policy_id', e.target.value)}
            aria-invalid={!!errors.policy_id}
            aria-describedby={errors.policy_id ? 'policy_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.policy_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.policy_id && (
            <div
              id="policy_id-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-policy_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.policy_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="action_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Action Id *
          </label>
          <input
            type="text"
            id="action_id"
            data-testid="auto_fix_execution_history-field-action_id"
            value={(formData.action_id as string) || ''}
            onChange={(e) => handleChange('action_id', e.target.value)}
            aria-invalid={!!errors.action_id}
            aria-describedby={errors.action_id ? 'action_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.action_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.action_id && (
            <div
              id="action_id-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-action_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.action_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="triggered_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Triggered At *
          </label>
          <input
            type="datetime-local"
            id="triggered_at"
            data-testid="auto_fix_execution_history-field-triggered_at"
            value={(formData.triggered_at as string) || ''}
            onChange={(e) => handleChange('triggered_at', e.target.value)}
            aria-invalid={!!errors.triggered_at}
            aria-describedby={errors.triggered_at ? 'triggered_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.triggered_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.triggered_at && (
            <div
              id="triggered_at-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-triggered_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.triggered_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="executed_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Executed At *
          </label>
          <input
            type="datetime-local"
            id="executed_at"
            data-testid="auto_fix_execution_history-field-executed_at"
            value={(formData.executed_at as string) || ''}
            onChange={(e) => handleChange('executed_at', e.target.value)}
            aria-invalid={!!errors.executed_at}
            aria-describedby={errors.executed_at ? 'executed_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.executed_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.executed_at && (
            <div
              id="executed_at-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-executed_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.executed_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="completed_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Completed At *
          </label>
          <input
            type="datetime-local"
            id="completed_at"
            data-testid="auto_fix_execution_history-field-completed_at"
            value={(formData.completed_at as string) || ''}
            onChange={(e) => handleChange('completed_at', e.target.value)}
            aria-invalid={!!errors.completed_at}
            aria-describedby={errors.completed_at ? 'completed_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.completed_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.completed_at && (
            <div
              id="completed_at-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-completed_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.completed_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="trigger_metric"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Trigger Metric *
          </label>
          <input
            type="text"
            id="trigger_metric"
            data-testid="auto_fix_execution_history-field-trigger_metric"
            value={(formData.trigger_metric as string) || ''}
            onChange={(e) => handleChange('trigger_metric', e.target.value)}
            aria-invalid={!!errors.trigger_metric}
            aria-describedby={errors.trigger_metric ? 'trigger_metric-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.trigger_metric ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.trigger_metric && (
            <div
              id="trigger_metric-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-trigger_metric-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.trigger_metric}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="trigger_value"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Trigger Value *
          </label>
          <input
            type="number"
            id="trigger_value"
            data-testid="auto_fix_execution_history-field-trigger_value"
            value={(formData.trigger_value as number) || 0}
            onChange={(e) => handleChange('trigger_value', Number(e.target.value))}
            aria-invalid={!!errors.trigger_value}
            aria-describedby={errors.trigger_value ? 'trigger_value-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.trigger_value ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.trigger_value && (
            <div
              id="trigger_value-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-trigger_value-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.trigger_value}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="trigger_threshold"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Trigger Threshold *
          </label>
          <input
            type="number"
            id="trigger_threshold"
            data-testid="auto_fix_execution_history-field-trigger_threshold"
            value={(formData.trigger_threshold as number) || 0}
            onChange={(e) => handleChange('trigger_threshold', Number(e.target.value))}
            aria-invalid={!!errors.trigger_threshold}
            aria-describedby={errors.trigger_threshold ? 'trigger_threshold-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.trigger_threshold ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.trigger_threshold && (
            <div
              id="trigger_threshold-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-trigger_threshold-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.trigger_threshold}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="executed"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Executed *
          </label>
          <input
            type="checkbox"
            id="executed"
            data-testid="auto_fix_execution_history-field-executed"
            checked={(formData.executed as boolean) || false}
            onChange={(e) => handleChange('executed', e.target.checked)}
            aria-invalid={!!errors.executed}
            aria-describedby={errors.executed ? 'executed-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.executed && (
            <div
              id="executed-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-executed-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.executed}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="executor"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Executor *
          </label>
          <input
            type="text"
            id="executor"
            data-testid="auto_fix_execution_history-field-executor"
            value={(formData.executor as string) || ''}
            onChange={(e) => handleChange('executor', e.target.value)}
            aria-invalid={!!errors.executor}
            aria-describedby={errors.executor ? 'executor-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.executor ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.executor && (
            <div
              id="executor-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-executor-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.executor}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="executor_agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Executor Agent Id *
          </label>
          <input
            type="text"
            id="executor_agent_id"
            data-testid="auto_fix_execution_history-field-executor_agent_id"
            value={(formData.executor_agent_id as string) || ''}
            onChange={(e) => handleChange('executor_agent_id', e.target.value)}
            aria-invalid={!!errors.executor_agent_id}
            aria-describedby={errors.executor_agent_id ? 'executor_agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.executor_agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.executor_agent_id && (
            <div
              id="executor_agent_id-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-executor_agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.executor_agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="outcome"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Outcome *
          </label>
          <input
            type="text"
            id="outcome"
            data-testid="auto_fix_execution_history-field-outcome"
            value={(formData.outcome as string) || ''}
            onChange={(e) => handleChange('outcome', e.target.value)}
            aria-invalid={!!errors.outcome}
            aria-describedby={errors.outcome ? 'outcome-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.outcome ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.outcome && (
            <div
              id="outcome-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-outcome-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.outcome}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="duration_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Duration Seconds *
          </label>
          <input
            type="number"
            id="duration_seconds"
            data-testid="auto_fix_execution_history-field-duration_seconds"
            value={(formData.duration_seconds as number) || 0}
            onChange={(e) => handleChange('duration_seconds', Number(e.target.value))}
            aria-invalid={!!errors.duration_seconds}
            aria-describedby={errors.duration_seconds ? 'duration_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.duration_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.duration_seconds && (
            <div
              id="duration_seconds-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-duration_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.duration_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_usd"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Usd *
          </label>
          <input
            type="number"
            id="cost_usd"
            data-testid="auto_fix_execution_history-field-cost_usd"
            value={(formData.cost_usd as number) || 0}
            onChange={(e) => handleChange('cost_usd', Number(e.target.value))}
            aria-invalid={!!errors.cost_usd}
            aria-describedby={errors.cost_usd ? 'cost_usd-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_usd ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cost_usd && (
            <div
              id="cost_usd-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-cost_usd-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_usd}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="safety_violations"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Safety Violations *
          </label>
          <textarea
            id="safety_violations"
            data-testid="auto_fix_execution_history-field-safety_violations"
            value={(formData.safety_violations as string) || ''}
            onChange={(e) => handleChange('safety_violations', e.target.value)}
            aria-invalid={!!errors.safety_violations}
            aria-describedby={errors.safety_violations ? 'safety_violations-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.safety_violations ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.safety_violations && (
            <div
              id="safety_violations-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-safety_violations-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.safety_violations}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="evidence_correlation_ids"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Correlation Ids *
          </label>
          <textarea
            id="evidence_correlation_ids"
            data-testid="auto_fix_execution_history-field-evidence_correlation_ids"
            value={(formData.evidence_correlation_ids as string) || ''}
            onChange={(e) => handleChange('evidence_correlation_ids', e.target.value)}
            aria-invalid={!!errors.evidence_correlation_ids}
            aria-describedby={errors.evidence_correlation_ids ? 'evidence_correlation_ids-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_correlation_ids ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.evidence_correlation_ids && (
            <div
              id="evidence_correlation_ids-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-evidence_correlation_ids-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_correlation_ids}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="execution_history_refs"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution History Refs *
          </label>
          <textarea
            id="execution_history_refs"
            data-testid="auto_fix_execution_history-field-execution_history_refs"
            value={(formData.execution_history_refs as string) || ''}
            onChange={(e) => handleChange('execution_history_refs', e.target.value)}
            aria-invalid={!!errors.execution_history_refs}
            aria-describedby={errors.execution_history_refs ? 'execution_history_refs-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_history_refs ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.execution_history_refs && (
            <div
              id="execution_history_refs-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-execution_history_refs-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_history_refs}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_attempted"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Attempted *
          </label>
          <input
            type="checkbox"
            id="rollback_attempted"
            data-testid="auto_fix_execution_history-field-rollback_attempted"
            checked={(formData.rollback_attempted as boolean) || false}
            onChange={(e) => handleChange('rollback_attempted', e.target.checked)}
            aria-invalid={!!errors.rollback_attempted}
            aria-describedby={errors.rollback_attempted ? 'rollback_attempted-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.rollback_attempted && (
            <div
              id="rollback_attempted-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-rollback_attempted-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_attempted}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_successful"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Successful *
          </label>
          <input
            type="text"
            id="rollback_successful"
            data-testid="auto_fix_execution_history-field-rollback_successful"
            value={(formData.rollback_successful as string) || ''}
            onChange={(e) => handleChange('rollback_successful', e.target.value)}
            aria-invalid={!!errors.rollback_successful}
            aria-describedby={errors.rollback_successful ? 'rollback_successful-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rollback_successful ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rollback_successful && (
            <div
              id="rollback_successful-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-rollback_successful-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_successful}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rollback_reason"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rollback Reason *
          </label>
          <input
            type="text"
            id="rollback_reason"
            data-testid="auto_fix_execution_history-field-rollback_reason"
            value={(formData.rollback_reason as string) || ''}
            onChange={(e) => handleChange('rollback_reason', e.target.value)}
            aria-invalid={!!errors.rollback_reason}
            aria-describedby={errors.rollback_reason ? 'rollback_reason-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rollback_reason ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rollback_reason && (
            <div
              id="rollback_reason-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-rollback_reason-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rollback_reason}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="target"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Target *
          </label>
          <textarea
            id="target"
            data-testid="auto_fix_execution_history-field-target"
            value={(formData.target as string) || ''}
            onChange={(e) => handleChange('target', e.target.value)}
            aria-invalid={!!errors.target}
            aria-describedby={errors.target ? 'target-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.target ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.target && (
            <div
              id="target-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-target-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.target}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="before_state"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Before State *
          </label>
          <textarea
            id="before_state"
            data-testid="auto_fix_execution_history-field-before_state"
            value={(formData.before_state as string) || ''}
            onChange={(e) => handleChange('before_state', e.target.value)}
            aria-invalid={!!errors.before_state}
            aria-describedby={errors.before_state ? 'before_state-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.before_state ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.before_state && (
            <div
              id="before_state-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-before_state-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.before_state}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="after_state"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            After State *
          </label>
          <textarea
            id="after_state"
            data-testid="auto_fix_execution_history-field-after_state"
            value={(formData.after_state as string) || ''}
            onChange={(e) => handleChange('after_state', e.target.value)}
            aria-invalid={!!errors.after_state}
            aria-describedby={errors.after_state ? 'after_state-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.after_state ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.after_state && (
            <div
              id="after_state-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-after_state-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.after_state}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="auto_fix_execution_history-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="auto_fix_execution_history-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="auto_fix_execution_history-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="auto_fix_execution_history-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

