/**
 * WbsItemEditForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L26 (wbs)
 * Generated: 2026-03-11T15:12:21Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for editing existing wbs records with validation and error handling.
 * 
 * Features:
 * - Pre-populated with existing values
 * - Client-side validation
 * - Field-level error messages
 * - Update/Cancel actions
 * - Accessible form markup
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState, useEffect } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { updateWbsItemRecord } from '@api/wbsApi';
import type { WbsItemRecord, UpdateWbsItemRecordInput } from '@/types/wbs';

interface WbsItemEditFormProps {
  record: WbsItemRecord;
  onSuccess: (record: WbsItemRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const WbsItemEditForm: React.FC<WbsItemEditFormProps> = ({ record, onSuccess, onCancel }) => {
  const t = useLiterals('wbs.edit_form');

  const [formData, setFormData] = useState<Partial<UpdateWbsItemRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  // Initialize form data with existing record values
  useEffect(() => {
    const { layer, partition_key, _attachments, _etag, _rid, _self, _ts, ...editable } = record as any;
    setFormData(editable);
  }, [record]);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
        if (!formData.baseline_start) {
      newErrors.baseline_start = t('errors.required');
    }
    if (!formData.done_criteria) {
      newErrors.done_criteria = t('errors.required');
    }
    if (!formData.baseline_end) {
      newErrors.baseline_end = t('errors.required');
    }
    if (!formData.stories_done) {
      newErrors.stories_done = t('errors.required');
    }
    if (!formData.points_total) {
      newErrors.points_total = t('errors.required');
    }
    if (!formData.estimate_at_completion) {
      newErrors.estimate_at_completion = t('errors.required');
    }
    if (!formData.planned_end) {
      newErrors.planned_end = t('errors.required');
    }
    if (!formData.team) {
      newErrors.team = t('errors.required');
    }
    if (!formData.deliverable) {
      newErrors.deliverable = t('errors.required');
    }
    if (!formData.earned_value) {
      newErrors.earned_value = t('errors.required');
    }
    if (!formData.actual_start) {
      newErrors.actual_start = t('errors.required');
    }
    if (!formData.ado_epic_id) {
      newErrors.ado_epic_id = t('errors.required');
    }
    if (!formData.variance_at_completion) {
      newErrors.variance_at_completion = t('errors.required');
    }
    if (!formData.sprints_planned) {
      newErrors.sprints_planned = t('errors.required');
    }
    if (!formData.depends_on_infra) {
      newErrors.depends_on_infra = t('errors.required');
    }
    if (!formData.sprint_count) {
      newErrors.sprint_count = t('errors.required');
    }
    if (!formData.notes) {
      newErrors.notes = t('errors.required');
    }
    if (!formData.ado_feature_id) {
      newErrors.ado_feature_id = t('errors.required');
    }
    if (!formData.status) {
      newErrors.status = t('errors.required');
    }
    if (!formData.points_done) {
      newErrors.points_done = t('errors.required');
    }
    if (!formData.planned_value) {
      newErrors.planned_value = t('errors.required');
    }
    if (!formData.risk_notes) {
      newErrors.risk_notes = t('errors.required');
    }
    if (!formData.sprint) {
      newErrors.sprint = t('errors.required');
    }
    if (!formData.owner) {
      newErrors.owner = t('errors.required');
    }
    if (!formData.label_fr) {
      newErrors.label_fr = t('errors.required');
    }
    if (!formData.project_id) {
      newErrors.project_id = t('errors.required');
    }
    if (!formData.level) {
      newErrors.level = t('errors.required');
    }
    if (!formData.stream) {
      newErrors.stream = t('errors.required');
    }
    if (!formData.milestone) {
      newErrors.milestone = t('errors.required');
    }
    if (!formData.work_items) {
      newErrors.work_items = t('errors.required');
    }
    if (!formData.is_active) {
      newErrors.is_active = t('errors.required');
    }
    if (!formData.planned_start) {
      newErrors.planned_start = t('errors.required');
    }
    if (!formData.spi) {
      newErrors.spi = t('errors.required');
    }
    if (!formData.label) {
      newErrors.label = t('errors.required');
    }
    if (!formData.defect_rate) {
      newErrors.defect_rate = t('errors.required');
    }
    if (!formData.cycle_time_days) {
      newErrors.cycle_time_days = t('errors.required');
    }
    if (!formData.sprints_done) {
      newErrors.sprints_done = t('errors.required');
    }
    if (!formData.sprint_start) {
      newErrors.sprint_start = t('errors.required');
    }
    if (!formData.actual_end) {
      newErrors.actual_end = t('errors.required');
    }
    if (!formData.stories_total) {
      newErrors.stories_total = t('errors.required');
    }
    if (!formData.depends_on_wbs) {
      newErrors.depends_on_wbs = t('errors.required');
    }
    if (!formData.actual_cost) {
      newErrors.actual_cost = t('errors.required');
    }
    if (!formData.percent_complete) {
      newErrors.percent_complete = t('errors.required');
    }
    if (!formData.cpi) {
      newErrors.cpi = t('errors.required');
    }
    if (!formData.methodology) {
      newErrors.methodology = t('errors.required');
    }
    if (!formData.parent_wbs_id) {
      newErrors.parent_wbs_id = t('errors.required');
    }
    if (!formData.sprint_end) {
      newErrors.sprint_end = t('errors.required');
    }
    if (!formData._cached) {
      newErrors._cached = t('errors.required');
    }
    if (!formData.budget_at_completion) {
      newErrors.budget_at_completion = t('errors.required');
    }
    if (!formData.risk_level) {
      newErrors.risk_level = t('errors.required');
    }
    if (!formData.velocity) {
      newErrors.velocity = t('errors.required');
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const hasChanges = (): boolean => {
    const { layer, partition_key, _attachments, _etag, _rid, _self, _ts, ...original } = record as any;
    return JSON.stringify(formData) !== JSON.stringify(original);
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!hasChanges()) {
      setSubmitError(t.noChanges);
      return;
    }

    if (!validate()) return;

    setSubmitting(true);
    try {
      const updated = await updateWbsItemRecord(
        record.id,
        formData as UpdateWbsItemRecordInput
      );
      onSuccess(updated);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="wbs-edit-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <div style={{ marginBottom: 24 }}>
        <h2 style={{ margin: '0 0 4px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
          {t.title}
        </h2>
        <p style={{ margin: 0, fontSize: '0.75rem', color: GC_MUTED }}>
          L26: {record.id}
        </p>
      </div>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="wbs-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t.error}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="evidence_id_pattern"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Id Pattern
          </label>
          <input
            type="text"
            id="evidence_id_pattern"
            data-testid="wbs-field-evidence_id_pattern"
            value={(formData.evidence_id_pattern as string) || ''}
            onChange={(e) => handleChange('evidence_id_pattern', e.target.value)}
            aria-invalid={!!errors.evidence_id_pattern}
            aria-describedby={errors.evidence_id_pattern ? 'evidence_id_pattern-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_id_pattern ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.evidence_id_pattern && (
            <div
              id="evidence_id_pattern-error"
              role="alert"
              data-testid="wbs-field-evidence_id_pattern-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_id_pattern}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="baseline_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Baseline Start *
          </label>
          <input
            type="text"
            id="baseline_start"
            data-testid="wbs-field-baseline_start"
            value={(formData.baseline_start as string) || ''}
            onChange={(e) => handleChange('baseline_start', e.target.value)}
            aria-invalid={!!errors.baseline_start}
            aria-describedby={errors.baseline_start ? 'baseline_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.baseline_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.baseline_start && (
            <div
              id="baseline_start-error"
              role="alert"
              data-testid="wbs-field-baseline_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.baseline_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="done_criteria"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Done Criteria *
          </label>
          <input
            type="text"
            id="done_criteria"
            data-testid="wbs-field-done_criteria"
            value={(formData.done_criteria as string) || ''}
            onChange={(e) => handleChange('done_criteria', e.target.value)}
            aria-invalid={!!errors.done_criteria}
            aria-describedby={errors.done_criteria ? 'done_criteria-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.done_criteria ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.done_criteria && (
            <div
              id="done_criteria-error"
              role="alert"
              data-testid="wbs-field-done_criteria-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.done_criteria}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="baseline_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Baseline End *
          </label>
          <input
            type="text"
            id="baseline_end"
            data-testid="wbs-field-baseline_end"
            value={(formData.baseline_end as string) || ''}
            onChange={(e) => handleChange('baseline_end', e.target.value)}
            aria-invalid={!!errors.baseline_end}
            aria-describedby={errors.baseline_end ? 'baseline_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.baseline_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.baseline_end && (
            <div
              id="baseline_end-error"
              role="alert"
              data-testid="wbs-field-baseline_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.baseline_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="stories_done"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Stories Done *
          </label>
          <input
            type="number"
            id="stories_done"
            data-testid="wbs-field-stories_done"
            value={(formData.stories_done as number) || 0}
            onChange={(e) => handleChange('stories_done', Number(e.target.value))}
            aria-invalid={!!errors.stories_done}
            aria-describedby={errors.stories_done ? 'stories_done-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.stories_done ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.stories_done && (
            <div
              id="stories_done-error"
              role="alert"
              data-testid="wbs-field-stories_done-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.stories_done}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="points_total"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Points Total *
          </label>
          <input
            type="text"
            id="points_total"
            data-testid="wbs-field-points_total"
            value={(formData.points_total as string) || ''}
            onChange={(e) => handleChange('points_total', e.target.value)}
            aria-invalid={!!errors.points_total}
            aria-describedby={errors.points_total ? 'points_total-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.points_total ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.points_total && (
            <div
              id="points_total-error"
              role="alert"
              data-testid="wbs-field-points_total-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.points_total}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="estimate_at_completion"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Estimate At Completion *
          </label>
          <input
            type="text"
            id="estimate_at_completion"
            data-testid="wbs-field-estimate_at_completion"
            value={(formData.estimate_at_completion as string) || ''}
            onChange={(e) => handleChange('estimate_at_completion', e.target.value)}
            aria-invalid={!!errors.estimate_at_completion}
            aria-describedby={errors.estimate_at_completion ? 'estimate_at_completion-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.estimate_at_completion ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.estimate_at_completion && (
            <div
              id="estimate_at_completion-error"
              role="alert"
              data-testid="wbs-field-estimate_at_completion-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.estimate_at_completion}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="planned_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Planned End *
          </label>
          <input
            type="text"
            id="planned_end"
            data-testid="wbs-field-planned_end"
            value={(formData.planned_end as string) || ''}
            onChange={(e) => handleChange('planned_end', e.target.value)}
            aria-invalid={!!errors.planned_end}
            aria-describedby={errors.planned_end ? 'planned_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.planned_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.planned_end && (
            <div
              id="planned_end-error"
              role="alert"
              data-testid="wbs-field-planned_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.planned_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="team"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Team *
          </label>
          <input
            type="text"
            id="team"
            data-testid="wbs-field-team"
            value={(formData.team as string) || ''}
            onChange={(e) => handleChange('team', e.target.value)}
            aria-invalid={!!errors.team}
            aria-describedby={errors.team ? 'team-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.team ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.team && (
            <div
              id="team-error"
              role="alert"
              data-testid="wbs-field-team-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.team}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deliverable"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deliverable *
          </label>
          <input
            type="text"
            id="deliverable"
            data-testid="wbs-field-deliverable"
            value={(formData.deliverable as string) || ''}
            onChange={(e) => handleChange('deliverable', e.target.value)}
            aria-invalid={!!errors.deliverable}
            aria-describedby={errors.deliverable ? 'deliverable-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deliverable ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deliverable && (
            <div
              id="deliverable-error"
              role="alert"
              data-testid="wbs-field-deliverable-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deliverable}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="earned_value"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Earned Value *
          </label>
          <input
            type="text"
            id="earned_value"
            data-testid="wbs-field-earned_value"
            value={(formData.earned_value as string) || ''}
            onChange={(e) => handleChange('earned_value', e.target.value)}
            aria-invalid={!!errors.earned_value}
            aria-describedby={errors.earned_value ? 'earned_value-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.earned_value ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.earned_value && (
            <div
              id="earned_value-error"
              role="alert"
              data-testid="wbs-field-earned_value-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.earned_value}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="actual_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Actual Start *
          </label>
          <input
            type="text"
            id="actual_start"
            data-testid="wbs-field-actual_start"
            value={(formData.actual_start as string) || ''}
            onChange={(e) => handleChange('actual_start', e.target.value)}
            aria-invalid={!!errors.actual_start}
            aria-describedby={errors.actual_start ? 'actual_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.actual_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.actual_start && (
            <div
              id="actual_start-error"
              role="alert"
              data-testid="wbs-field-actual_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.actual_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="ado_epic_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            ADO Epic ID *
          </label>
          <input
            type="text"
            id="ado_epic_id"
            data-testid="wbs-field-ado_epic_id"
            value={(formData.ado_epic_id as string) || ''}
            onChange={(e) => handleChange('ado_epic_id', e.target.value)}
            aria-invalid={!!errors.ado_epic_id}
            aria-describedby={errors.ado_epic_id ? 'ado_epic_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.ado_epic_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ado_epic_id && (
            <div
              id="ado_epic_id-error"
              role="alert"
              data-testid="wbs-field-ado_epic_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ado_epic_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="variance_at_completion"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Variance At Completion *
          </label>
          <input
            type="text"
            id="variance_at_completion"
            data-testid="wbs-field-variance_at_completion"
            value={(formData.variance_at_completion as string) || ''}
            onChange={(e) => handleChange('variance_at_completion', e.target.value)}
            aria-invalid={!!errors.variance_at_completion}
            aria-describedby={errors.variance_at_completion ? 'variance_at_completion-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.variance_at_completion ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.variance_at_completion && (
            <div
              id="variance_at_completion-error"
              role="alert"
              data-testid="wbs-field-variance_at_completion-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.variance_at_completion}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprints_planned"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprints Planned *
          </label>
          <input
            type="text"
            id="sprints_planned"
            data-testid="wbs-field-sprints_planned"
            value={(formData.sprints_planned as string) || ''}
            onChange={(e) => handleChange('sprints_planned', e.target.value)}
            aria-invalid={!!errors.sprints_planned}
            aria-describedby={errors.sprints_planned ? 'sprints_planned-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprints_planned ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprints_planned && (
            <div
              id="sprints_planned-error"
              role="alert"
              data-testid="wbs-field-sprints_planned-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprints_planned}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="depends_on_infra"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Depends On Infra *
          </label>
          <textarea
            id="depends_on_infra"
            data-testid="wbs-field-depends_on_infra"
            value={(formData.depends_on_infra as string) || ''}
            onChange={(e) => handleChange('depends_on_infra', e.target.value)}
            aria-invalid={!!errors.depends_on_infra}
            aria-describedby={errors.depends_on_infra ? 'depends_on_infra-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.depends_on_infra ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.depends_on_infra && (
            <div
              id="depends_on_infra-error"
              role="alert"
              data-testid="wbs-field-depends_on_infra-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.depends_on_infra}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprint_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint Count *
          </label>
          <input
            type="number"
            id="sprint_count"
            data-testid="wbs-field-sprint_count"
            value={(formData.sprint_count as number) || 0}
            onChange={(e) => handleChange('sprint_count', Number(e.target.value))}
            aria-invalid={!!errors.sprint_count}
            aria-describedby={errors.sprint_count ? 'sprint_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprint_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint_count && (
            <div
              id="sprint_count-error"
              role="alert"
              data-testid="wbs-field-sprint_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes *
          </label>
          <textarea
            id="notes"
            data-testid="wbs-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <div
              id="notes-error"
              role="alert"
              data-testid="wbs-field-notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="ado_feature_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Ado Feature Id *
          </label>
          <input
            type="text"
            id="ado_feature_id"
            data-testid="wbs-field-ado_feature_id"
            value={(formData.ado_feature_id as string) || ''}
            onChange={(e) => handleChange('ado_feature_id', e.target.value)}
            aria-invalid={!!errors.ado_feature_id}
            aria-describedby={errors.ado_feature_id ? 'ado_feature_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.ado_feature_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ado_feature_id && (
            <div
              id="ado_feature_id-error"
              role="alert"
              data-testid="wbs-field-ado_feature_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ado_feature_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="ci_runbook"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Ci Runbook
          </label>
          <input
            type="text"
            id="ci_runbook"
            data-testid="wbs-field-ci_runbook"
            value={(formData.ci_runbook as string) || ''}
            onChange={(e) => handleChange('ci_runbook', e.target.value)}
            aria-invalid={!!errors.ci_runbook}
            aria-describedby={errors.ci_runbook ? 'ci_runbook-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.ci_runbook ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ci_runbook && (
            <div
              id="ci_runbook-error"
              role="alert"
              data-testid="wbs-field-ci_runbook-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ci_runbook}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="wbs-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="wbs-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="points_done"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Points Done *
          </label>
          <input
            type="number"
            id="points_done"
            data-testid="wbs-field-points_done"
            value={(formData.points_done as number) || 0}
            onChange={(e) => handleChange('points_done', Number(e.target.value))}
            aria-invalid={!!errors.points_done}
            aria-describedby={errors.points_done ? 'points_done-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.points_done ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.points_done && (
            <div
              id="points_done-error"
              role="alert"
              data-testid="wbs-field-points_done-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.points_done}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="planned_value"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Planned Value *
          </label>
          <input
            type="text"
            id="planned_value"
            data-testid="wbs-field-planned_value"
            value={(formData.planned_value as string) || ''}
            onChange={(e) => handleChange('planned_value', e.target.value)}
            aria-invalid={!!errors.planned_value}
            aria-describedby={errors.planned_value ? 'planned_value-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.planned_value ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.planned_value && (
            <div
              id="planned_value-error"
              role="alert"
              data-testid="wbs-field-planned_value-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.planned_value}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="risk_notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Risk Notes *
          </label>
          <input
            type="text"
            id="risk_notes"
            data-testid="wbs-field-risk_notes"
            value={(formData.risk_notes as string) || ''}
            onChange={(e) => handleChange('risk_notes', e.target.value)}
            aria-invalid={!!errors.risk_notes}
            aria-describedby={errors.risk_notes ? 'risk_notes-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.risk_notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.risk_notes && (
            <div
              id="risk_notes-error"
              role="alert"
              data-testid="wbs-field-risk_notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.risk_notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprint"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint *
          </label>
          <input
            type="text"
            id="sprint"
            data-testid="wbs-field-sprint"
            value={(formData.sprint as string) || ''}
            onChange={(e) => handleChange('sprint', e.target.value)}
            aria-invalid={!!errors.sprint}
            aria-describedby={errors.sprint ? 'sprint-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprint ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint && (
            <div
              id="sprint-error"
              role="alert"
              data-testid="wbs-field-sprint-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="owner"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Owner *
          </label>
          <input
            type="text"
            id="owner"
            data-testid="wbs-field-owner"
            value={(formData.owner as string) || ''}
            onChange={(e) => handleChange('owner', e.target.value)}
            aria-invalid={!!errors.owner}
            aria-describedby={errors.owner ? 'owner-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.owner ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.owner && (
            <div
              id="owner-error"
              role="alert"
              data-testid="wbs-field-owner-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.owner}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="label_fr"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label Fr *
          </label>
          <input
            type="text"
            id="label_fr"
            data-testid="wbs-field-label_fr"
            value={(formData.label_fr as string) || ''}
            onChange={(e) => handleChange('label_fr', e.target.value)}
            aria-invalid={!!errors.label_fr}
            aria-describedby={errors.label_fr ? 'label_fr-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.label_fr ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label_fr && (
            <div
              id="label_fr-error"
              role="alert"
              data-testid="wbs-field-label_fr-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label_fr}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="project_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Id *
          </label>
          <input
            type="text"
            id="project_id"
            data-testid="wbs-field-project_id"
            value={(formData.project_id as string) || ''}
            onChange={(e) => handleChange('project_id', e.target.value)}
            aria-invalid={!!errors.project_id}
            aria-describedby={errors.project_id ? 'project_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.project_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_id && (
            <div
              id="project_id-error"
              role="alert"
              data-testid="wbs-field-project_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="level"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Level *
          </label>
          <input
            type="text"
            id="level"
            data-testid="wbs-field-level"
            value={(formData.level as string) || ''}
            onChange={(e) => handleChange('level', e.target.value)}
            aria-invalid={!!errors.level}
            aria-describedby={errors.level ? 'level-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.level ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.level && (
            <div
              id="level-error"
              role="alert"
              data-testid="wbs-field-level-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.level}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="stream"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Stream *
          </label>
          <input
            type="text"
            id="stream"
            data-testid="wbs-field-stream"
            value={(formData.stream as string) || ''}
            onChange={(e) => handleChange('stream', e.target.value)}
            aria-invalid={!!errors.stream}
            aria-describedby={errors.stream ? 'stream-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.stream ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.stream && (
            <div
              id="stream-error"
              role="alert"
              data-testid="wbs-field-stream-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.stream}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="milestone"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Milestone *
          </label>
          <input
            type="checkbox"
            id="milestone"
            data-testid="wbs-field-milestone"
            checked={(formData.milestone as boolean) || false}
            onChange={(e) => handleChange('milestone', e.target.checked)}
            aria-invalid={!!errors.milestone}
            aria-describedby={errors.milestone ? 'milestone-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.milestone && (
            <div
              id="milestone-error"
              role="alert"
              data-testid="wbs-field-milestone-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.milestone}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="work_items"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Work Items *
          </label>
          <textarea
            id="work_items"
            data-testid="wbs-field-work_items"
            value={(formData.work_items as string) || ''}
            onChange={(e) => handleChange('work_items', e.target.value)}
            aria-invalid={!!errors.work_items}
            aria-describedby={errors.work_items ? 'work_items-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.work_items ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.work_items && (
            <div
              id="work_items-error"
              role="alert"
              data-testid="wbs-field-work_items-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.work_items}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="wbs-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="wbs-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="planned_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Planned Start *
          </label>
          <input
            type="text"
            id="planned_start"
            data-testid="wbs-field-planned_start"
            value={(formData.planned_start as string) || ''}
            onChange={(e) => handleChange('planned_start', e.target.value)}
            aria-invalid={!!errors.planned_start}
            aria-describedby={errors.planned_start ? 'planned_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.planned_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.planned_start && (
            <div
              id="planned_start-error"
              role="alert"
              data-testid="wbs-field-planned_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.planned_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="spi"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Spi *
          </label>
          <input
            type="text"
            id="spi"
            data-testid="wbs-field-spi"
            value={(formData.spi as string) || ''}
            onChange={(e) => handleChange('spi', e.target.value)}
            aria-invalid={!!errors.spi}
            aria-describedby={errors.spi ? 'spi-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.spi ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.spi && (
            <div
              id="spi-error"
              role="alert"
              data-testid="wbs-field-spi-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.spi}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="wbs-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.label ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <div
              id="label-error"
              role="alert"
              data-testid="wbs-field-label-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="defect_rate"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Defect Rate *
          </label>
          <input
            type="text"
            id="defect_rate"
            data-testid="wbs-field-defect_rate"
            value={(formData.defect_rate as string) || ''}
            onChange={(e) => handleChange('defect_rate', e.target.value)}
            aria-invalid={!!errors.defect_rate}
            aria-describedby={errors.defect_rate ? 'defect_rate-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.defect_rate ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.defect_rate && (
            <div
              id="defect_rate-error"
              role="alert"
              data-testid="wbs-field-defect_rate-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.defect_rate}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cycle_time_days"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cycle Time Days *
          </label>
          <input
            type="text"
            id="cycle_time_days"
            data-testid="wbs-field-cycle_time_days"
            value={(formData.cycle_time_days as string) || ''}
            onChange={(e) => handleChange('cycle_time_days', e.target.value)}
            aria-invalid={!!errors.cycle_time_days}
            aria-describedby={errors.cycle_time_days ? 'cycle_time_days-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cycle_time_days ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cycle_time_days && (
            <div
              id="cycle_time_days-error"
              role="alert"
              data-testid="wbs-field-cycle_time_days-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cycle_time_days}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprints_done"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprints Done *
          </label>
          <input
            type="number"
            id="sprints_done"
            data-testid="wbs-field-sprints_done"
            value={(formData.sprints_done as number) || 0}
            onChange={(e) => handleChange('sprints_done', Number(e.target.value))}
            aria-invalid={!!errors.sprints_done}
            aria-describedby={errors.sprints_done ? 'sprints_done-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprints_done ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprints_done && (
            <div
              id="sprints_done-error"
              role="alert"
              data-testid="wbs-field-sprints_done-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprints_done}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprint_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint Start *
          </label>
          <input
            type="text"
            id="sprint_start"
            data-testid="wbs-field-sprint_start"
            value={(formData.sprint_start as string) || ''}
            onChange={(e) => handleChange('sprint_start', e.target.value)}
            aria-invalid={!!errors.sprint_start}
            aria-describedby={errors.sprint_start ? 'sprint_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprint_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint_start && (
            <div
              id="sprint_start-error"
              role="alert"
              data-testid="wbs-field-sprint_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="actual_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Actual End *
          </label>
          <input
            type="text"
            id="actual_end"
            data-testid="wbs-field-actual_end"
            value={(formData.actual_end as string) || ''}
            onChange={(e) => handleChange('actual_end', e.target.value)}
            aria-invalid={!!errors.actual_end}
            aria-describedby={errors.actual_end ? 'actual_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.actual_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.actual_end && (
            <div
              id="actual_end-error"
              role="alert"
              data-testid="wbs-field-actual_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.actual_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="stories_total"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Stories Total *
          </label>
          <input
            type="text"
            id="stories_total"
            data-testid="wbs-field-stories_total"
            value={(formData.stories_total as string) || ''}
            onChange={(e) => handleChange('stories_total', e.target.value)}
            aria-invalid={!!errors.stories_total}
            aria-describedby={errors.stories_total ? 'stories_total-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.stories_total ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.stories_total && (
            <div
              id="stories_total-error"
              role="alert"
              data-testid="wbs-field-stories_total-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.stories_total}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="depends_on_wbs"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Depends On Wbs *
          </label>
          <textarea
            id="depends_on_wbs"
            data-testid="wbs-field-depends_on_wbs"
            value={(formData.depends_on_wbs as string) || ''}
            onChange={(e) => handleChange('depends_on_wbs', e.target.value)}
            aria-invalid={!!errors.depends_on_wbs}
            aria-describedby={errors.depends_on_wbs ? 'depends_on_wbs-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.depends_on_wbs ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.depends_on_wbs && (
            <div
              id="depends_on_wbs-error"
              role="alert"
              data-testid="wbs-field-depends_on_wbs-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.depends_on_wbs}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="actual_cost"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Actual Cost *
          </label>
          <input
            type="text"
            id="actual_cost"
            data-testid="wbs-field-actual_cost"
            value={(formData.actual_cost as string) || ''}
            onChange={(e) => handleChange('actual_cost', e.target.value)}
            aria-invalid={!!errors.actual_cost}
            aria-describedby={errors.actual_cost ? 'actual_cost-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.actual_cost ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.actual_cost && (
            <div
              id="actual_cost-error"
              role="alert"
              data-testid="wbs-field-actual_cost-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.actual_cost}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="percent_complete"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Percent Complete *
          </label>
          <input
            type="number"
            id="percent_complete"
            data-testid="wbs-field-percent_complete"
            value={(formData.percent_complete as number) || 0}
            onChange={(e) => handleChange('percent_complete', Number(e.target.value))}
            aria-invalid={!!errors.percent_complete}
            aria-describedby={errors.percent_complete ? 'percent_complete-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.percent_complete ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.percent_complete && (
            <div
              id="percent_complete-error"
              role="alert"
              data-testid="wbs-field-percent_complete-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.percent_complete}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cpi"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cpi *
          </label>
          <input
            type="text"
            id="cpi"
            data-testid="wbs-field-cpi"
            value={(formData.cpi as string) || ''}
            onChange={(e) => handleChange('cpi', e.target.value)}
            aria-invalid={!!errors.cpi}
            aria-describedby={errors.cpi ? 'cpi-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cpi ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.cpi && (
            <div
              id="cpi-error"
              role="alert"
              data-testid="wbs-field-cpi-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cpi}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="methodology"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Methodology *
          </label>
          <input
            type="text"
            id="methodology"
            data-testid="wbs-field-methodology"
            value={(formData.methodology as string) || ''}
            onChange={(e) => handleChange('methodology', e.target.value)}
            aria-invalid={!!errors.methodology}
            aria-describedby={errors.methodology ? 'methodology-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.methodology ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.methodology && (
            <div
              id="methodology-error"
              role="alert"
              data-testid="wbs-field-methodology-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.methodology}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="phase_gate"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Phase Gate
          </label>
          <input
            type="text"
            id="phase_gate"
            data-testid="wbs-field-phase_gate"
            value={(formData.phase_gate as string) || ''}
            onChange={(e) => handleChange('phase_gate', e.target.value)}
            aria-invalid={!!errors.phase_gate}
            aria-describedby={errors.phase_gate ? 'phase_gate-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.phase_gate ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.phase_gate && (
            <div
              id="phase_gate-error"
              role="alert"
              data-testid="wbs-field-phase_gate-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.phase_gate}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="parent_wbs_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Parent Wbs Id *
          </label>
          <input
            type="text"
            id="parent_wbs_id"
            data-testid="wbs-field-parent_wbs_id"
            value={(formData.parent_wbs_id as string) || ''}
            onChange={(e) => handleChange('parent_wbs_id', e.target.value)}
            aria-invalid={!!errors.parent_wbs_id}
            aria-describedby={errors.parent_wbs_id ? 'parent_wbs_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.parent_wbs_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.parent_wbs_id && (
            <div
              id="parent_wbs_id-error"
              role="alert"
              data-testid="wbs-field-parent_wbs_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.parent_wbs_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="sprint_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Sprint End *
          </label>
          <input
            type="text"
            id="sprint_end"
            data-testid="wbs-field-sprint_end"
            value={(formData.sprint_end as string) || ''}
            onChange={(e) => handleChange('sprint_end', e.target.value)}
            aria-invalid={!!errors.sprint_end}
            aria-describedby={errors.sprint_end ? 'sprint_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.sprint_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.sprint_end && (
            <div
              id="sprint_end-error"
              role="alert"
              data-testid="wbs-field-sprint_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.sprint_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="_cached"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
             Cached *
          </label>
          <input
            type="checkbox"
            id="_cached"
            data-testid="wbs-field-_cached"
            checked={(formData._cached as boolean) || false}
            onChange={(e) => handleChange('_cached', e.target.checked)}
            aria-invalid={!!errors._cached}
            aria-describedby={errors._cached ? '_cached-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors._cached && (
            <div
              id="_cached-error"
              role="alert"
              data-testid="wbs-field-_cached-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors._cached}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="budget_at_completion"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Budget At Completion *
          </label>
          <input
            type="text"
            id="budget_at_completion"
            data-testid="wbs-field-budget_at_completion"
            value={(formData.budget_at_completion as string) || ''}
            onChange={(e) => handleChange('budget_at_completion', e.target.value)}
            aria-invalid={!!errors.budget_at_completion}
            aria-describedby={errors.budget_at_completion ? 'budget_at_completion-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.budget_at_completion ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.budget_at_completion && (
            <div
              id="budget_at_completion-error"
              role="alert"
              data-testid="wbs-field-budget_at_completion-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.budget_at_completion}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="risk_level"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Risk Level *
          </label>
          <input
            type="text"
            id="risk_level"
            data-testid="wbs-field-risk_level"
            value={(formData.risk_level as string) || ''}
            onChange={(e) => handleChange('risk_level', e.target.value)}
            aria-invalid={!!errors.risk_level}
            aria-describedby={errors.risk_level ? 'risk_level-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.risk_level ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.risk_level && (
            <div
              id="risk_level-error"
              role="alert"
              data-testid="wbs-field-risk_level-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.risk_level}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="velocity"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Velocity *
          </label>
          <input
            type="text"
            id="velocity"
            data-testid="wbs-field-velocity"
            value={(formData.velocity as string) || ''}
            onChange={(e) => handleChange('velocity', e.target.value)}
            aria-invalid={!!errors.velocity}
            aria-describedby={errors.velocity ? 'velocity-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.velocity ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.velocity && (
            <div
              id="velocity-error"
              role="alert"
              data-testid="wbs-field-velocity-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.velocity}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="wbs-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t.cancel}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="wbs-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t.submitting : t.submit}
        </button>
      </div>
    </form>
  );
};

