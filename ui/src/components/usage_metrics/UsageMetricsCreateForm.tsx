/**
 * UsageMetricsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L43 (usage_metrics)
 * Generated: 2026-03-12T09:01:03Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new usage_metrics records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createUsageMetricsRecord } from '@api/usage_metricsApi';
import type { UsageMetricsRecord, CreateUsageMetricsRecordInput } from '@/types/usage_metrics';

interface UsageMetricsCreateFormProps {
  onSuccess: (record: UsageMetricsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const UsageMetricsCreateForm: React.FC<UsageMetricsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('usage_metrics.create_form');

  const [formData, setFormData] = useState<Partial<CreateUsageMetricsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createUsageMetricsRecord(formData as CreateUsageMetricsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="usage_metrics-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="usage_metrics-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="metric_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Metric Type *
          </label>
          <input
            type="text"
            id="metric_type"
            data-testid="usage_metrics-field-metric_type"
            value={(formData.metric_type as string) || ''}
            onChange={(e) => handleChange('metric_type', e.target.value)}
            aria-invalid={!!errors.metric_type}
            aria-describedby={errors.metric_type ? 'metric_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.metric_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.metric_type && (
            <div
              id="metric_type-error"
              role="alert"
              data-testid="usage_metrics-field-metric_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.metric_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="usage_metrics-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="usage_metrics-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="time_period"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Time Period *
          </label>
          <input
            type="text"
            id="time_period"
            data-testid="usage_metrics-field-time_period"
            value={(formData.time_period as string) || ''}
            onChange={(e) => handleChange('time_period', e.target.value)}
            aria-invalid={!!errors.time_period}
            aria-describedby={errors.time_period ? 'time_period-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.time_period ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.time_period && (
            <div
              id="time_period-error"
              role="alert"
              data-testid="usage_metrics-field-time_period-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.time_period}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="time_granularity"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Time Granularity *
          </label>
          <input
            type="text"
            id="time_granularity"
            data-testid="usage_metrics-field-time_granularity"
            value={(formData.time_granularity as string) || ''}
            onChange={(e) => handleChange('time_granularity', e.target.value)}
            aria-invalid={!!errors.time_granularity}
            aria-describedby={errors.time_granularity ? 'time_granularity-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.time_granularity ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.time_granularity && (
            <div
              id="time_granularity-error"
              role="alert"
              data-testid="usage_metrics-field-time_granularity-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.time_granularity}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="total_write_operations"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Total Write Operations
          </label>
          <input
            type="number"
            id="total_write_operations"
            data-testid="usage_metrics-field-total_write_operations"
            value={(formData.total_write_operations as number) || 0}
            onChange={(e) => handleChange('total_write_operations', Number(e.target.value))}
            aria-invalid={!!errors.total_write_operations}
            aria-describedby={errors.total_write_operations ? 'total_write_operations-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.total_write_operations ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.total_write_operations && (
            <div
              id="total_write_operations-error"
              role="alert"
              data-testid="usage_metrics-field-total_write_operations-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.total_write_operations}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="operations_by_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Operations By Type
          </label>
          <textarea
            id="operations_by_type"
            data-testid="usage_metrics-field-operations_by_type"
            value={(formData.operations_by_type as string) || ''}
            onChange={(e) => handleChange('operations_by_type', e.target.value)}
            aria-invalid={!!errors.operations_by_type}
            aria-describedby={errors.operations_by_type ? 'operations_by_type-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.operations_by_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.operations_by_type && (
            <div
              id="operations_by_type-error"
              role="alert"
              data-testid="usage_metrics-field-operations_by_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.operations_by_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="most_updated_layers"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Most Updated Layers
          </label>
          <textarea
            id="most_updated_layers"
            data-testid="usage_metrics-field-most_updated_layers"
            value={(formData.most_updated_layers as string) || ''}
            onChange={(e) => handleChange('most_updated_layers', e.target.value)}
            aria-invalid={!!errors.most_updated_layers}
            aria-describedby={errors.most_updated_layers ? 'most_updated_layers-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.most_updated_layers ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.most_updated_layers && (
            <div
              id="most_updated_layers-error"
              role="alert"
              data-testid="usage_metrics-field-most_updated_layers-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.most_updated_layers}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="write_success_rate_percent"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Write Success Rate Percent
          </label>
          <input
            type="number"
            id="write_success_rate_percent"
            data-testid="usage_metrics-field-write_success_rate_percent"
            value={(formData.write_success_rate_percent as number) || 0}
            onChange={(e) => handleChange('write_success_rate_percent', Number(e.target.value))}
            aria-invalid={!!errors.write_success_rate_percent}
            aria-describedby={errors.write_success_rate_percent ? 'write_success_rate_percent-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.write_success_rate_percent ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.write_success_rate_percent && (
            <div
              id="write_success_rate_percent-error"
              role="alert"
              data-testid="usage_metrics-field-write_success_rate_percent-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.write_success_rate_percent}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="average_write_latency_ms"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Average Write Latency Ms
          </label>
          <input
            type="number"
            id="average_write_latency_ms"
            data-testid="usage_metrics-field-average_write_latency_ms"
            value={(formData.average_write_latency_ms as number) || 0}
            onChange={(e) => handleChange('average_write_latency_ms', Number(e.target.value))}
            aria-invalid={!!errors.average_write_latency_ms}
            aria-describedby={errors.average_write_latency_ms ? 'average_write_latency_ms-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.average_write_latency_ms ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.average_write_latency_ms && (
            <div
              id="average_write_latency_ms-error"
              role="alert"
              data-testid="usage_metrics-field-average_write_latency_ms-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.average_write_latency_ms}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="commits_with_violations"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Commits With Violations
          </label>
          <input
            type="number"
            id="commits_with_violations"
            data-testid="usage_metrics-field-commits_with_violations"
            value={(formData.commits_with_violations as number) || 0}
            onChange={(e) => handleChange('commits_with_violations', Number(e.target.value))}
            aria-invalid={!!errors.commits_with_violations}
            aria-describedby={errors.commits_with_violations ? 'commits_with_violations-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.commits_with_violations ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.commits_with_violations && (
            <div
              id="commits_with_violations-error"
              role="alert"
              data-testid="usage_metrics-field-commits_with_violations-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.commits_with_violations}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="usage_metrics-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="usage_metrics-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="usage_metrics-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="usage_metrics-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

