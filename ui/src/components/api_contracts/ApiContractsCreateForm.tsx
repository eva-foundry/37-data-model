/**
 * ApiContractsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L12 (api_contracts)
 * Generated: 2026-03-12T09:00:42Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new api_contracts records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createApiContractsRecord } from '@api/api_contractsApi';
import type { ApiContractsRecord, CreateApiContractsRecordInput } from '@/types/api_contracts';

interface ApiContractsCreateFormProps {
  onSuccess: (record: ApiContractsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const ApiContractsCreateForm: React.FC<ApiContractsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('api_contracts.create_form');

  const [formData, setFormData] = useState<Partial<CreateApiContractsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createApiContractsRecord(formData as CreateApiContractsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="api_contracts-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="api_contracts-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="endpoint_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Endpoint Id *
          </label>
          <input
            type="text"
            id="endpoint_id"
            data-testid="api_contracts-field-endpoint_id"
            value={(formData.endpoint_id as string) || ''}
            onChange={(e) => handleChange('endpoint_id', e.target.value)}
            aria-invalid={!!errors.endpoint_id}
            aria-describedby={errors.endpoint_id ? 'endpoint_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.endpoint_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.endpoint_id && (
            <div
              id="endpoint_id-error"
              role="alert"
              data-testid="api_contracts-field-endpoint_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.endpoint_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="openapi_version"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Openapi Version *
          </label>
          <input
            type="text"
            id="openapi_version"
            data-testid="api_contracts-field-openapi_version"
            value={(formData.openapi_version as string) || ''}
            onChange={(e) => handleChange('openapi_version', e.target.value)}
            aria-invalid={!!errors.openapi_version}
            aria-describedby={errors.openapi_version ? 'openapi_version-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.openapi_version ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.openapi_version && (
            <div
              id="openapi_version-error"
              role="alert"
              data-testid="api_contracts-field-openapi_version-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.openapi_version}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="title"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Title *
          </label>
          <input
            type="text"
            id="title"
            data-testid="api_contracts-field-title"
            value={(formData.title as string) || ''}
            onChange={(e) => handleChange('title', e.target.value)}
            aria-invalid={!!errors.title}
            aria-describedby={errors.title ? 'title-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.title ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.title && (
            <div
              id="title-error"
              role="alert"
              data-testid="api_contracts-field-title-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.title}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="description"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Description *
          </label>
          <textarea
            id="description"
            data-testid="api_contracts-field-description"
            value={(formData.description as string) || ''}
            onChange={(e) => handleChange('description', e.target.value)}
            aria-invalid={!!errors.description}
            aria-describedby={errors.description ? 'description-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.description ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.description && (
            <div
              id="description-error"
              role="alert"
              data-testid="api_contracts-field-description-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.description}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="request_schema"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Request Schema *
          </label>
          <textarea
            id="request_schema"
            data-testid="api_contracts-field-request_schema"
            value={(formData.request_schema as string) || ''}
            onChange={(e) => handleChange('request_schema', e.target.value)}
            aria-invalid={!!errors.request_schema}
            aria-describedby={errors.request_schema ? 'request_schema-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.request_schema ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.request_schema && (
            <div
              id="request_schema-error"
              role="alert"
              data-testid="api_contracts-field-request_schema-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.request_schema}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="response_schema"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Response Schema *
          </label>
          <textarea
            id="response_schema"
            data-testid="api_contracts-field-response_schema"
            value={(formData.response_schema as string) || ''}
            onChange={(e) => handleChange('response_schema', e.target.value)}
            aria-invalid={!!errors.response_schema}
            aria-describedby={errors.response_schema ? 'response_schema-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.response_schema ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.response_schema && (
            <div
              id="response_schema-error"
              role="alert"
              data-testid="api_contracts-field-response_schema-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.response_schema}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status_codes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status Codes *
          </label>
          <textarea
            id="status_codes"
            data-testid="api_contracts-field-status_codes"
            value={(formData.status_codes as string) || ''}
            onChange={(e) => handleChange('status_codes', e.target.value)}
            aria-invalid={!!errors.status_codes}
            aria-describedby={errors.status_codes ? 'status_codes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status_codes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.status_codes && (
            <div
              id="status_codes-error"
              role="alert"
              data-testid="api_contracts-field-status_codes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status_codes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="auth_required"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Auth Required *
          </label>
          <input
            type="checkbox"
            id="auth_required"
            data-testid="api_contracts-field-auth_required"
            checked={(formData.auth_required as boolean) || false}
            onChange={(e) => handleChange('auth_required', e.target.checked)}
            aria-invalid={!!errors.auth_required}
            aria-describedby={errors.auth_required ? 'auth_required-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.auth_required && (
            <div
              id="auth_required-error"
              role="alert"
              data-testid="api_contracts-field-auth_required-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.auth_required}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rate_limit"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rate Limit *
          </label>
          <input
            type="text"
            id="rate_limit"
            data-testid="api_contracts-field-rate_limit"
            value={(formData.rate_limit as string) || ''}
            onChange={(e) => handleChange('rate_limit', e.target.value)}
            aria-invalid={!!errors.rate_limit}
            aria-describedby={errors.rate_limit ? 'rate_limit-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rate_limit ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rate_limit && (
            <div
              id="rate_limit-error"
              role="alert"
              data-testid="api_contracts-field-rate_limit-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rate_limit}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deprecation_date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deprecation Date *
          </label>
          <input
            type="text"
            id="deprecation_date"
            data-testid="api_contracts-field-deprecation_date"
            value={(formData.deprecation_date as string) || ''}
            onChange={(e) => handleChange('deprecation_date', e.target.value)}
            aria-invalid={!!errors.deprecation_date}
            aria-describedby={errors.deprecation_date ? 'deprecation_date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deprecation_date ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.deprecation_date && (
            <div
              id="deprecation_date-error"
              role="alert"
              data-testid="api_contracts-field-deprecation_date-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deprecation_date}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="version_released"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Version Released *
          </label>
          <input
            type="text"
            id="version_released"
            data-testid="api_contracts-field-version_released"
            value={(formData.version_released as string) || ''}
            onChange={(e) => handleChange('version_released', e.target.value)}
            aria-invalid={!!errors.version_released}
            aria-describedby={errors.version_released ? 'version_released-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.version_released ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.version_released && (
            <div
              id="version_released-error"
              role="alert"
              data-testid="api_contracts-field-version_released-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.version_released}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="api_contracts-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="api_contracts-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="api_contracts-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="api_contracts-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

