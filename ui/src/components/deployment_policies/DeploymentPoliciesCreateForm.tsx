/**
 * DeploymentPoliciesCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L36 (deployment_policies)
 * Generated: 2026-03-12T09:00:58Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new deployment_policies records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createDeploymentPoliciesRecord } from '@api/deployment_policiesApi';
import type { DeploymentPoliciesRecord, CreateDeploymentPoliciesRecordInput } from '@/types/deployment_policies';

interface DeploymentPoliciesCreateFormProps {
  onSuccess: (record: DeploymentPoliciesRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const DeploymentPoliciesCreateForm: React.FC<DeploymentPoliciesCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('deployment_policies.create_form');

  const [formData, setFormData] = useState<Partial<CreateDeploymentPoliciesRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createDeploymentPoliciesRecord(formData as CreateDeploymentPoliciesRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="deployment_policies-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="deployment_policies-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="project_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Id *
          </label>
          <input
            type="text"
            id="project_id"
            data-testid="deployment_policies-field-project_id"
            value={(formData.project_id as string) || ''}
            onChange={(e) => handleChange('project_id', e.target.value)}
            aria-invalid={!!errors.project_id}
            aria-describedby={errors.project_id ? 'project_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.project_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_id && (
            <div
              id="project_id-error"
              role="alert"
              data-testid="deployment_policies-field-project_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="project_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Name *
          </label>
          <input
            type="text"
            id="project_name"
            data-testid="deployment_policies-field-project_name"
            value={(formData.project_name as string) || ''}
            onChange={(e) => handleChange('project_name', e.target.value)}
            aria-invalid={!!errors.project_name}
            aria-describedby={errors.project_name ? 'project_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.project_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_name && (
            <div
              id="project_name-error"
              role="alert"
              data-testid="deployment_policies-field-project_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="description"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Description *
          </label>
          <textarea
            id="description"
            data-testid="deployment_policies-field-description"
            value={(formData.description as string) || ''}
            onChange={(e) => handleChange('description', e.target.value)}
            aria-invalid={!!errors.description}
            aria-describedby={errors.description ? 'description-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.description ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.description && (
            <div
              id="description-error"
              role="alert"
              data-testid="deployment_policies-field-description-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.description}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="container_app"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Container App *
          </label>
          <textarea
            id="container_app"
            data-testid="deployment_policies-field-container_app"
            value={(formData.container_app as string) || ''}
            onChange={(e) => handleChange('container_app', e.target.value)}
            aria-invalid={!!errors.container_app}
            aria-describedby={errors.container_app ? 'container_app-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.container_app ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.container_app && (
            <div
              id="container_app-error"
              role="alert"
              data-testid="deployment_policies-field-container_app-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.container_app}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_limits"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Limits *
          </label>
          <textarea
            id="resource_limits"
            data-testid="deployment_policies-field-resource_limits"
            value={(formData.resource_limits as string) || ''}
            onChange={(e) => handleChange('resource_limits', e.target.value)}
            aria-invalid={!!errors.resource_limits}
            aria-describedby={errors.resource_limits ? 'resource_limits-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_limits ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.resource_limits && (
            <div
              id="resource_limits-error"
              role="alert"
              data-testid="deployment_policies-field-resource_limits-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_limits}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="scaling"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Scaling *
          </label>
          <textarea
            id="scaling"
            data-testid="deployment_policies-field-scaling"
            value={(formData.scaling as string) || ''}
            onChange={(e) => handleChange('scaling', e.target.value)}
            aria-invalid={!!errors.scaling}
            aria-describedby={errors.scaling ? 'scaling-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.scaling ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.scaling && (
            <div
              id="scaling-error"
              role="alert"
              data-testid="deployment_policies-field-scaling-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.scaling}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="health_probes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Health Probes *
          </label>
          <textarea
            id="health_probes"
            data-testid="deployment_policies-field-health_probes"
            value={(formData.health_probes as string) || ''}
            onChange={(e) => handleChange('health_probes', e.target.value)}
            aria-invalid={!!errors.health_probes}
            aria-describedby={errors.health_probes ? 'health_probes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.health_probes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.health_probes && (
            <div
              id="health_probes-error"
              role="alert"
              data-testid="deployment_policies-field-health_probes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.health_probes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="revision_strategy"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Revision Strategy *
          </label>
          <textarea
            id="revision_strategy"
            data-testid="deployment_policies-field-revision_strategy"
            value={(formData.revision_strategy as string) || ''}
            onChange={(e) => handleChange('revision_strategy', e.target.value)}
            aria-invalid={!!errors.revision_strategy}
            aria-describedby={errors.revision_strategy ? 'revision_strategy-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.revision_strategy ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.revision_strategy && (
            <div
              id="revision_strategy-error"
              role="alert"
              data-testid="deployment_policies-field-revision_strategy-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.revision_strategy}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deployment_gates"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Gates *
          </label>
          <textarea
            id="deployment_gates"
            data-testid="deployment_policies-field-deployment_gates"
            value={(formData.deployment_gates as string) || ''}
            onChange={(e) => handleChange('deployment_gates', e.target.value)}
            aria-invalid={!!errors.deployment_gates}
            aria-describedby={errors.deployment_gates ? 'deployment_gates-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_gates ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.deployment_gates && (
            <div
              id="deployment_gates-error"
              role="alert"
              data-testid="deployment_policies-field-deployment_gates-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_gates}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="environment_variables"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Environment Variables *
          </label>
          <textarea
            id="environment_variables"
            data-testid="deployment_policies-field-environment_variables"
            value={(formData.environment_variables as string) || ''}
            onChange={(e) => handleChange('environment_variables', e.target.value)}
            aria-invalid={!!errors.environment_variables}
            aria-describedby={errors.environment_variables ? 'environment_variables-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.environment_variables ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.environment_variables && (
            <div
              id="environment_variables-error"
              role="alert"
              data-testid="deployment_policies-field-environment_variables-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.environment_variables}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deployment_automation"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deployment Automation
          </label>
          <textarea
            id="deployment_automation"
            data-testid="deployment_policies-field-deployment_automation"
            value={(formData.deployment_automation as string) || ''}
            onChange={(e) => handleChange('deployment_automation', e.target.value)}
            aria-invalid={!!errors.deployment_automation}
            aria-describedby={errors.deployment_automation ? 'deployment_automation-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deployment_automation ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.deployment_automation && (
            <div
              id="deployment_automation-error"
              role="alert"
              data-testid="deployment_policies-field-deployment_automation-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deployment_automation}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="deployment_policies-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="deployment_policies-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="deployment_policies-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="deployment_policies-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

