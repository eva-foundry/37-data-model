/**
 * AgentsOntologyEditForm -- Auto-generated by Screens Machine v2.0.0
 *
 * Data Model Layer: L01 (agents_ontology)
 * Generated: 2026-03-11T06:28:13Z
 * Generator: screens-machine-v2.0.0
 *
 * Form for editing existing agents_ontology records with validation and error handling.
 *
 * Features:
 * - Pre-populated with existing values
 * - Client-side validation
 * - Field-level error messages
 * - Update/Cancel actions
 * - Accessible form markup
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState, useEffect } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import type {
  AgentsOntologyRecord,
  UpdateAgentsOntologyRecordInput,
} from '@/types/agents_ontology';

interface AgentsOntologyEditFormProps {
  record: AgentsOntologyRecord;
  onSuccess: (record: AgentsOntologyRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';
const GC_FONT    = 'Noto Sans, sans-serif';

// API constants
const GC_API_METHOD_PUT    = 'PUT';
const GC_API_HEADER_CT     = 'Content-Type';
const GC_API_HEADER_ACTOR  = 'X-Actor';
const GC_API_ENV_KEY       = 'VITE_DATA_MODEL_URL';
const GC_API_DEFAULT_BASE  =
  'https://msub-eva-data-model.victoriousgrass-30debbd3.canadacentral.azurecontainerapps.io';

// Stub API function (replace with real @api/agentsOntologyApi when available)
async function updateAgentsOntologyRecord(
  id: string,
  input: UpdateAgentsOntologyRecordInput
): Promise<AgentsOntologyRecord> {
  const base =
    (typeof import.meta !== 'undefined' &&
      (import.meta as { env?: Record<string, string> }).env?.[GC_API_ENV_KEY]) ||
    GC_API_DEFAULT_BASE;
  const res = await fetch(`${base}/model/agents_ontology/${id}`, {
    method: GC_API_METHOD_PUT,
    headers: {
      [GC_API_HEADER_CT]: 'application/json',
      [GC_API_HEADER_ACTOR]: 'ui:edit-form',
    },
    body: JSON.stringify(input),
  });
  if (!res.ok) throw new Error(`Update failed: ${res.status}`);
  return res.json() as Promise<AgentsOntologyRecord>;
}

export const AgentsOntologyEditForm: React.FC<AgentsOntologyEditFormProps> = ({
  record,
  onSuccess,
  onCancel,
}) => {
  const t = useLiterals('agents_ontology.edit_form');

  const [formData, setFormData] =
    useState<Partial<UpdateAgentsOntologyRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  // Initialize form data with existing record values
  useEffect(() => {
    const initial: Partial<UpdateAgentsOntologyRecordInput> = {};
    initial.id = record.id;
    initial.label = record.label;
    initial.label_fr = record.label_fr;
    initial.agent_type = record.agent_type;
    initial.domain = record.domain;
    initial.ontology_class = record.ontology_class;
    initial.status = record.status;
    initial.description = record.description;
    initial.capabilities = record.capabilities;
    initial.skills = record.skills;
    initial.version = record.version;
    initial.parent_id = record.parent_id;
    initial.notes = record.notes;
    setFormData(initial);
  }, [record]);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};

    if (!formData.id) {
      newErrors['id'] = t('errors.required');
    }
    if (!formData.label) {
      newErrors['label'] = t('errors.required');
    }
    if (!formData.label_fr) {
      newErrors['label_fr'] = t('errors.required');
    }
    if (!formData.agent_type) {
      newErrors['agent_type'] = t('errors.required');
    }
    if (!formData.domain) {
      newErrors['domain'] = t('errors.required');
    }
    if (!formData.ontology_class) {
      newErrors['ontology_class'] = t('errors.required');
    }
    if (!formData.status) {
      newErrors['status'] = t('errors.required');
    }
    if (!formData.description) {
      newErrors['description'] = t('errors.required');
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const updated = await updateAgentsOntologyRecord(
        record.id,
        formData as UpdateAgentsOntologyRecordInput
      );
      onSuccess(updated);
    } catch (error) {
      setSubmitError(
        error instanceof Error ? error.message : t('errors.unknown')
      );
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: string | string[]) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  const fieldStyle: React.CSSProperties = {
    display: 'flex',
    flexDirection: 'column',
    gap: 4,
  };

  const labelStyle: React.CSSProperties = {
    fontSize: '0.875rem',
    fontWeight: 600,
    color: GC_TEXT,
  };

  const inputStyle = (hasError: boolean): React.CSSProperties => ({
    padding: '8px 10px',
    border: `2px solid ${hasError ? GC_ERROR : GC_BORDER}`,
    borderRadius: 4,
    fontSize: '0.875rem',
    color: GC_TEXT,
    background: '#fff',
    outline: 'none',
    width: '100%',
    boxSizing: 'border-box' as const,
  });

  const errorStyle: React.CSSProperties = {
    fontSize: '0.75rem',
    color: GC_ERROR,
    marginTop: 2,
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="agents-ontology-edit-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: GC_FONT,
      }}
    >
      {/* Form header */}
      <h2
        style={{
          margin: '0 0 8px',
          fontSize: '1.25rem',
          fontWeight: 700,
          color: GC_TEXT,
        }}
      >
        {t('title')}
      </h2>
      <p style={{ margin: '0 0 24px', fontSize: '0.78rem', color: GC_MUTED }}>
        {t('subtitle')}: <strong>{record.id}</strong>
      </p>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="agents-ontology-edit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
        {/* id (read-only) */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-id" style={labelStyle}>
            {t('fields.id')}
          </label>
          <input
            id="agents-ontology-edit-id"
            type="text"
            value={(formData.id as string) ?? ''}
            readOnly
            data-testid="agents-ontology-edit-field-id"
            style={{ ...inputStyle(false), background: GC_SURFACE, color: GC_MUTED }}
          />
        </div>

        {/* label */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-label" style={labelStyle}>
            {t('fields.label')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <input
            id="agents-ontology-edit-label"
            type="text"
            value={(formData.label as string) ?? ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-required="true"
            aria-describedby={errors['label'] ? 'agents-ontology-edit-label-error' : undefined}
            data-testid="agents-ontology-edit-field-label"
            style={inputStyle(!!errors['label'])}
          />
          {errors['label'] && (
            <span
              id="agents-ontology-edit-label-error"
              style={errorStyle}
              role="alert"
            >
              {errors['label']}
            </span>
          )}
        </div>

        {/* label_fr */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-label-fr" style={labelStyle}>
            {t('fields.label_fr')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <input
            id="agents-ontology-edit-label-fr"
            type="text"
            value={(formData.label_fr as string) ?? ''}
            onChange={(e) => handleChange('label_fr', e.target.value)}
            aria-required="true"
            aria-describedby={errors['label_fr'] ? 'agents-ontology-edit-label-fr-error' : undefined}
            data-testid="agents-ontology-edit-field-label-fr"
            style={inputStyle(!!errors['label_fr'])}
          />
          {errors['label_fr'] && (
            <span
              id="agents-ontology-edit-label-fr-error"
              style={errorStyle}
              role="alert"
            >
              {errors['label_fr']}
            </span>
          )}
        </div>

        {/* agent_type */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-agent-type" style={labelStyle}>
            {t('fields.agent_type')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <input
            id="agents-ontology-edit-agent-type"
            type="text"
            value={(formData.agent_type as string) ?? ''}
            onChange={(e) => handleChange('agent_type', e.target.value)}
            aria-required="true"
            aria-describedby={
              errors['agent_type'] ? 'agents-ontology-edit-agent-type-error' : undefined
            }
            data-testid="agents-ontology-edit-field-agent-type"
            style={inputStyle(!!errors['agent_type'])}
          />
          {errors['agent_type'] && (
            <span
              id="agents-ontology-edit-agent-type-error"
              style={errorStyle}
              role="alert"
            >
              {errors['agent_type']}
            </span>
          )}
        </div>

        {/* domain */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-domain" style={labelStyle}>
            {t('fields.domain')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <input
            id="agents-ontology-edit-domain"
            type="text"
            value={(formData.domain as string) ?? ''}
            onChange={(e) => handleChange('domain', e.target.value)}
            aria-required="true"
            aria-describedby={errors['domain'] ? 'agents-ontology-edit-domain-error' : undefined}
            data-testid="agents-ontology-edit-field-domain"
            style={inputStyle(!!errors['domain'])}
          />
          {errors['domain'] && (
            <span
              id="agents-ontology-edit-domain-error"
              style={errorStyle}
              role="alert"
            >
              {errors['domain']}
            </span>
          )}
        </div>

        {/* ontology_class */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-class" style={labelStyle}>
            {t('fields.ontology_class')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <input
            id="agents-ontology-edit-class"
            type="text"
            value={(formData.ontology_class as string) ?? ''}
            onChange={(e) => handleChange('ontology_class', e.target.value)}
            aria-required="true"
            aria-describedby={
              errors['ontology_class'] ? 'agents-ontology-edit-class-error' : undefined
            }
            data-testid="agents-ontology-edit-field-ontology-class"
            style={inputStyle(!!errors['ontology_class'])}
          />
          {errors['ontology_class'] && (
            <span
              id="agents-ontology-edit-class-error"
              style={errorStyle}
              role="alert"
            >
              {errors['ontology_class']}
            </span>
          )}
        </div>

        {/* status */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-status" style={labelStyle}>
            {t('fields.status')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <select
            id="agents-ontology-edit-status"
            value={(formData.status as string) ?? ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-required="true"
            aria-describedby={errors['status'] ? 'agents-ontology-edit-status-error' : undefined}
            data-testid="agents-ontology-edit-field-status"
            style={{
              ...inputStyle(!!errors['status']),
              background: GC_SURFACE,
              cursor: 'pointer',
            }}
          >
            <option value="">{t('fields.status_placeholder')}</option>
            <option value="active">{t('status.active')}</option>
            <option value="planned">{t('status.planned')}</option>
            <option value="deprecated">{t('status.deprecated')}</option>
            <option value="archived">{t('status.archived')}</option>
          </select>
          {errors['status'] && (
            <span
              id="agents-ontology-edit-status-error"
              style={errorStyle}
              role="alert"
            >
              {errors['status']}
            </span>
          )}
        </div>

        {/* description */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-description" style={labelStyle}>
            {t('fields.description')} <span aria-hidden="true" style={{ color: GC_ERROR }}>*</span>
          </label>
          <textarea
            id="agents-ontology-edit-description"
            rows={4}
            value={(formData.description as string) ?? ''}
            onChange={(e) => handleChange('description', e.target.value)}
            aria-required="true"
            aria-describedby={
              errors['description'] ? 'agents-ontology-edit-description-error' : undefined
            }
            data-testid="agents-ontology-edit-field-description"
            style={{
              ...inputStyle(!!errors['description']),
              resize: 'vertical',
            }}
          />
          {errors['description'] && (
            <span
              id="agents-ontology-edit-description-error"
              style={errorStyle}
              role="alert"
            >
              {errors['description']}
            </span>
          )}
        </div>

        {/* version */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-version" style={labelStyle}>
            {t('fields.version')}
          </label>
          <input
            id="agents-ontology-edit-version"
            type="text"
            value={(formData.version as string) ?? ''}
            onChange={(e) => handleChange('version', e.target.value)}
            data-testid="agents-ontology-edit-field-version"
            style={inputStyle(false)}
          />
        </div>

        {/* parent_id */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-parent-id" style={labelStyle}>
            {t('fields.parent_id')}
          </label>
          <input
            id="agents-ontology-edit-parent-id"
            type="text"
            value={(formData.parent_id as string) ?? ''}
            onChange={(e) => handleChange('parent_id', e.target.value)}
            data-testid="agents-ontology-edit-field-parent-id"
            style={inputStyle(false)}
          />
        </div>

        {/* notes */}
        <div style={fieldStyle}>
          <label htmlFor="agents-ontology-edit-notes" style={labelStyle}>
            {t('fields.notes')}
          </label>
          <textarea
            id="agents-ontology-edit-notes"
            rows={3}
            value={(formData.notes as string) ?? ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            data-testid="agents-ontology-edit-field-notes"
            style={{
              ...inputStyle(false),
              resize: 'vertical',
            }}
          />
        </div>
      </div>

      {/* Form actions */}
      <div
        style={{
          display: 'flex',
          gap: 12,
          justifyContent: 'flex-end',
          marginTop: 28,
          paddingTop: 20,
          borderTop: `1px solid ${GC_BORDER}`,
        }}
      >
        <button
          type="button"
          onClick={onCancel}
          data-testid="agents-ontology-edit-cancel-btn"
          style={{
            padding: '8px 20px',
            background: 'transparent',
            border: `2px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            color: GC_TEXT,
            cursor: 'pointer',
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="agents-ontology-edit-submit-btn"
          style={{
            padding: '8px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            color: '#fff',
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('actions.submitting') : t('actions.submit')}
        </button>
      </div>

      {/* Success color reference */}
      <span style={{ display: 'none', color: GC_SUCCESS }} aria-hidden="true" />
    </form>
  );
};

export default AgentsOntologyEditForm;
