/**
 * WorkflowMetricsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L26 (workflow_metrics)
 * Generated: 2026-03-12T09:00:51Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new workflow_metrics records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createWorkflowMetricsRecord } from '@api/workflow_metricsApi';
import type { WorkflowMetricsRecord, CreateWorkflowMetricsRecordInput } from '@/types/workflow_metrics';

interface WorkflowMetricsCreateFormProps {
  onSuccess: (record: WorkflowMetricsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const WorkflowMetricsCreateForm: React.FC<WorkflowMetricsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('workflow_metrics.create_form');

  const [formData, setFormData] = useState<Partial<CreateWorkflowMetricsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createWorkflowMetricsRecord(formData as CreateWorkflowMetricsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="workflow_metrics-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="workflow_metrics-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="workflow_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Workflow Id *
          </label>
          <input
            type="text"
            id="workflow_id"
            data-testid="workflow_metrics-field-workflow_id"
            value={(formData.workflow_id as string) || ''}
            onChange={(e) => handleChange('workflow_id', e.target.value)}
            aria-invalid={!!errors.workflow_id}
            aria-describedby={errors.workflow_id ? 'workflow_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.workflow_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.workflow_id && (
            <div
              id="workflow_id-error"
              role="alert"
              data-testid="workflow_metrics-field-workflow_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.workflow_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="execution_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Count *
          </label>
          <input
            type="number"
            id="execution_count"
            data-testid="workflow_metrics-field-execution_count"
            value={(formData.execution_count as number) || 0}
            onChange={(e) => handleChange('execution_count', Number(e.target.value))}
            aria-invalid={!!errors.execution_count}
            aria-describedby={errors.execution_count ? 'execution_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_count && (
            <div
              id="execution_count-error"
              role="alert"
              data-testid="workflow_metrics-field-execution_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="success_rate_percent"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Success Rate Percent *
          </label>
          <input
            type="number"
            id="success_rate_percent"
            data-testid="workflow_metrics-field-success_rate_percent"
            value={(formData.success_rate_percent as number) || 0}
            onChange={(e) => handleChange('success_rate_percent', Number(e.target.value))}
            aria-invalid={!!errors.success_rate_percent}
            aria-describedby={errors.success_rate_percent ? 'success_rate_percent-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.success_rate_percent ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.success_rate_percent && (
            <div
              id="success_rate_percent-error"
              role="alert"
              data-testid="workflow_metrics-field-success_rate_percent-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.success_rate_percent}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="avg_duration_ms"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Avg Duration Ms *
          </label>
          <input
            type="number"
            id="avg_duration_ms"
            data-testid="workflow_metrics-field-avg_duration_ms"
            value={(formData.avg_duration_ms as number) || 0}
            onChange={(e) => handleChange('avg_duration_ms', Number(e.target.value))}
            aria-invalid={!!errors.avg_duration_ms}
            aria-describedby={errors.avg_duration_ms ? 'avg_duration_ms-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.avg_duration_ms ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.avg_duration_ms && (
            <div
              id="avg_duration_ms-error"
              role="alert"
              data-testid="workflow_metrics-field-avg_duration_ms-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.avg_duration_ms}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="avg_tokens_used"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Avg Tokens Used *
          </label>
          <input
            type="number"
            id="avg_tokens_used"
            data-testid="workflow_metrics-field-avg_tokens_used"
            value={(formData.avg_tokens_used as number) || 0}
            onChange={(e) => handleChange('avg_tokens_used', Number(e.target.value))}
            aria-invalid={!!errors.avg_tokens_used}
            aria-describedby={errors.avg_tokens_used ? 'avg_tokens_used-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.avg_tokens_used ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.avg_tokens_used && (
            <div
              id="avg_tokens_used-error"
              role="alert"
              data-testid="workflow_metrics-field-avg_tokens_used-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.avg_tokens_used}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="error_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Error Count *
          </label>
          <input
            type="number"
            id="error_count"
            data-testid="workflow_metrics-field-error_count"
            value={(formData.error_count as number) || 0}
            onChange={(e) => handleChange('error_count', Number(e.target.value))}
            aria-invalid={!!errors.error_count}
            aria-describedby={errors.error_count ? 'error_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.error_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.error_count && (
            <div
              id="error_count-error"
              role="alert"
              data-testid="workflow_metrics-field-error_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.error_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_execution"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Execution *
          </label>
          <input
            type="datetime-local"
            id="last_execution"
            data-testid="workflow_metrics-field-last_execution"
            value={(formData.last_execution as string) || ''}
            onChange={(e) => handleChange('last_execution', e.target.value)}
            aria-invalid={!!errors.last_execution}
            aria-describedby={errors.last_execution ? 'last_execution-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_execution ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_execution && (
            <div
              id="last_execution-error"
              role="alert"
              data-testid="workflow_metrics-field-last_execution-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_execution}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="workflow_metrics-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="workflow_metrics-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="workflow_metrics-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="workflow_metrics-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

