/**
 * SyntheticTestsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L39 (synthetic_tests)
 * Generated: 2026-03-12T09:01:00Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new synthetic_tests records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createSyntheticTestsRecord } from '@api/synthetic_testsApi';
import type { SyntheticTestsRecord, CreateSyntheticTestsRecordInput } from '@/types/synthetic_tests';

interface SyntheticTestsCreateFormProps {
  onSuccess: (record: SyntheticTestsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const SyntheticTestsCreateForm: React.FC<SyntheticTestsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('synthetic_tests.create_form');

  const [formData, setFormData] = useState<Partial<CreateSyntheticTestsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createSyntheticTestsRecord(formData as CreateSyntheticTestsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="synthetic_tests-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="synthetic_tests-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="endpoint_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Endpoint Id *
          </label>
          <input
            type="text"
            id="endpoint_id"
            data-testid="synthetic_tests-field-endpoint_id"
            value={(formData.endpoint_id as string) || ''}
            onChange={(e) => handleChange('endpoint_id', e.target.value)}
            aria-invalid={!!errors.endpoint_id}
            aria-describedby={errors.endpoint_id ? 'endpoint_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.endpoint_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.endpoint_id && (
            <div
              id="endpoint_id-error"
              role="alert"
              data-testid="synthetic_tests-field-endpoint_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.endpoint_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="environment"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Environment *
          </label>
          <input
            type="text"
            id="environment"
            data-testid="synthetic_tests-field-environment"
            value={(formData.environment as string) || ''}
            onChange={(e) => handleChange('environment', e.target.value)}
            aria-invalid={!!errors.environment}
            aria-describedby={errors.environment ? 'environment-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.environment ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.environment && (
            <div
              id="environment-error"
              role="alert"
              data-testid="synthetic_tests-field-environment-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.environment}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="synthetic_tests-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="synthetic_tests-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="test_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Test Name *
          </label>
          <input
            type="text"
            id="test_name"
            data-testid="synthetic_tests-field-test_name"
            value={(formData.test_name as string) || ''}
            onChange={(e) => handleChange('test_name', e.target.value)}
            aria-invalid={!!errors.test_name}
            aria-describedby={errors.test_name ? 'test_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.test_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.test_name && (
            <div
              id="test_name-error"
              role="alert"
              data-testid="synthetic_tests-field-test_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.test_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="frequency_minutes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Frequency Minutes *
          </label>
          <input
            type="number"
            id="frequency_minutes"
            data-testid="synthetic_tests-field-frequency_minutes"
            value={(formData.frequency_minutes as number) || 0}
            onChange={(e) => handleChange('frequency_minutes', Number(e.target.value))}
            aria-invalid={!!errors.frequency_minutes}
            aria-describedby={errors.frequency_minutes ? 'frequency_minutes-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.frequency_minutes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.frequency_minutes && (
            <div
              id="frequency_minutes-error"
              role="alert"
              data-testid="synthetic_tests-field-frequency_minutes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.frequency_minutes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="timeout_seconds"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Timeout Seconds *
          </label>
          <input
            type="number"
            id="timeout_seconds"
            data-testid="synthetic_tests-field-timeout_seconds"
            value={(formData.timeout_seconds as number) || 0}
            onChange={(e) => handleChange('timeout_seconds', Number(e.target.value))}
            aria-invalid={!!errors.timeout_seconds}
            aria-describedby={errors.timeout_seconds ? 'timeout_seconds-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.timeout_seconds ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.timeout_seconds && (
            <div
              id="timeout_seconds-error"
              role="alert"
              data-testid="synthetic_tests-field-timeout_seconds-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.timeout_seconds}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="expected_response_code"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Expected Response Code *
          </label>
          <input
            type="number"
            id="expected_response_code"
            data-testid="synthetic_tests-field-expected_response_code"
            value={(formData.expected_response_code as number) || 0}
            onChange={(e) => handleChange('expected_response_code', Number(e.target.value))}
            aria-invalid={!!errors.expected_response_code}
            aria-describedby={errors.expected_response_code ? 'expected_response_code-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.expected_response_code ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.expected_response_code && (
            <div
              id="expected_response_code-error"
              role="alert"
              data-testid="synthetic_tests-field-expected_response_code-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.expected_response_code}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_execution"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Execution *
          </label>
          <input
            type="datetime-local"
            id="last_execution"
            data-testid="synthetic_tests-field-last_execution"
            value={(formData.last_execution as string) || ''}
            onChange={(e) => handleChange('last_execution', e.target.value)}
            aria-invalid={!!errors.last_execution}
            aria-describedby={errors.last_execution ? 'last_execution-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_execution ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_execution && (
            <div
              id="last_execution-error"
              role="alert"
              data-testid="synthetic_tests-field-last_execution-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_execution}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_execution_time_ms"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Execution Time Ms *
          </label>
          <input
            type="number"
            id="last_execution_time_ms"
            data-testid="synthetic_tests-field-last_execution_time_ms"
            value={(formData.last_execution_time_ms as number) || 0}
            onChange={(e) => handleChange('last_execution_time_ms', Number(e.target.value))}
            aria-invalid={!!errors.last_execution_time_ms}
            aria-describedby={errors.last_execution_time_ms ? 'last_execution_time_ms-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_execution_time_ms ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_execution_time_ms && (
            <div
              id="last_execution_time_ms-error"
              role="alert"
              data-testid="synthetic_tests-field-last_execution_time_ms-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_execution_time_ms}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="success_rate_percent"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Success Rate Percent *
          </label>
          <input
            type="number"
            id="success_rate_percent"
            data-testid="synthetic_tests-field-success_rate_percent"
            value={(formData.success_rate_percent as number) || 0}
            onChange={(e) => handleChange('success_rate_percent', Number(e.target.value))}
            aria-invalid={!!errors.success_rate_percent}
            aria-describedby={errors.success_rate_percent ? 'success_rate_percent-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.success_rate_percent ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.success_rate_percent && (
            <div
              id="success_rate_percent-error"
              role="alert"
              data-testid="synthetic_tests-field-success_rate_percent-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.success_rate_percent}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="alert_enabled"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Alert Enabled *
          </label>
          <input
            type="checkbox"
            id="alert_enabled"
            data-testid="synthetic_tests-field-alert_enabled"
            checked={(formData.alert_enabled as boolean) || false}
            onChange={(e) => handleChange('alert_enabled', e.target.checked)}
            aria-invalid={!!errors.alert_enabled}
            aria-describedby={errors.alert_enabled ? 'alert_enabled-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.alert_enabled && (
            <div
              id="alert_enabled-error"
              role="alert"
              data-testid="synthetic_tests-field-alert_enabled-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.alert_enabled}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="synthetic_tests-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="synthetic_tests-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="synthetic_tests-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="synthetic_tests-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

