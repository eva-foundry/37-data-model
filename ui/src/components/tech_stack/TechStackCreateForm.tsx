/**
 * TechStackCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L9 (tech_stack)
 * Generated: 2026-03-12T09:00:40Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new tech_stack records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createTechStackRecord } from '@api/tech_stackApi';
import type { TechStackRecord, CreateTechStackRecordInput } from '@/types/tech_stack';

interface TechStackCreateFormProps {
  onSuccess: (record: TechStackRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const TechStackCreateForm: React.FC<TechStackCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('tech_stack.create_form');

  const [formData, setFormData] = useState<Partial<CreateTechStackRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createTechStackRecord(formData as CreateTechStackRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="tech_stack-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="tech_stack-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="tech_stack-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="tech_stack-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="technology"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Technology *
          </label>
          <input
            type="text"
            id="technology"
            data-testid="tech_stack-field-technology"
            value={(formData.technology as string) || ''}
            onChange={(e) => handleChange('technology', e.target.value)}
            aria-invalid={!!errors.technology}
            aria-describedby={errors.technology ? 'technology-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.technology ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.technology && (
            <div
              id="technology-error"
              role="alert"
              data-testid="tech_stack-field-technology-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.technology}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="category"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Category *
          </label>
          <input
            type="text"
            id="category"
            data-testid="tech_stack-field-category"
            value={(formData.category as string) || ''}
            onChange={(e) => handleChange('category', e.target.value)}
            aria-invalid={!!errors.category}
            aria-describedby={errors.category ? 'category-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.category ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.category && (
            <div
              id="category-error"
              role="alert"
              data-testid="tech_stack-field-category-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.category}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="version"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Version *
          </label>
          <input
            type="text"
            id="version"
            data-testid="tech_stack-field-version"
            value={(formData.version as string) || ''}
            onChange={(e) => handleChange('version', e.target.value)}
            aria-invalid={!!errors.version}
            aria-describedby={errors.version ? 'version-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.version ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.version && (
            <div
              id="version-error"
              role="alert"
              data-testid="tech_stack-field-version-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.version}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="license"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            License *
          </label>
          <input
            type="text"
            id="license"
            data-testid="tech_stack-field-license"
            value={(formData.license as string) || ''}
            onChange={(e) => handleChange('license', e.target.value)}
            aria-invalid={!!errors.license}
            aria-describedby={errors.license ? 'license-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.license ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.license && (
            <div
              id="license-error"
              role="alert"
              data-testid="tech_stack-field-license-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.license}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="security_scan_status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Security Scan Status *
          </label>
          <input
            type="text"
            id="security_scan_status"
            data-testid="tech_stack-field-security_scan_status"
            value={(formData.security_scan_status as string) || ''}
            onChange={(e) => handleChange('security_scan_status', e.target.value)}
            aria-invalid={!!errors.security_scan_status}
            aria-describedby={errors.security_scan_status ? 'security_scan_status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.security_scan_status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.security_scan_status && (
            <div
              id="security_scan_status-error"
              role="alert"
              data-testid="tech_stack-field-security_scan_status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.security_scan_status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="last_updated"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Last Updated *
          </label>
          <input
            type="datetime-local"
            id="last_updated"
            data-testid="tech_stack-field-last_updated"
            value={(formData.last_updated as string) || ''}
            onChange={(e) => handleChange('last_updated', e.target.value)}
            aria-invalid={!!errors.last_updated}
            aria-describedby={errors.last_updated ? 'last_updated-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.last_updated ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.last_updated && (
            <div
              id="last_updated-error"
              role="alert"
              data-testid="tech_stack-field-last_updated-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.last_updated}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="tech_stack-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="tech_stack-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="tech_stack-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="tech_stack-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

