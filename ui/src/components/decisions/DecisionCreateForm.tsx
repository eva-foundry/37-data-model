/**
 * DecisionCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L30 (decisions)
 * Generated: 2026-03-11T15:12:22Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new decisions records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createDecisionRecord } from '@api/decisionsApi';
import type { DecisionRecord, CreateDecisionRecordInput } from '@/types/decisions';

interface DecisionCreateFormProps {
  onSuccess: (record: DecisionRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const DecisionCreateForm: React.FC<DecisionCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('decisions.create_form');

  const [formData, setFormData] = useState<Partial<CreateDecisionRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
        if (!formData.project_id) {
      newErrors.project_id = t('errors.required');
    }
    if (!formData.title) {
      newErrors.title = t('errors.required');
    }
    if (!formData.date) {
      newErrors.date = t('errors.required');
    }
    if (!formData.status) {
      newErrors.status = t('errors.required');
    }
    if (!formData.context) {
      newErrors.context = t('errors.required');
    }
    if (!formData.decision) {
      newErrors.decision = t('errors.required');
    }
    if (!formData.consequences) {
      newErrors.consequences = t('errors.required');
    }
    if (!formData.alternatives_considered) {
      newErrors.alternatives_considered = t('errors.required');
    }
    if (!formData.superseded_by) {
      newErrors.superseded_by = t('errors.required');
    }
    if (!formData.deciders) {
      newErrors.deciders = t('errors.required');
    }
    if (!formData.tags) {
      newErrors.tags = t('errors.required');
    }
    if (!formData.related_wbs_ids) {
      newErrors.related_wbs_ids = t('errors.required');
    }
    if (!formData.evidence_path) {
      newErrors.evidence_path = t('errors.required');
    }
    if (!formData.notes) {
      newErrors.notes = t('errors.required');
    }
    if (!formData.is_active) {
      newErrors.is_active = t('errors.required');
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createDecisionRecord(formData as CreateDecisionRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="decisions-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="decisions-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="project_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Id *
          </label>
          <input
            type="text"
            id="project_id"
            data-testid="decisions-field-project_id"
            value={(formData.project_id as string) || ''}
            onChange={(e) => handleChange('project_id', e.target.value)}
            aria-invalid={!!errors.project_id}
            aria-describedby={errors.project_id ? 'project_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.project_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_id && (
            <div
              id="project_id-error"
              role="alert"
              data-testid="decisions-field-project_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="title"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Title *
          </label>
          <input
            type="text"
            id="title"
            data-testid="decisions-field-title"
            value={(formData.title as string) || ''}
            onChange={(e) => handleChange('title', e.target.value)}
            aria-invalid={!!errors.title}
            aria-describedby={errors.title ? 'title-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.title ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.title && (
            <div
              id="title-error"
              role="alert"
              data-testid="decisions-field-title-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.title}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Date *
          </label>
          <input
            type="text"
            id="date"
            data-testid="decisions-field-date"
            value={(formData.date as string) || ''}
            onChange={(e) => handleChange('date', e.target.value)}
            aria-invalid={!!errors.date}
            aria-describedby={errors.date ? 'date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.date ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.date && (
            <div
              id="date-error"
              role="alert"
              data-testid="decisions-field-date-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.date}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="decisions-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="decisions-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="context"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Context *
          </label>
          <input
            type="text"
            id="context"
            data-testid="decisions-field-context"
            value={(formData.context as string) || ''}
            onChange={(e) => handleChange('context', e.target.value)}
            aria-invalid={!!errors.context}
            aria-describedby={errors.context ? 'context-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.context ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.context && (
            <div
              id="context-error"
              role="alert"
              data-testid="decisions-field-context-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.context}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="decision"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Decision *
          </label>
          <input
            type="text"
            id="decision"
            data-testid="decisions-field-decision"
            value={(formData.decision as string) || ''}
            onChange={(e) => handleChange('decision', e.target.value)}
            aria-invalid={!!errors.decision}
            aria-describedby={errors.decision ? 'decision-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.decision ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.decision && (
            <div
              id="decision-error"
              role="alert"
              data-testid="decisions-field-decision-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.decision}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="consequences"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Consequences *
          </label>
          <input
            type="text"
            id="consequences"
            data-testid="decisions-field-consequences"
            value={(formData.consequences as string) || ''}
            onChange={(e) => handleChange('consequences', e.target.value)}
            aria-invalid={!!errors.consequences}
            aria-describedby={errors.consequences ? 'consequences-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.consequences ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.consequences && (
            <div
              id="consequences-error"
              role="alert"
              data-testid="decisions-field-consequences-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.consequences}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="alternatives_considered"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Alternatives Considered *
          </label>
          <textarea
            id="alternatives_considered"
            data-testid="decisions-field-alternatives_considered"
            value={(formData.alternatives_considered as string) || ''}
            onChange={(e) => handleChange('alternatives_considered', e.target.value)}
            aria-invalid={!!errors.alternatives_considered}
            aria-describedby={errors.alternatives_considered ? 'alternatives_considered-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.alternatives_considered ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.alternatives_considered && (
            <div
              id="alternatives_considered-error"
              role="alert"
              data-testid="decisions-field-alternatives_considered-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.alternatives_considered}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="superseded_by"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Superseded By *
          </label>
          <input
            type="text"
            id="superseded_by"
            data-testid="decisions-field-superseded_by"
            value={(formData.superseded_by as string) || ''}
            onChange={(e) => handleChange('superseded_by', e.target.value)}
            aria-invalid={!!errors.superseded_by}
            aria-describedby={errors.superseded_by ? 'superseded_by-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.superseded_by ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.superseded_by && (
            <div
              id="superseded_by-error"
              role="alert"
              data-testid="decisions-field-superseded_by-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.superseded_by}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="deciders"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Deciders *
          </label>
          <textarea
            id="deciders"
            data-testid="decisions-field-deciders"
            value={(formData.deciders as string) || ''}
            onChange={(e) => handleChange('deciders', e.target.value)}
            aria-invalid={!!errors.deciders}
            aria-describedby={errors.deciders ? 'deciders-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.deciders ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.deciders && (
            <div
              id="deciders-error"
              role="alert"
              data-testid="decisions-field-deciders-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.deciders}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="tags"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Tags *
          </label>
          <textarea
            id="tags"
            data-testid="decisions-field-tags"
            value={(formData.tags as string) || ''}
            onChange={(e) => handleChange('tags', e.target.value)}
            aria-invalid={!!errors.tags}
            aria-describedby={errors.tags ? 'tags-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.tags ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.tags && (
            <div
              id="tags-error"
              role="alert"
              data-testid="decisions-field-tags-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.tags}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="related_wbs_ids"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Related Wbs Ids *
          </label>
          <textarea
            id="related_wbs_ids"
            data-testid="decisions-field-related_wbs_ids"
            value={(formData.related_wbs_ids as string) || ''}
            onChange={(e) => handleChange('related_wbs_ids', e.target.value)}
            aria-invalid={!!errors.related_wbs_ids}
            aria-describedby={errors.related_wbs_ids ? 'related_wbs_ids-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.related_wbs_ids ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.related_wbs_ids && (
            <div
              id="related_wbs_ids-error"
              role="alert"
              data-testid="decisions-field-related_wbs_ids-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.related_wbs_ids}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="evidence_path"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Path *
          </label>
          <input
            type="text"
            id="evidence_path"
            data-testid="decisions-field-evidence_path"
            value={(formData.evidence_path as string) || ''}
            onChange={(e) => handleChange('evidence_path', e.target.value)}
            aria-invalid={!!errors.evidence_path}
            aria-describedby={errors.evidence_path ? 'evidence_path-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_path ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.evidence_path && (
            <div
              id="evidence_path-error"
              role="alert"
              data-testid="decisions-field-evidence_path-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_path}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes *
          </label>
          <textarea
            id="notes"
            data-testid="decisions-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.notes ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <div
              id="notes-error"
              role="alert"
              data-testid="decisions-field-notes-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="decisions-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="decisions-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="decisions-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="decisions-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

