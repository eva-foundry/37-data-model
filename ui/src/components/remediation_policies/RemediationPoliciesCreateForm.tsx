/**
 * RemediationPoliciesCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L48 (remediation_policies)
 * Generated: 2026-03-12T09:01:06Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new remediation_policies records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createRemediationPoliciesRecord } from '@api/remediation_policiesApi';
import type { RemediationPoliciesRecord, CreateRemediationPoliciesRecordInput } from '@/types/remediation_policies';

interface RemediationPoliciesCreateFormProps {
  onSuccess: (record: RemediationPoliciesRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const RemediationPoliciesCreateForm: React.FC<RemediationPoliciesCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('remediation_policies.create_form');

  const [formData, setFormData] = useState<Partial<CreateRemediationPoliciesRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createRemediationPoliciesRecord(formData as CreateRemediationPoliciesRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="remediation_policies-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="remediation_policies-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="policy_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Policy Id *
          </label>
          <input
            type="text"
            id="policy_id"
            data-testid="remediation_policies-field-policy_id"
            value={(formData.policy_id as string) || ''}
            onChange={(e) => handleChange('policy_id', e.target.value)}
            aria-invalid={!!errors.policy_id}
            aria-describedby={errors.policy_id ? 'policy_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.policy_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.policy_id && (
            <div
              id="policy_id-error"
              role="alert"
              data-testid="remediation_policies-field-policy_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.policy_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="policy_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Policy Name *
          </label>
          <input
            type="text"
            id="policy_name"
            data-testid="remediation_policies-field-policy_name"
            value={(formData.policy_name as string) || ''}
            onChange={(e) => handleChange('policy_name', e.target.value)}
            aria-invalid={!!errors.policy_name}
            aria-describedby={errors.policy_name ? 'policy_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.policy_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.policy_name && (
            <div
              id="policy_name-error"
              role="alert"
              data-testid="remediation_policies-field-policy_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.policy_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="scope"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Scope *
          </label>
          <input
            type="text"
            id="scope"
            data-testid="remediation_policies-field-scope"
            value={(formData.scope as string) || ''}
            onChange={(e) => handleChange('scope', e.target.value)}
            aria-invalid={!!errors.scope}
            aria-describedby={errors.scope ? 'scope-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.scope ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.scope && (
            <div
              id="scope-error"
              role="alert"
              data-testid="remediation_policies-field-scope-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.scope}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="triggers"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Triggers *
          </label>
          <textarea
            id="triggers"
            data-testid="remediation_policies-field-triggers"
            value={(formData.triggers as string) || ''}
            onChange={(e) => handleChange('triggers', e.target.value)}
            aria-invalid={!!errors.triggers}
            aria-describedby={errors.triggers ? 'triggers-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.triggers ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.triggers && (
            <div
              id="triggers-error"
              role="alert"
              data-testid="remediation_policies-field-triggers-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.triggers}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="remediation_actions"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Remediation Actions *
          </label>
          <textarea
            id="remediation_actions"
            data-testid="remediation_policies-field-remediation_actions"
            value={(formData.remediation_actions as string) || ''}
            onChange={(e) => handleChange('remediation_actions', e.target.value)}
            aria-invalid={!!errors.remediation_actions}
            aria-describedby={errors.remediation_actions ? 'remediation_actions-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.remediation_actions ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.remediation_actions && (
            <div
              id="remediation_actions-error"
              role="alert"
              data-testid="remediation_policies-field-remediation_actions-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.remediation_actions}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="approval_required"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Approval Required *
          </label>
          <input
            type="checkbox"
            id="approval_required"
            data-testid="remediation_policies-field-approval_required"
            checked={(formData.approval_required as boolean) || false}
            onChange={(e) => handleChange('approval_required', e.target.checked)}
            aria-invalid={!!errors.approval_required}
            aria-describedby={errors.approval_required ? 'approval_required-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.approval_required && (
            <div
              id="approval_required-error"
              role="alert"
              data-testid="remediation_policies-field-approval_required-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.approval_required}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="auto_execute"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Auto Execute *
          </label>
          <input
            type="checkbox"
            id="auto_execute"
            data-testid="remediation_policies-field-auto_execute"
            checked={(formData.auto_execute as boolean) || false}
            onChange={(e) => handleChange('auto_execute', e.target.checked)}
            aria-invalid={!!errors.auto_execute}
            aria-describedby={errors.auto_execute ? 'auto_execute-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.auto_execute && (
            <div
              id="auto_execute-error"
              role="alert"
              data-testid="remediation_policies-field-auto_execute-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.auto_execute}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="enabled"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Enabled *
          </label>
          <input
            type="checkbox"
            id="enabled"
            data-testid="remediation_policies-field-enabled"
            checked={(formData.enabled as boolean) || false}
            onChange={(e) => handleChange('enabled', e.target.checked)}
            aria-invalid={!!errors.enabled}
            aria-describedby={errors.enabled ? 'enabled-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.enabled && (
            <div
              id="enabled-error"
              role="alert"
              data-testid="remediation_policies-field-enabled-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.enabled}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="priority"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Priority *
          </label>
          <input
            type="text"
            id="priority"
            data-testid="remediation_policies-field-priority"
            value={(formData.priority as string) || ''}
            onChange={(e) => handleChange('priority', e.target.value)}
            aria-invalid={!!errors.priority}
            aria-describedby={errors.priority ? 'priority-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.priority ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.priority && (
            <div
              id="priority-error"
              role="alert"
              data-testid="remediation_policies-field-priority-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.priority}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="linked_policies"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Linked Policies *
          </label>
          <textarea
            id="linked_policies"
            data-testid="remediation_policies-field-linked_policies"
            value={(formData.linked_policies as string) || ''}
            onChange={(e) => handleChange('linked_policies', e.target.value)}
            aria-invalid={!!errors.linked_policies}
            aria-describedby={errors.linked_policies ? 'linked_policies-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.linked_policies ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.linked_policies && (
            <div
              id="linked_policies-error"
              role="alert"
              data-testid="remediation_policies-field-linked_policies-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.linked_policies}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="created"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Created *
          </label>
          <input
            type="datetime-local"
            id="created"
            data-testid="remediation_policies-field-created"
            value={(formData.created as string) || ''}
            onChange={(e) => handleChange('created', e.target.value)}
            aria-invalid={!!errors.created}
            aria-describedby={errors.created ? 'created-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.created ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.created && (
            <div
              id="created-error"
              role="alert"
              data-testid="remediation_policies-field-created-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.created}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="execution_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Execution Count *
          </label>
          <input
            type="number"
            id="execution_count"
            data-testid="remediation_policies-field-execution_count"
            value={(formData.execution_count as number) || 0}
            onChange={(e) => handleChange('execution_count', Number(e.target.value))}
            aria-invalid={!!errors.execution_count}
            aria-describedby={errors.execution_count ? 'execution_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.execution_count ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.execution_count && (
            <div
              id="execution_count-error"
              role="alert"
              data-testid="remediation_policies-field-execution_count-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.execution_count}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="success_rate"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Success Rate *
          </label>
          <input
            type="text"
            id="success_rate"
            data-testid="remediation_policies-field-success_rate"
            value={(formData.success_rate as string) || ''}
            onChange={(e) => handleChange('success_rate', e.target.value)}
            aria-invalid={!!errors.success_rate}
            aria-describedby={errors.success_rate ? 'success_rate-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.success_rate ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.success_rate && (
            <div
              id="success_rate-error"
              role="alert"
              data-testid="remediation_policies-field-success_rate-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.success_rate}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="remediation_policies-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="remediation_policies-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="remediation_policies-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="remediation_policies-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

