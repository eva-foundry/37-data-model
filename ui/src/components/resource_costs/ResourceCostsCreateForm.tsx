/**
 * ResourceCostsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L47 (resource_costs)
 * Generated: 2026-03-12T09:01:05Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new resource_costs records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createResourceCostsRecord } from '@api/resource_costsApi';
import type { ResourceCostsRecord, CreateResourceCostsRecordInput } from '@/types/resource_costs';

interface ResourceCostsCreateFormProps {
  onSuccess: (record: ResourceCostsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const ResourceCostsCreateForm: React.FC<ResourceCostsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('resource_costs.create_form');

  const [formData, setFormData] = useState<Partial<CreateResourceCostsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createResourceCostsRecord(formData as CreateResourceCostsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="resource_costs-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="resource_costs-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="metadata"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Metadata
          </label>
          <textarea
            id="metadata"
            data-testid="resource_costs-field-metadata"
            value={(formData.metadata as string) || ''}
            onChange={(e) => handleChange('metadata', e.target.value)}
            aria-invalid={!!errors.metadata}
            aria-describedby={errors.metadata ? 'metadata-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.metadata ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.metadata && (
            <div
              id="metadata-error"
              role="alert"
              data-testid="resource_costs-field-metadata-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.metadata}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="currency"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Currency
          </label>
          <input
            type="text"
            id="currency"
            data-testid="resource_costs-field-currency"
            value={(formData.currency as string) || ''}
            onChange={(e) => handleChange('currency', e.target.value)}
            aria-invalid={!!errors.currency}
            aria-describedby={errors.currency ? 'currency-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.currency ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.currency && (
            <div
              id="currency-error"
              role="alert"
              data-testid="resource_costs-field-currency-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.currency}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="budget"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Budget
          </label>
          <textarea
            id="budget"
            data-testid="resource_costs-field-budget"
            value={(formData.budget as string) || ''}
            onChange={(e) => handleChange('budget', e.target.value)}
            aria-invalid={!!errors.budget}
            aria-describedby={errors.budget ? 'budget-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.budget ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.budget && (
            <div
              id="budget-error"
              role="alert"
              data-testid="resource_costs-field-budget-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.budget}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="forecasted_cost"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Forecasted Cost
          </label>
          <textarea
            id="forecasted_cost"
            data-testid="resource_costs-field-forecasted_cost"
            value={(formData.forecasted_cost as string) || ''}
            onChange={(e) => handleChange('forecasted_cost', e.target.value)}
            aria-invalid={!!errors.forecasted_cost}
            aria-describedby={errors.forecasted_cost ? 'forecasted_cost-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.forecasted_cost ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.forecasted_cost && (
            <div
              id="forecasted_cost-error"
              role="alert"
              data-testid="resource_costs-field-forecasted_cost-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.forecasted_cost}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="total_cost"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Total Cost
          </label>
          <input
            type="number"
            id="total_cost"
            data-testid="resource_costs-field-total_cost"
            value={(formData.total_cost as number) || 0}
            onChange={(e) => handleChange('total_cost', Number(e.target.value))}
            aria-invalid={!!errors.total_cost}
            aria-describedby={errors.total_cost ? 'total_cost-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.total_cost ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.total_cost && (
            <div
              id="total_cost-error"
              role="alert"
              data-testid="resource_costs-field-total_cost-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.total_cost}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="optimization_opportunities"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Optimization Opportunities
          </label>
          <textarea
            id="optimization_opportunities"
            data-testid="resource_costs-field-optimization_opportunities"
            value={(formData.optimization_opportunities as string) || ''}
            onChange={(e) => handleChange('optimization_opportunities', e.target.value)}
            aria-invalid={!!errors.optimization_opportunities}
            aria-describedby={errors.optimization_opportunities ? 'optimization_opportunities-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.optimization_opportunities ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.optimization_opportunities && (
            <div
              id="optimization_opportunities-error"
              role="alert"
              data-testid="resource_costs-field-optimization_opportunities-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.optimization_opportunities}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_by_service"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost By Service
          </label>
          <textarea
            id="cost_by_service"
            data-testid="resource_costs-field-cost_by_service"
            value={(formData.cost_by_service as string) || ''}
            onChange={(e) => handleChange('cost_by_service', e.target.value)}
            aria-invalid={!!errors.cost_by_service}
            aria-describedby={errors.cost_by_service ? 'cost_by_service-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_by_service ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.cost_by_service && (
            <div
              id="cost_by_service-error"
              role="alert"
              data-testid="resource_costs-field-cost_by_service-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_by_service}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="subscription_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Subscription Id
          </label>
          <input
            type="text"
            id="subscription_id"
            data-testid="resource_costs-field-subscription_id"
            value={(formData.subscription_id as string) || ''}
            onChange={(e) => handleChange('subscription_id', e.target.value)}
            aria-invalid={!!errors.subscription_id}
            aria-describedby={errors.subscription_id ? 'subscription_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.subscription_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.subscription_id && (
            <div
              id="subscription_id-error"
              role="alert"
              data-testid="resource_costs-field-subscription_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.subscription_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="reporting_period"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Reporting Period
          </label>
          <input
            type="text"
            id="reporting_period"
            data-testid="resource_costs-field-reporting_period"
            value={(formData.reporting_period as string) || ''}
            onChange={(e) => handleChange('reporting_period', e.target.value)}
            aria-invalid={!!errors.reporting_period}
            aria-describedby={errors.reporting_period ? 'reporting_period-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.reporting_period ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.reporting_period && (
            <div
              id="reporting_period-error"
              role="alert"
              data-testid="resource_costs-field-reporting_period-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.reporting_period}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="resource_costs-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="resource_costs-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="resource_costs-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="resource_costs-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

