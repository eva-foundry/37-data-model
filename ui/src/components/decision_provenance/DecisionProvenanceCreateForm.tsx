/**
 * DecisionProvenanceCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L31 (decision_provenance)
 * Generated: 2026-03-12T09:00:55Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new decision_provenance records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createDecisionProvenanceRecord } from '@api/decision_provenanceApi';
import type { DecisionProvenanceRecord, CreateDecisionProvenanceRecordInput } from '@/types/decision_provenance';

interface DecisionProvenanceCreateFormProps {
  onSuccess: (record: DecisionProvenanceRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const DecisionProvenanceCreateForm: React.FC<DecisionProvenanceCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('decision_provenance.create_form');

  const [formData, setFormData] = useState<Partial<CreateDecisionProvenanceRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createDecisionProvenanceRecord(formData as CreateDecisionProvenanceRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="decision_provenance-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="decision_provenance-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="evidence_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Evidence Id *
          </label>
          <input
            type="text"
            id="evidence_id"
            data-testid="decision_provenance-field-evidence_id"
            value={(formData.evidence_id as string) || ''}
            onChange={(e) => handleChange('evidence_id', e.target.value)}
            aria-invalid={!!errors.evidence_id}
            aria-describedby={errors.evidence_id ? 'evidence_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.evidence_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.evidence_id && (
            <div
              id="evidence_id-error"
              role="alert"
              data-testid="decision_provenance-field-evidence_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.evidence_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="decision_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Decision Type *
          </label>
          <input
            type="text"
            id="decision_type"
            data-testid="decision_provenance-field-decision_type"
            value={(formData.decision_type as string) || ''}
            onChange={(e) => handleChange('decision_type', e.target.value)}
            aria-invalid={!!errors.decision_type}
            aria-describedby={errors.decision_type ? 'decision_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.decision_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.decision_type && (
            <div
              id="decision_type-error"
              role="alert"
              data-testid="decision_provenance-field-decision_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.decision_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="decision_maker"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Decision Maker *
          </label>
          <input
            type="text"
            id="decision_maker"
            data-testid="decision_provenance-field-decision_maker"
            value={(formData.decision_maker as string) || ''}
            onChange={(e) => handleChange('decision_maker', e.target.value)}
            aria-invalid={!!errors.decision_maker}
            aria-describedby={errors.decision_maker ? 'decision_maker-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.decision_maker ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.decision_maker && (
            <div
              id="decision_maker-error"
              role="alert"
              data-testid="decision_provenance-field-decision_maker-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.decision_maker}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="decision_timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Decision Timestamp *
          </label>
          <input
            type="datetime-local"
            id="decision_timestamp"
            data-testid="decision_provenance-field-decision_timestamp"
            value={(formData.decision_timestamp as string) || ''}
            onChange={(e) => handleChange('decision_timestamp', e.target.value)}
            aria-invalid={!!errors.decision_timestamp}
            aria-describedby={errors.decision_timestamp ? 'decision_timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.decision_timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.decision_timestamp && (
            <div
              id="decision_timestamp-error"
              role="alert"
              data-testid="decision_provenance-field-decision_timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.decision_timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="rationale"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Rationale *
          </label>
          <input
            type="text"
            id="rationale"
            data-testid="decision_provenance-field-rationale"
            value={(formData.rationale as string) || ''}
            onChange={(e) => handleChange('rationale', e.target.value)}
            aria-invalid={!!errors.rationale}
            aria-describedby={errors.rationale ? 'rationale-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.rationale ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.rationale && (
            <div
              id="rationale-error"
              role="alert"
              data-testid="decision_provenance-field-rationale-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.rationale}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="supporting_evidence"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Supporting Evidence *
          </label>
          <textarea
            id="supporting_evidence"
            data-testid="decision_provenance-field-supporting_evidence"
            value={(formData.supporting_evidence as string) || ''}
            onChange={(e) => handleChange('supporting_evidence', e.target.value)}
            aria-invalid={!!errors.supporting_evidence}
            aria-describedby={errors.supporting_evidence ? 'supporting_evidence-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.supporting_evidence ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.supporting_evidence && (
            <div
              id="supporting_evidence-error"
              role="alert"
              data-testid="decision_provenance-field-supporting_evidence-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.supporting_evidence}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="impact"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Impact *
          </label>
          <input
            type="text"
            id="impact"
            data-testid="decision_provenance-field-impact"
            value={(formData.impact as string) || ''}
            onChange={(e) => handleChange('impact', e.target.value)}
            aria-invalid={!!errors.impact}
            aria-describedby={errors.impact ? 'impact-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.impact ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.impact && (
            <div
              id="impact-error"
              role="alert"
              data-testid="decision_provenance-field-impact-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.impact}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="decision_provenance-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="decision_provenance-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="decision_provenance-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="decision_provenance-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

