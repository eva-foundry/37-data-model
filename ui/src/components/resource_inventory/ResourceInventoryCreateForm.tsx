/**
 * ResourceInventoryCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L42 (resource_inventory)
 * Generated: 2026-03-12T09:01:02Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new resource_inventory records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createResourceInventoryRecord } from '@api/resource_inventoryApi';
import type { ResourceInventoryRecord, CreateResourceInventoryRecordInput } from '@/types/resource_inventory';

interface ResourceInventoryCreateFormProps {
  onSuccess: (record: ResourceInventoryRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const ResourceInventoryCreateForm: React.FC<ResourceInventoryCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('resource_inventory.create_form');

  const [formData, setFormData] = useState<Partial<CreateResourceInventoryRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createResourceInventoryRecord(formData as CreateResourceInventoryRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="resource_inventory-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="resource_inventory-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="subscription_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Subscription Id *
          </label>
          <input
            type="text"
            id="subscription_id"
            data-testid="resource_inventory-field-subscription_id"
            value={(formData.subscription_id as string) || ''}
            onChange={(e) => handleChange('subscription_id', e.target.value)}
            aria-invalid={!!errors.subscription_id}
            aria-describedby={errors.subscription_id ? 'subscription_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.subscription_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.subscription_id && (
            <div
              id="subscription_id-error"
              role="alert"
              data-testid="resource_inventory-field-subscription_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.subscription_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_group"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Group *
          </label>
          <input
            type="text"
            id="resource_group"
            data-testid="resource_inventory-field-resource_group"
            value={(formData.resource_group as string) || ''}
            onChange={(e) => handleChange('resource_group', e.target.value)}
            aria-invalid={!!errors.resource_group}
            aria-describedby={errors.resource_group ? 'resource_group-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_group ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_group && (
            <div
              id="resource_group-error"
              role="alert"
              data-testid="resource_inventory-field-resource_group-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_group}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Name *
          </label>
          <input
            type="text"
            id="resource_name"
            data-testid="resource_inventory-field-resource_name"
            value={(formData.resource_name as string) || ''}
            onChange={(e) => handleChange('resource_name', e.target.value)}
            aria-invalid={!!errors.resource_name}
            aria-describedby={errors.resource_name ? 'resource_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_name && (
            <div
              id="resource_name-error"
              role="alert"
              data-testid="resource_inventory-field-resource_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Type *
          </label>
          <input
            type="text"
            id="resource_type"
            data-testid="resource_inventory-field-resource_type"
            value={(formData.resource_type as string) || ''}
            onChange={(e) => handleChange('resource_type', e.target.value)}
            aria-invalid={!!errors.resource_type}
            aria-describedby={errors.resource_type ? 'resource_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_type && (
            <div
              id="resource_type-error"
              role="alert"
              data-testid="resource_inventory-field-resource_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="region"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Region *
          </label>
          <input
            type="text"
            id="region"
            data-testid="resource_inventory-field-region"
            value={(formData.region as string) || ''}
            onChange={(e) => handleChange('region', e.target.value)}
            aria-invalid={!!errors.region}
            aria-describedby={errors.region ? 'region-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.region ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.region && (
            <div
              id="region-error"
              role="alert"
              data-testid="resource_inventory-field-region-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.region}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="resource_inventory-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <div
              id="status-error"
              role="alert"
              data-testid="resource_inventory-field-status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="created_date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Created Date *
          </label>
          <input
            type="datetime-local"
            id="created_date"
            data-testid="resource_inventory-field-created_date"
            value={(formData.created_date as string) || ''}
            onChange={(e) => handleChange('created_date', e.target.value)}
            aria-invalid={!!errors.created_date}
            aria-describedby={errors.created_date ? 'created_date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.created_date ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.created_date && (
            <div
              id="created_date-error"
              role="alert"
              data-testid="resource_inventory-field-created_date-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.created_date}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="updated_date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Updated Date *
          </label>
          <input
            type="datetime-local"
            id="updated_date"
            data-testid="resource_inventory-field-updated_date"
            value={(formData.updated_date as string) || ''}
            onChange={(e) => handleChange('updated_date', e.target.value)}
            aria-invalid={!!errors.updated_date}
            aria-describedby={errors.updated_date ? 'updated_date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.updated_date ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.updated_date && (
            <div
              id="updated_date-error"
              role="alert"
              data-testid="resource_inventory-field-updated_date-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.updated_date}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="tags"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Tags *
          </label>
          <textarea
            id="tags"
            data-testid="resource_inventory-field-tags"
            value={(formData.tags as string) || ''}
            onChange={(e) => handleChange('tags', e.target.value)}
            aria-invalid={!!errors.tags}
            aria-describedby={errors.tags ? 'tags-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.tags ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.tags && (
            <div
              id="tags-error"
              role="alert"
              data-testid="resource_inventory-field-tags-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.tags}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="configuration"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Configuration *
          </label>
          <textarea
            id="configuration"
            data-testid="resource_inventory-field-configuration"
            value={(formData.configuration as string) || ''}
            onChange={(e) => handleChange('configuration', e.target.value)}
            aria-invalid={!!errors.configuration}
            aria-describedby={errors.configuration ? 'configuration-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.configuration ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.configuration && (
            <div
              id="configuration-error"
              role="alert"
              data-testid="resource_inventory-field-configuration-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.configuration}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="security_config"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Security Config *
          </label>
          <textarea
            id="security_config"
            data-testid="resource_inventory-field-security_config"
            value={(formData.security_config as string) || ''}
            onChange={(e) => handleChange('security_config', e.target.value)}
            aria-invalid={!!errors.security_config}
            aria-describedby={errors.security_config ? 'security_config-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.security_config ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.security_config && (
            <div
              id="security_config-error"
              role="alert"
              data-testid="resource_inventory-field-security_config-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.security_config}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="cost_tracking"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Cost Tracking *
          </label>
          <textarea
            id="cost_tracking"
            data-testid="resource_inventory-field-cost_tracking"
            value={(formData.cost_tracking as string) || ''}
            onChange={(e) => handleChange('cost_tracking', e.target.value)}
            aria-invalid={!!errors.cost_tracking}
            aria-describedby={errors.cost_tracking ? 'cost_tracking-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.cost_tracking ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.cost_tracking && (
            <div
              id="cost_tracking-error"
              role="alert"
              data-testid="resource_inventory-field-cost_tracking-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.cost_tracking}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="resource_inventory-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="resource_inventory-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="resource_inventory-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="resource_inventory-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

