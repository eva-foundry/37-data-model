/**
 * InfrastructureEventsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L45 (infrastructure_events)
 * Generated: 2026-03-12T09:01:04Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new infrastructure_events records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createInfrastructureEventsRecord } from '@api/infrastructure_eventsApi';
import type { InfrastructureEventsRecord, CreateInfrastructureEventsRecordInput } from '@/types/infrastructure_events';

interface InfrastructureEventsCreateFormProps {
  onSuccess: (record: InfrastructureEventsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const InfrastructureEventsCreateForm: React.FC<InfrastructureEventsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('infrastructure_events.create_form');

  const [formData, setFormData] = useState<Partial<CreateInfrastructureEventsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createInfrastructureEventsRecord(formData as CreateInfrastructureEventsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="infrastructure_events-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="infrastructure_events-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="event_type"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Event Type *
          </label>
          <input
            type="text"
            id="event_type"
            data-testid="infrastructure_events-field-event_type"
            value={(formData.event_type as string) || ''}
            onChange={(e) => handleChange('event_type', e.target.value)}
            aria-invalid={!!errors.event_type}
            aria-describedby={errors.event_type ? 'event_type-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.event_type ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.event_type && (
            <div
              id="event_type-error"
              role="alert"
              data-testid="infrastructure_events-field-event_type-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.event_type}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="timestamp"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Timestamp *
          </label>
          <input
            type="datetime-local"
            id="timestamp"
            data-testid="infrastructure_events-field-timestamp"
            value={(formData.timestamp as string) || ''}
            onChange={(e) => handleChange('timestamp', e.target.value)}
            aria-invalid={!!errors.timestamp}
            aria-describedby={errors.timestamp ? 'timestamp-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.timestamp ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.timestamp && (
            <div
              id="timestamp-error"
              role="alert"
              data-testid="infrastructure_events-field-timestamp-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.timestamp}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="severity"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Severity *
          </label>
          <input
            type="text"
            id="severity"
            data-testid="infrastructure_events-field-severity"
            value={(formData.severity as string) || ''}
            onChange={(e) => handleChange('severity', e.target.value)}
            aria-invalid={!!errors.severity}
            aria-describedby={errors.severity ? 'severity-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.severity ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.severity && (
            <div
              id="severity-error"
              role="alert"
              data-testid="infrastructure_events-field-severity-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.severity}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="service_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Service Id *
          </label>
          <input
            type="text"
            id="service_id"
            data-testid="infrastructure_events-field-service_id"
            value={(formData.service_id as string) || ''}
            onChange={(e) => handleChange('service_id', e.target.value)}
            aria-invalid={!!errors.service_id}
            aria-describedby={errors.service_id ? 'service_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.service_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.service_id && (
            <div
              id="service_id-error"
              role="alert"
              data-testid="infrastructure_events-field-service_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.service_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resource_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resource Name *
          </label>
          <input
            type="text"
            id="resource_name"
            data-testid="infrastructure_events-field-resource_name"
            value={(formData.resource_name as string) || ''}
            onChange={(e) => handleChange('resource_name', e.target.value)}
            aria-invalid={!!errors.resource_name}
            aria-describedby={errors.resource_name ? 'resource_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resource_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resource_name && (
            <div
              id="resource_name-error"
              role="alert"
              data-testid="infrastructure_events-field-resource_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resource_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="event_summary"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Event Summary *
          </label>
          <input
            type="text"
            id="event_summary"
            data-testid="infrastructure_events-field-event_summary"
            value={(formData.event_summary as string) || ''}
            onChange={(e) => handleChange('event_summary', e.target.value)}
            aria-invalid={!!errors.event_summary}
            aria-describedby={errors.event_summary ? 'event_summary-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.event_summary ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.event_summary && (
            <div
              id="event_summary-error"
              role="alert"
              data-testid="infrastructure_events-field-event_summary-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.event_summary}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="event_details"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Event Details *
          </label>
          <textarea
            id="event_details"
            data-testid="infrastructure_events-field-event_details"
            value={(formData.event_details as string) || ''}
            onChange={(e) => handleChange('event_details', e.target.value)}
            aria-invalid={!!errors.event_details}
            aria-describedby={errors.event_details ? 'event_details-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.event_details ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.event_details && (
            <div
              id="event_details-error"
              role="alert"
              data-testid="infrastructure_events-field-event_details-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.event_details}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="initiated_by"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Initiated By *
          </label>
          <input
            type="text"
            id="initiated_by"
            data-testid="infrastructure_events-field-initiated_by"
            value={(formData.initiated_by as string) || ''}
            onChange={(e) => handleChange('initiated_by', e.target.value)}
            aria-invalid={!!errors.initiated_by}
            aria-describedby={errors.initiated_by ? 'initiated_by-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.initiated_by ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.initiated_by && (
            <div
              id="initiated_by-error"
              role="alert"
              data-testid="infrastructure_events-field-initiated_by-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.initiated_by}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="correlation_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Correlation Id *
          </label>
          <input
            type="text"
            id="correlation_id"
            data-testid="infrastructure_events-field-correlation_id"
            value={(formData.correlation_id as string) || ''}
            onChange={(e) => handleChange('correlation_id', e.target.value)}
            aria-invalid={!!errors.correlation_id}
            aria-describedby={errors.correlation_id ? 'correlation_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.correlation_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.correlation_id && (
            <div
              id="correlation_id-error"
              role="alert"
              data-testid="infrastructure_events-field-correlation_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.correlation_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="resolution_status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Resolution Status *
          </label>
          <input
            type="text"
            id="resolution_status"
            data-testid="infrastructure_events-field-resolution_status"
            value={(formData.resolution_status as string) || ''}
            onChange={(e) => handleChange('resolution_status', e.target.value)}
            aria-invalid={!!errors.resolution_status}
            aria-describedby={errors.resolution_status ? 'resolution_status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.resolution_status ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.resolution_status && (
            <div
              id="resolution_status-error"
              role="alert"
              data-testid="infrastructure_events-field-resolution_status-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.resolution_status}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="impact"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Impact *
          </label>
          <input
            type="text"
            id="impact"
            data-testid="infrastructure_events-field-impact"
            value={(formData.impact as string) || ''}
            onChange={(e) => handleChange('impact', e.target.value)}
            aria-invalid={!!errors.impact}
            aria-describedby={errors.impact ? 'impact-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.impact ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.impact && (
            <div
              id="impact-error"
              role="alert"
              data-testid="infrastructure_events-field-impact-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.impact}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="infrastructure_events-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="infrastructure_events-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="infrastructure_events-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="infrastructure_events-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

