/**
 * SprintsEditForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L27 (sprints)
 * Generated: 2026-03-11T01:43:49Z
 * Generator: screens-machine-v2.0.0
 * 
 * Form for editing existing sprints records with validation and error handling.
 * 
 * Features:
 * - Pre-populated with existing values
 * - Client-side validation
 * - Field-level error messages
 * - Update/Cancel actions
 * - Accessible form markup
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState, useEffect } from 'react';
import { useLang } from '@context/LangContext';
import { updateSprintsRecord } from '@api/sprintsApi';
import type { SprintsRecord, UpdateSprintsRecordInput } from '@/types/sprints';

interface SprintsEditFormProps {
  record: SprintsRecord;
  onSuccess: (record: SprintsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const SprintsEditForm: React.FC<SprintsEditFormProps> = ({ record, onSuccess, onCancel }) => {
  const { lang } = useLang();

  const t = {
    title:       lang === 'fr' ? 'Modifier l\\'enregistrement' : 'Edit record',
    submit:      lang === 'fr' ? 'Enregistrer' : 'Save',
    cancel:      lang === 'fr' ? 'Annuler' : 'Cancel',
    required:    lang === 'fr' ? 'Champ obligatoire' : 'This field is required',
    submitting:  lang === 'fr' ? 'Mise à jour en cours...' : 'Updating...',
    error:       lang === 'fr' ? 'Erreur lors de la mise à jour' : 'Failed to update record',
    noChanges:   lang === 'fr' ? 'Aucune modification détectée' : 'No changes detected',
  };

  const [formData, setFormData] = useState<Partial<UpdateSprintsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  // Initialize form data with existing record values
  useEffect(() => {
    const initial: Partial<UpdateSprintsRecordInput> = {};
        initial.id = record.id;
    initial.label = record.label;
    initial.project_id = record.project_id;
    initial.start_date = record.start_date;
    initial.end_date = record.end_date;
    initial.status = record.status;
    initial.goal = record.goal;
    initial.velocity_planned = record.velocity_planned;
    initial.velocity_actual = record.velocity_actual;
    initial.story_count = record.story_count;
    initial.stories_completed = record.stories_completed;
    initial.ado_iteration_path = record.ado_iteration_path;
    initial.mti_at_close = record.mti_at_close;
    initial.notes = record.notes;
    setFormData(initial);
  }, [record]);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
        if (!formData.id) {
      newErrors.id = t.required;
    }
    if (!formData.label) {
      newErrors.label = t.required;
    }
    if (!formData.status) {
      newErrors.status = t.required;
    }
    if (!formData.goal) {
      newErrors.goal = t.required;
    }

    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const hasChanges = (): boolean => {
        initial.id = record.id;
    initial.label = record.label;
    initial.project_id = record.project_id;
    initial.start_date = record.start_date;
    initial.end_date = record.end_date;
    initial.status = record.status;
    initial.goal = record.goal;
    initial.velocity_planned = record.velocity_planned;
    initial.velocity_actual = record.velocity_actual;
    initial.story_count = record.story_count;
    initial.stories_completed = record.stories_completed;
    initial.ado_iteration_path = record.ado_iteration_path;
    initial.mti_at_close = record.mti_at_close;
    initial.notes = record.notes;
    return false;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!hasChanges()) {
      setSubmitError(t.noChanges);
      return;
    }

    if (!validate()) return;

    setSubmitting(true);
    try {
      const updated = await updateSprintsRecord(
        record.id,
        formData as UpdateSprintsRecordInput
      );
      onSuccess(updated);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="sprints-edit-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <div style={{ marginBottom: 24 }}>
        <h2 style={{ margin: '0 0 4px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
          {t.title}
        </h2>
        <p style={{ margin: 0, fontSize: '0.75rem', color: GC_MUTED }}>
          L27: {record.id}
        </p>
      </div>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="sprints-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t.error}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
              <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
        <div>
          <label
            htmlFor="id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Id *
          </label>
          <input
            type="text"
            id="id"
            data-testid="sprints-field-id"
            value={(formData.id as string) || ''}
            onChange={(e) => handleChange('id', e.target.value)}
            aria-invalid={!!errors.id}
            aria-describedby={errors.id ? 'id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.id ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.id && (
            <p
              id="id-error"
              data-testid="sprints-error-id"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.id}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="label"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Label *
          </label>
          <input
            type="text"
            id="label"
            data-testid="sprints-field-label"
            value={(formData.label as string) || ''}
            onChange={(e) => handleChange('label', e.target.value)}
            aria-invalid={!!errors.label}
            aria-describedby={errors.label ? 'label-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.label ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.label && (
            <p
              id="label-error"
              data-testid="sprints-error-label"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.label}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="project_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Project Id
          </label>
          <input
            type="text"
            id="project_id"
            data-testid="sprints-field-project_id"
            value={(formData.project_id as string) || ''}
            onChange={(e) => handleChange('project_id', e.target.value)}
            aria-invalid={!!errors.project_id}
            aria-describedby={errors.project_id ? 'project_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.project_id ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.project_id && (
            <p
              id="project_id-error"
              data-testid="sprints-error-project_id"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.project_id}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="start_date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Start Date
          </label>
          <input
            type="text"
            id="start_date"
            data-testid="sprints-field-start_date"
            value={(formData.start_date as string) || ''}
            onChange={(e) => handleChange('start_date', e.target.value)}
            aria-invalid={!!errors.start_date}
            aria-describedby={errors.start_date ? 'start_date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.start_date ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.start_date && (
            <p
              id="start_date-error"
              data-testid="sprints-error-start_date"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.start_date}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="end_date"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            End Date
          </label>
          <input
            type="text"
            id="end_date"
            data-testid="sprints-field-end_date"
            value={(formData.end_date as string) || ''}
            onChange={(e) => handleChange('end_date', e.target.value)}
            aria-invalid={!!errors.end_date}
            aria-describedby={errors.end_date ? 'end_date-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.end_date ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.end_date && (
            <p
              id="end_date-error"
              data-testid="sprints-error-end_date"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.end_date}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="status"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Status *
          </label>
          <input
            type="text"
            id="status"
            data-testid="sprints-field-status"
            value={(formData.status as string) || ''}
            onChange={(e) => handleChange('status', e.target.value)}
            aria-invalid={!!errors.status}
            aria-describedby={errors.status ? 'status-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.status ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.status && (
            <p
              id="status-error"
              data-testid="sprints-error-status"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.status}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="goal"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Goal *
          </label>
          <textarea
            id="goal"
            data-testid="sprints-field-goal"
            value={(formData.goal as string) || ''}
            onChange={(e) => handleChange('goal', e.target.value)}
            aria-invalid={!!errors.goal}
            aria-describedby={errors.goal ? 'goal-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.goal ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.goal && (
            <p
              id="goal-error"
              data-testid="sprints-error-goal"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.goal}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="velocity_planned"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Velocity Planned
          </label>
          <input
            type="text"
            id="velocity_planned"
            data-testid="sprints-field-velocity_planned"
            value={(formData.velocity_planned as string) || ''}
            onChange={(e) => handleChange('velocity_planned', e.target.value)}
            aria-invalid={!!errors.velocity_planned}
            aria-describedby={errors.velocity_planned ? 'velocity_planned-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.velocity_planned ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.velocity_planned && (
            <p
              id="velocity_planned-error"
              data-testid="sprints-error-velocity_planned"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.velocity_planned}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="velocity_actual"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Velocity Actual
          </label>
          <input
            type="text"
            id="velocity_actual"
            data-testid="sprints-field-velocity_actual"
            value={(formData.velocity_actual as string) || ''}
            onChange={(e) => handleChange('velocity_actual', e.target.value)}
            aria-invalid={!!errors.velocity_actual}
            aria-describedby={errors.velocity_actual ? 'velocity_actual-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.velocity_actual ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.velocity_actual && (
            <p
              id="velocity_actual-error"
              data-testid="sprints-error-velocity_actual"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.velocity_actual}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="story_count"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Story Count
          </label>
          <input
            type="text"
            id="story_count"
            data-testid="sprints-field-story_count"
            value={(formData.story_count as string) || ''}
            onChange={(e) => handleChange('story_count', e.target.value)}
            aria-invalid={!!errors.story_count}
            aria-describedby={errors.story_count ? 'story_count-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.story_count ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.story_count && (
            <p
              id="story_count-error"
              data-testid="sprints-error-story_count"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.story_count}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="stories_completed"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Stories Completed
          </label>
          <input
            type="text"
            id="stories_completed"
            data-testid="sprints-field-stories_completed"
            value={(formData.stories_completed as string) || ''}
            onChange={(e) => handleChange('stories_completed', e.target.value)}
            aria-invalid={!!errors.stories_completed}
            aria-describedby={errors.stories_completed ? 'stories_completed-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.stories_completed ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.stories_completed && (
            <p
              id="stories_completed-error"
              data-testid="sprints-error-stories_completed"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.stories_completed}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="ado_iteration_path"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Ado Iteration Path
          </label>
          <input
            type="text"
            id="ado_iteration_path"
            data-testid="sprints-field-ado_iteration_path"
            value={(formData.ado_iteration_path as string) || ''}
            onChange={(e) => handleChange('ado_iteration_path', e.target.value)}
            aria-invalid={!!errors.ado_iteration_path}
            aria-describedby={errors.ado_iteration_path ? 'ado_iteration_path-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.ado_iteration_path ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.ado_iteration_path && (
            <p
              id="ado_iteration_path-error"
              data-testid="sprints-error-ado_iteration_path"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.ado_iteration_path}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="mti_at_close"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Mti At Close
          </label>
          <input
            type="text"
            id="mti_at_close"
            data-testid="sprints-field-mti_at_close"
            value={(formData.mti_at_close as string) || ''}
            onChange={(e) => handleChange('mti_at_close', e.target.value)}
            aria-invalid={!!errors.mti_at_close}
            aria-describedby={errors.mti_at_close ? 'mti_at_close-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.mti_at_close ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.mti_at_close && (
            <p
              id="mti_at_close-error"
              data-testid="sprints-error-mti_at_close"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.mti_at_close}
            </p>
          )}
        </div>
        <div>
          <label
            htmlFor="notes"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Notes
          </label>
          <textarea
            id="notes"
            data-testid="sprints-field-notes"
            value={(formData.notes as string) || ''}
            onChange={(e) => handleChange('notes', e.target.value)}
            aria-invalid={!!errors.notes}
            aria-describedby={errors.notes ? 'notes-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: 1px solid ${errors.notes ? GC_ERROR : GC_BORDER},
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.notes && (
            <p
              id="notes-error"
              data-testid="sprints-error-notes"
              role="alert"
              style={{
                margin: '4px 0 0',
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.notes}
            </p>
          )}
        </div>
      </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="sprints-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t.cancel}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="sprints-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t.submitting : t.submit}
        </button>
      </div>
    </form>
  );
};

