/**
 * PerformanceTrendsCreateForm -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L87 (performance_trends)
 * Generated: 2026-03-12T09:01:33Z
 * Generator: screens-machine-v1.0.0
 * 
 * Form for creating new performance_trends records with validation and error handling.
 * 
 * Features:
 * - Client-side validation
 * - Field-level error messages
 * - Submit/Cancel actions
 * - Accessible form markup (labels, aria-describedby)
 * - WCAG 2.1 Level AA compliant
 */

import React, { useState } from 'react';
import { useLiterals } from '@hooks/useLiterals';
import { createPerformanceTrendsRecord } from '@api/performance_trendsApi';
import type { PerformanceTrendsRecord, CreatePerformanceTrendsRecordInput } from '@/types/performance_trends';

interface PerformanceTrendsCreateFormProps {
  onSuccess: (record: PerformanceTrendsRecord) => void;
  onCancel: () => void;
}

// GC Design System colors
const GC_TEXT    = '#0b0c0e';
const GC_MUTED   = '#505a5f';
const GC_BORDER  = '#b1b4b6';
const GC_SURFACE = '#f8f8f8';
const GC_BLUE    = '#1d70b8';
const GC_ERROR   = '#d4351c';
const GC_SUCCESS = '#00703c';

export const PerformanceTrendsCreateForm: React.FC<PerformanceTrendsCreateFormProps> = ({ onSuccess, onCancel }) => {
  const t = useLiterals('performance_trends.create_form');

  const [formData, setFormData] = useState<Partial<CreatePerformanceTrendsRecordInput>>({});
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState<string | null>(null);

  const validate = (): boolean => {
    const newErrors: Record<string, string> = {};
    
    // TODO: Add validation rules based on schema
    
    setErrors(newErrors);
    return Object.keys(newErrors).length === 0;
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setSubmitError(null);

    if (!validate()) return;

    setSubmitting(true);
    try {
      const record = await createPerformanceTrendsRecord(formData as CreatePerformanceTrendsRecordInput);
      onSuccess(record);
    } catch (error) {
      setSubmitError(error instanceof Error ? error.message : 'Unknown error');
    } finally {
      setSubmitting(false);
    }
  };

  const handleChange = (field: string, value: any) => {
    setFormData((prev) => ({ ...prev, [field]: value }));
    // Clear error for this field
    if (errors[field]) {
      setErrors((prev) => {
        const next = { ...prev };
        delete next[field];
        return next;
      });
    }
  };

  return (
    <form
      onSubmit={handleSubmit}
      data-testid="performance_trends-create-form"
      style={{
        maxWidth: 600,
        margin: '0 auto',
        padding: '24px',
        background: '#fff',
        border: `1px solid ${GC_BORDER}`,
        borderRadius: 4,
        fontFamily: 'Noto Sans, sans-serif',
      }}
    >
      {/* Form header */}
      <h2 style={{ margin: '0 0 24px', fontSize: '1.25rem', fontWeight: 700, color: GC_TEXT }}>
        {t('title')}
      </h2>

      {/* Submit error banner */}
      {submitError && (
        <div
          role="alert"
          data-testid="performance_trends-submit-error"
          style={{
            padding: '12px 16px',
            marginBottom: 20,
            background: '#fef7f7',
            border: `1px solid ${GC_ERROR}`,
            borderRadius: 4,
            color: GC_ERROR,
            fontSize: '0.875rem',
          }}
        >
          <strong>{t('messages.error')}:</strong> {submitError}
        </div>
      )}

      {/* Form fields (auto-generated from schema) */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 20 }}>
                <div>
          <label
            htmlFor="agent_id"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Id *
          </label>
          <input
            type="text"
            id="agent_id"
            data-testid="performance_trends-field-agent_id"
            value={(formData.agent_id as string) || ''}
            onChange={(e) => handleChange('agent_id', e.target.value)}
            aria-invalid={!!errors.agent_id}
            aria-describedby={errors.agent_id ? 'agent_id-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_id ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_id && (
            <div
              id="agent_id-error"
              role="alert"
              data-testid="performance_trends-field-agent_id-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_id}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="agent_name"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Agent Name *
          </label>
          <input
            type="text"
            id="agent_name"
            data-testid="performance_trends-field-agent_name"
            value={(formData.agent_name as string) || ''}
            onChange={(e) => handleChange('agent_name', e.target.value)}
            aria-invalid={!!errors.agent_name}
            aria-describedby={errors.agent_name ? 'agent_name-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.agent_name ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.agent_name && (
            <div
              id="agent_name-error"
              role="alert"
              data-testid="performance_trends-field-agent_name-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.agent_name}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="metric_period"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Metric Period *
          </label>
          <input
            type="text"
            id="metric_period"
            data-testid="performance_trends-field-metric_period"
            value={(formData.metric_period as string) || ''}
            onChange={(e) => handleChange('metric_period', e.target.value)}
            aria-invalid={!!errors.metric_period}
            aria-describedby={errors.metric_period ? 'metric_period-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.metric_period ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.metric_period && (
            <div
              id="metric_period-error"
              role="alert"
              data-testid="performance_trends-field-metric_period-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.metric_period}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="period_start"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Period Start *
          </label>
          <input
            type="text"
            id="period_start"
            data-testid="performance_trends-field-period_start"
            value={(formData.period_start as string) || ''}
            onChange={(e) => handleChange('period_start', e.target.value)}
            aria-invalid={!!errors.period_start}
            aria-describedby={errors.period_start ? 'period_start-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.period_start ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.period_start && (
            <div
              id="period_start-error"
              role="alert"
              data-testid="performance_trends-field-period_start-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.period_start}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="period_end"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Period End *
          </label>
          <input
            type="text"
            id="period_end"
            data-testid="performance_trends-field-period_end"
            value={(formData.period_end as string) || ''}
            onChange={(e) => handleChange('period_end', e.target.value)}
            aria-invalid={!!errors.period_end}
            aria-describedby={errors.period_end ? 'period_end-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.period_end ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.period_end && (
            <div
              id="period_end-error"
              role="alert"
              data-testid="performance_trends-field-period_end-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.period_end}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="days_in_period"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Days In Period *
          </label>
          <input
            type="number"
            id="days_in_period"
            data-testid="performance_trends-field-days_in_period"
            value={(formData.days_in_period as number) || 0}
            onChange={(e) => handleChange('days_in_period', Number(e.target.value))}
            aria-invalid={!!errors.days_in_period}
            aria-describedby={errors.days_in_period ? 'days_in_period-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.days_in_period ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.days_in_period && (
            <div
              id="days_in_period-error"
              role="alert"
              data-testid="performance_trends-field-days_in_period-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.days_in_period}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="metrics_snapshot"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Metrics Snapshot *
          </label>
          <textarea
            id="metrics_snapshot"
            data-testid="performance_trends-field-metrics_snapshot"
            value={(formData.metrics_snapshot as string) || ''}
            onChange={(e) => handleChange('metrics_snapshot', e.target.value)}
            aria-invalid={!!errors.metrics_snapshot}
            aria-describedby={errors.metrics_snapshot ? 'metrics_snapshot-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.metrics_snapshot ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.metrics_snapshot && (
            <div
              id="metrics_snapshot-error"
              role="alert"
              data-testid="performance_trends-field-metrics_snapshot-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.metrics_snapshot}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="trend_indicators"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Trend Indicators *
          </label>
          <textarea
            id="trend_indicators"
            data-testid="performance_trends-field-trend_indicators"
            value={(formData.trend_indicators as string) || ''}
            onChange={(e) => handleChange('trend_indicators', e.target.value)}
            aria-invalid={!!errors.trend_indicators}
            aria-describedby={errors.trend_indicators ? 'trend_indicators-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.trend_indicators ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.trend_indicators && (
            <div
              id="trend_indicators-error"
              role="alert"
              data-testid="performance_trends-field-trend_indicators-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.trend_indicators}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="daily_breakdown"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Daily Breakdown *
          </label>
          <textarea
            id="daily_breakdown"
            data-testid="performance_trends-field-daily_breakdown"
            value={(formData.daily_breakdown as string) || ''}
            onChange={(e) => handleChange('daily_breakdown', e.target.value)}
            aria-invalid={!!errors.daily_breakdown}
            aria-describedby={errors.daily_breakdown ? 'daily_breakdown-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.daily_breakdown ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.daily_breakdown && (
            <div
              id="daily_breakdown-error"
              role="alert"
              data-testid="performance_trends-field-daily_breakdown-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.daily_breakdown}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="peer_comparison"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Peer Comparison *
          </label>
          <textarea
            id="peer_comparison"
            data-testid="performance_trends-field-peer_comparison"
            value={(formData.peer_comparison as string) || ''}
            onChange={(e) => handleChange('peer_comparison', e.target.value)}
            aria-invalid={!!errors.peer_comparison}
            aria-describedby={errors.peer_comparison ? 'peer_comparison-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.peer_comparison ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.peer_comparison && (
            <div
              id="peer_comparison-error"
              role="alert"
              data-testid="performance_trends-field-peer_comparison-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.peer_comparison}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="performance_insights"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Performance Insights *
          </label>
          <textarea
            id="performance_insights"
            data-testid="performance_trends-field-performance_insights"
            value={(formData.performance_insights as string) || ''}
            onChange={(e) => handleChange('performance_insights', e.target.value)}
            aria-invalid={!!errors.performance_insights}
            aria-describedby={errors.performance_insights ? 'performance_insights-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.performance_insights ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.performance_insights && (
            <div
              id="performance_insights-error"
              role="alert"
              data-testid="performance_trends-field-performance_insights-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.performance_insights}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="actions_recommended"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Actions Recommended *
          </label>
          <textarea
            id="actions_recommended"
            data-testid="performance_trends-field-actions_recommended"
            value={(formData.actions_recommended as string) || ''}
            onChange={(e) => handleChange('actions_recommended', e.target.value)}
            aria-invalid={!!errors.actions_recommended}
            aria-describedby={errors.actions_recommended ? 'actions_recommended-error' : undefined}
            disabled={submitting}
            rows={4}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.actions_recommended ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
              resize: 'vertical',
            }}
          />
          {errors.actions_recommended && (
            <div
              id="actions_recommended-error"
              role="alert"
              data-testid="performance_trends-field-actions_recommended-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.actions_recommended}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="calculated_at"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Calculated At *
          </label>
          <input
            type="datetime-local"
            id="calculated_at"
            data-testid="performance_trends-field-calculated_at"
            value={(formData.calculated_at as string) || ''}
            onChange={(e) => handleChange('calculated_at', e.target.value)}
            aria-invalid={!!errors.calculated_at}
            aria-describedby={errors.calculated_at ? 'calculated_at-error' : undefined}
            disabled={submitting}
            style={{
              width: '100%',
              padding: '8px 12px',
              border: `1px solid ${errors.calculated_at ? GC_ERROR : GC_BORDER}`,
              borderRadius: 4,
              fontSize: '0.875rem',
              fontFamily: 'inherit',
            }}
          />
          {errors.calculated_at && (
            <div
              id="calculated_at-error"
              role="alert"
              data-testid="performance_trends-field-calculated_at-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.calculated_at}
            </div>
          )}
        </div>

        <div>
          <label
            htmlFor="is_active"
            style={{
              display: 'block',
              marginBottom: 4,
              fontSize: '0.875rem',
              fontWeight: 600,
              color: GC_TEXT,
            }}
          >
            Is Active *
          </label>
          <input
            type="checkbox"
            id="is_active"
            data-testid="performance_trends-field-is_active"
            checked={(formData.is_active as boolean) || false}
            onChange={(e) => handleChange('is_active', e.target.checked)}
            aria-invalid={!!errors.is_active}
            aria-describedby={errors.is_active ? 'is_active-error' : undefined}
            disabled={submitting}
            style={{
              width: '18px',
              height: '18px',
              cursor: 'pointer',
            }}
          />
          {errors.is_active && (
            <div
              id="is_active-error"
              role="alert"
              data-testid="performance_trends-field-is_active-error"
              style={{
                marginTop: 4,
                fontSize: '0.75rem',
                color: GC_ERROR,
              }}
            >
              {errors.is_active}
            </div>
          )}
        </div>
      </div>

      {/* Form actions */}
      <div style={{ display: 'flex', gap: 12, marginTop: 24, justifyContent: 'flex-end' }}>
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          data-testid="performance_trends-cancel-button"
          style={{
            padding: '10px 20px',
            background: '#fff',
            color: GC_TEXT,
            border: `1px solid ${GC_BORDER}`,
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
            opacity: submitting ? 0.6 : 1,
          }}
        >
          {t('actions.cancel')}
        </button>
        <button
          type="submit"
          disabled={submitting}
          data-testid="performance_trends-submit-button"
          style={{
            padding: '10px 20px',
            background: submitting ? GC_MUTED : GC_BLUE,
            color: '#fff',
            border: 'none',
            borderRadius: 4,
            fontSize: '0.875rem',
            fontWeight: 600,
            cursor: submitting ? 'not-allowed' : 'pointer',
          }}
        >
          {submitting ? t('messages.creating') : t('actions.submit')}
        </button>
      </div>
    </form>
  );
};

