/**
 * WorkObligationsListView.test.tsx -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L99 (work_obligations)
 * Generated: 2026-03-11T08:28:25Z
 * Generator: screens-machine-v1.0.0
 * 
 * Unit tests for WorkObligationsListView component.
 * Based on: Project 31 (EVA Faces) testing patterns
 * 
 * Test Coverage:
 * - Component renders without crash
 * - Loading state displays correctly
 * - Error state displays correctly
 * - Records display in grid
 * - Filter interactions work
 * - Detail drawer opens/closes
 * - Keyboard navigation
 * - Accessibility (ARIA labels, roles)
 */

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, waitFor, within } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { WorkObligationsListView } from './WorkObligationsListView';
import { getDefaultWorkObligations } from '@eva/data-model-ui';
import { LangProvider } from '@context/LangContext';

// Mock Data Model Views
vi.mock('@eva/data-model-ui', () => ({
  getDefaultWorkObligations: vi.fn(),
}));

// Mock NavHeader to simplify tests
vi.mock('@components/NavHeader', () => ({
  NavHeader: () => <div data-testid="nav-header">NavHeader</div>,
}));

const mockRecords = [
  {
    id: 'work_obligations-001',
    // TODO: Add other fields based on schema
  },
  {
    id: 'work_obligations-002',
    // TODO: Add other fields based on schema
  },
  {
    id: 'work_obligations-003',
    // TODO: Add other fields based on schema
  },
];

const renderComponent = (lang: 'en' | 'fr' = 'en') => {
  return render(
    <LangProvider initialLang={lang}>
      <WorkObligationsListView />
    </LangProvider>
  );
};

describe('WorkObligationsListView', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  describe('Loading State', () => {
    it('displays loading message while fetching records', () => {
      vi.mocked(getDefaultWorkObligations).mockImplementation(
        () => new Promise(() => {}) // Never resolves
      );

      renderComponent();

      expect(screen.getByTestId('work_obligations-loading')).toBeInTheDocument();
      expect(screen.getByText(/loading/i)).toBeInTheDocument();
    });
  });

  describe('Error State', () => {
    it('displays error message when fetch fails', async () => {
      const errorMessage = 'Failed to fetch records';
      vi.mocked(getDefaultWorkObligations).mockRejectedValue(new Error(errorMessage));

      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-error')).toBeInTheDocument();
      });

      expect(screen.getByText(/failed to load/i)).toBeInTheDocument();
      expect(screen.getByText(errorMessage)).toBeInTheDocument();
    });

    it('displays error message in French when lang=fr', async () => {
      vi.mocked(getDefaultWorkObligations).mockRejectedValue(new Error('Test error'));

      renderComponent('fr');

      await waitFor(() => {
        expect(screen.getByText(/erreur de chargement/i)).toBeInTheDocument();
      });
    });
  });

  describe('Success State', () => {
    beforeEach(() => {
      vi.mocked(getDefaultWorkObligations).mockResolvedValue(mockRecords);
    });

    it('renders page header with title', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-title')).toBeInTheDocument();
      });

      expect(screen.getByTestId('work_obligations-title')).toHaveTextContent('WorkObligations');
    });

    it('displays record count', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-count')).toBeInTheDocument();
      });

      expect(screen.getByTestId('work_obligations-count')).toHaveTextContent('3 records');
    });

    it('displays refreshed timestamp', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByText(/refreshed/i)).toBeInTheDocument();
      });
    });

    it('renders records grid', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      const grid = screen.getByTestId('work_obligations-grid');
      expect(within(grid).getAllByRole('article')).toHaveLength(3);
    });

    it('displays filter bar', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-filter-bar')).toBeInTheDocument();
      });
    });
  });

  describe('Interactivity', () => {
    beforeEach(() => {
      vi.mocked(getDefaultWorkObligations).mockResolvedValue(mockRecords);
    });

    it('opens detail drawer when card clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      const firstCard = within(screen.getByTestId('work_obligations-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-drawer')).toBeInTheDocument();
      });
    });

    it('closes detail drawer when close button clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('work_obligations-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-drawer')).toBeInTheDocument();
      });

      // Close drawer
      const closeButton = screen.getByTestId('work_obligations-drawer-close');
      await user.click(closeButton);

      await waitFor(() => {
        expect(screen.queryByTestId('work_obligations-drawer')).not.toBeInTheDocument();
      });
    });

    it('closes detail drawer when Escape pressed', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('work_obligations-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-drawer')).toBeInTheDocument();
      });

      // Press Escape
      await user.keyboard('{Escape}');

      await waitFor(() => {
        expect(screen.queryByTestId('work_obligations-drawer')).not.toBeInTheDocument();
      });
    });

    it('closes detail drawer when backdrop clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('work_obligations-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-drawer')).toBeInTheDocument();
      });

      // Click backdrop
      const backdrop = screen.getByTestId('work_obligations-drawer-backdrop');
      await user.click(backdrop);

      await waitFor(() => {
        expect(screen.queryByTestId('work_obligations-drawer')).not.toBeInTheDocument();
      });
    });
  });

  describe('No Results', () => {
    it('displays no results message when no records match filters', async () => {
      vi.mocked(getDefaultWorkObligations).mockResolvedValue([]);

      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-no-results')).toBeInTheDocument();
      });

      expect(screen.getByText(/no records/i)).toBeInTheDocument();
    });
  });

  describe('Accessibility', () => {
    beforeEach(() => {
      vi.mocked(getDefaultWorkObligations).mockResolvedValue(mockRecords);
    });

    it('has proper ARIA labels', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-title')).toBeInTheDocument();
      });

      expect(screen.getByRole('region', { name: /page header/i })).toBeInTheDocument();
      expect(screen.getByRole('region', { name: /filters/i })).toBeInTheDocument();
    });

    it('drawer has proper dialog role and aria-modal', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-grid')).toBeInTheDocument();
      });

      const firstCard = within(screen.getByTestId('work_obligations-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByRole('dialog')).toBeInTheDocument();
      });

      const dialog = screen.getByRole('dialog');
      expect(dialog).toHaveAttribute('aria-modal', 'true');
    });
  });

  describe('i18n', () => {
    beforeEach(() => {
      vi.mocked(getDefaultWorkObligations).mockResolvedValue(mockRecords);
    });

    it('displays French translations when lang=fr', async () => {
      renderComponent('fr');

      await waitFor(() => {
        expect(screen.getByTestId('work_obligations-title')).toBeInTheDocument();
      });

      // Check for French text (adjust based on actual translations)
      expect(screen.getByTestId('work_obligations-count')).toHaveTextContent('enregistrements');
    });
  });
});

