/**
 * DecisionProvenanceListView.test.tsx -- Auto-generated by Screens Machine v1.0.0
 * 
 * Data Model Layer: L31 (decision_provenance)
 * Generated: 2026-03-12T09:00:55Z
 * Generator: screens-machine-v1.0.0
 * 
 * Unit tests for DecisionProvenanceListView component.
 * Based on: Project 31 (EVA Faces) testing patterns
 * 
 * Test Coverage:
 * - Component renders without crash
 * - Loading state displays correctly
 * - Error state displays correctly
 * - Records display in grid
 * - Filter interactions work
 * - Detail drawer opens/closes
 * - Keyboard navigation
 * - Accessibility (ARIA labels, roles)
 */

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, waitFor, within } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { DecisionProvenanceListView } from './DecisionProvenanceListView';
import { getDefaultDecisionProvenance } from '@eva/data-model-ui';
import { LangProvider } from '@context/LangContext';

// Mock Data Model Views
vi.mock('@eva/data-model-ui', () => ({
  getDefaultDecisionProvenance: vi.fn(),
}));

// Mock NavHeader to simplify tests
vi.mock('@components/NavHeader', () => ({
  NavHeader: () => <div data-testid="nav-header">NavHeader</div>,
}));

const mockRecords = [
  {
    id: 'decision_provenance-001',
    // TODO: Add other fields based on schema
  },
  {
    id: 'decision_provenance-002',
    // TODO: Add other fields based on schema
  },
  {
    id: 'decision_provenance-003',
    // TODO: Add other fields based on schema
  },
];

const renderComponent = (lang: 'en' | 'fr' = 'en') => {
  return render(
    <LangProvider initialLang={lang}>
      <DecisionProvenanceListView />
    </LangProvider>
  );
};

describe('DecisionProvenanceListView', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  describe('Loading State', () => {
    it('displays loading message while fetching records', () => {
      vi.mocked(getDefaultDecisionProvenance).mockImplementation(
        () => new Promise(() => {}) // Never resolves
      );

      renderComponent();

      expect(screen.getByTestId('decision_provenance-loading')).toBeInTheDocument();
      expect(screen.getByText(/loading/i)).toBeInTheDocument();
    });
  });

  describe('Error State', () => {
    it('displays error message when fetch fails', async () => {
      const errorMessage = 'Failed to fetch records';
      vi.mocked(getDefaultDecisionProvenance).mockRejectedValue(new Error(errorMessage));

      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-error')).toBeInTheDocument();
      });

      expect(screen.getByText(/failed to load/i)).toBeInTheDocument();
      expect(screen.getByText(errorMessage)).toBeInTheDocument();
    });

    it('displays error message in French when lang=fr', async () => {
      vi.mocked(getDefaultDecisionProvenance).mockRejectedValue(new Error('Test error'));

      renderComponent('fr');

      await waitFor(() => {
        expect(screen.getByText(/erreur de chargement/i)).toBeInTheDocument();
      });
    });
  });

  describe('Success State', () => {
    beforeEach(() => {
      vi.mocked(getDefaultDecisionProvenance).mockResolvedValue(mockRecords);
    });

    it('renders page header with title', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-title')).toBeInTheDocument();
      });

      expect(screen.getByTestId('decision_provenance-title')).toHaveTextContent('DecisionProvenance');
    });

    it('displays record count', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-count')).toBeInTheDocument();
      });

      expect(screen.getByTestId('decision_provenance-count')).toHaveTextContent('3 records');
    });

    it('displays refreshed timestamp', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByText(/refreshed/i)).toBeInTheDocument();
      });
    });

    it('renders records grid', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      const grid = screen.getByTestId('decision_provenance-grid');
      expect(within(grid).getAllByRole('article')).toHaveLength(3);
    });

    it('displays filter bar', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-filter-bar')).toBeInTheDocument();
      });
    });
  });

  describe('Interactivity', () => {
    beforeEach(() => {
      vi.mocked(getDefaultDecisionProvenance).mockResolvedValue(mockRecords);
    });

    it('opens detail drawer when card clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      const firstCard = within(screen.getByTestId('decision_provenance-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-drawer')).toBeInTheDocument();
      });
    });

    it('closes detail drawer when close button clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('decision_provenance-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-drawer')).toBeInTheDocument();
      });

      // Close drawer
      const closeButton = screen.getByTestId('decision_provenance-drawer-close');
      await user.click(closeButton);

      await waitFor(() => {
        expect(screen.queryByTestId('decision_provenance-drawer')).not.toBeInTheDocument();
      });
    });

    it('closes detail drawer when Escape pressed', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('decision_provenance-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-drawer')).toBeInTheDocument();
      });

      // Press Escape
      await user.keyboard('{Escape}');

      await waitFor(() => {
        expect(screen.queryByTestId('decision_provenance-drawer')).not.toBeInTheDocument();
      });
    });

    it('closes detail drawer when backdrop clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      // Open drawer
      const firstCard = within(screen.getByTestId('decision_provenance-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-drawer')).toBeInTheDocument();
      });

      // Click backdrop
      const backdrop = screen.getByTestId('decision_provenance-drawer-backdrop');
      await user.click(backdrop);

      await waitFor(() => {
        expect(screen.queryByTestId('decision_provenance-drawer')).not.toBeInTheDocument();
      });
    });
  });

  describe('No Results', () => {
    it('displays no results message when no records match filters', async () => {
      vi.mocked(getDefaultDecisionProvenance).mockResolvedValue([]);

      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-no-results')).toBeInTheDocument();
      });

      expect(screen.getByText(/no records/i)).toBeInTheDocument();
    });
  });

  describe('Accessibility', () => {
    beforeEach(() => {
      vi.mocked(getDefaultDecisionProvenance).mockResolvedValue(mockRecords);
    });

    it('has proper ARIA labels', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-title')).toBeInTheDocument();
      });

      expect(screen.getByRole('region', { name: /page header/i })).toBeInTheDocument();
      expect(screen.getByRole('region', { name: /filters/i })).toBeInTheDocument();
    });

    it('drawer has proper dialog role and aria-modal', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-grid')).toBeInTheDocument();
      });

      const firstCard = within(screen.getByTestId('decision_provenance-grid')).getAllByRole('article')[0];
      await user.click(firstCard);

      await waitFor(() => {
        expect(screen.getByRole('dialog')).toBeInTheDocument();
      });

      const dialog = screen.getByRole('dialog');
      expect(dialog).toHaveAttribute('aria-modal', 'true');
    });
  });

  describe('i18n', () => {
    beforeEach(() => {
      vi.mocked(getDefaultDecisionProvenance).mockResolvedValue(mockRecords);
    });

    it('displays French translations when lang=fr', async () => {
      renderComponent('fr');

      await waitFor(() => {
        expect(screen.getByTestId('decision_provenance-title')).toBeInTheDocument();
      });

      // Check for French text (adjust based on actual translations)
      expect(screen.getByTestId('decision_provenance-count')).toHaveTextContent('enregistrements');
    });
  });
});

