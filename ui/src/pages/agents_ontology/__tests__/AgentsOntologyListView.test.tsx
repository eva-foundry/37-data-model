/**
 * AgentsOntologyListView.test.tsx -- Auto-generated by Screens Machine v2.0.0
 *
 * Data Model Layer: L01 (agents_ontology)
 * Generated: 2026-03-11T06:28:13Z
 * Generator: screens-machine-v2.0.0
 *
 * Unit tests for AgentsOntologyListView component.
 * Based on: Project 31 (EVA Faces) testing patterns
 *
 * Test Coverage:
 * - Component renders without crash
 * - Loading state displays correctly
 * - Error state displays correctly
 * - Records display in grid
 * - Filter interactions work
 * - Detail drawer opens/closes
 * - Keyboard navigation
 * - Accessibility (ARIA labels, roles)
 */

import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { render, screen, waitFor, fireEvent } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { AgentsOntologyListView } from '../AgentsOntologyListView';
import * as agentsOntologyViews from '@lib/views/agents_ontology';
import { LangProvider } from '@context/LangContext';

// Mock the views library
vi.mock('@lib/views/agents_ontology', () => ({
  getDefaultAgentsOntology: vi.fn(),
}));

import type { AgentsOntologyRecord } from '@/types/agents_ontology';

const mockRecords: AgentsOntologyRecord[] = [
  {
    id: 'agents-ontology-001',
    label: 'Conversation Agent',
    label_fr: 'Agent de conversation',
    agent_type: 'conversation',
    domain: 'ai_runtime',
    ontology_class: 'conversational',
    status: 'active',
    description: 'Multi-turn conversation orchestrator for EVA-JP.',
    capabilities: ['dialogue', 'context-tracking', 'multilingual'],
    skills: ['nlp', 'intent-detection'],
    version: '1.0.0',
    parent_id: null,
    notes: '',
    layer: 'agents_ontology',
    is_active: true,
    row_version: 1,
    created_at: '2026-03-01T00:00:00Z',
    modified_at: '2026-03-10T00:00:00Z',
  },
  {
    id: 'agents-ontology-002',
    label: 'Screen Generator',
    label_fr: 'Generateur de pages',
    agent_type: 'generator',
    domain: 'ui',
    ontology_class: 'generative',
    status: 'active',
    description: 'Generates production-ready UI components from Epic specs.',
    capabilities: ['screen-generation', 'template-based', 'i18n'],
    skills: ['react', 'typescript'],
    version: '2.0.0',
    parent_id: null,
    notes: '',
    layer: 'agents_ontology',
    is_active: true,
    row_version: 2,
    created_at: '2026-03-01T00:00:00Z',
    modified_at: '2026-03-11T00:00:00Z',
  },
  {
    id: 'agents-ontology-003',
    label: 'Test Generator',
    label_fr: 'Generateur de tests',
    agent_type: 'testing',
    domain: 'qa',
    ontology_class: 'generative',
    status: 'planned',
    description: 'Generates Vitest unit test scaffolding for UI components.',
    capabilities: ['test-generation', 'coverage-analysis'],
    skills: ['vitest', 'testing-library'],
    version: '0.1.0',
    parent_id: 'agents-ontology-002',
    notes: 'Phase 3 implementation',
    layer: 'agents_ontology',
    is_active: true,
    row_version: 1,
    created_at: '2026-03-05T00:00:00Z',
    modified_at: '2026-03-05T00:00:00Z',
  },
];

const renderComponent = () => {
  return render(
    <LangProvider defaultLang="en">
      <AgentsOntologyListView />
    </LangProvider>
  );
};

describe('AgentsOntologyListView', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  describe('Loading State', () => {
    it('displays loading message while fetching records', () => {
      vi.mocked(agentsOntologyViews.getDefaultAgentsOntology).mockImplementation(
        () => new Promise(() => {}) // Never resolves
      );

      renderComponent();

      expect(
        screen.getByTestId('agents-ontology-loading')
      ).toBeInTheDocument();
    });
  });

  describe('Error State', () => {
    it('displays error message when fetch fails', async () => {
      const errorMessage = 'Failed to fetch agents ontology records';
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockRejectedValue(new Error(errorMessage));

      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-error')
        ).toBeInTheDocument();
      });

      expect(screen.getByText(errorMessage)).toBeInTheDocument();
    });
  });

  describe('Success State', () => {
    beforeEach(() => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue(mockRecords);
    });

    it('renders page header with title', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-title')
        ).toBeInTheDocument();
      });
    });

    it('shows correct record count', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-count')
        ).toBeInTheDocument();
      });

      const countEl = screen.getByTestId('agents-ontology-count');
      expect(countEl.textContent).toMatch(/3/);
    });

    it('renders a card for each record', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-card-agents-ontology-001')
        ).toBeInTheDocument();
      });

      expect(
        screen.getByTestId('agents-ontology-card-agents-ontology-002')
      ).toBeInTheDocument();
      expect(
        screen.getByTestId('agents-ontology-card-agents-ontology-003')
      ).toBeInTheDocument();
    });

    it('shows filter bar with domain, type, and status dropdowns', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-bar')
        ).toBeInTheDocument();
      });

      expect(
        screen.getByTestId('agents-ontology-filter-domain')
      ).toBeInTheDocument();
      expect(
        screen.getByTestId('agents-ontology-filter-type')
      ).toBeInTheDocument();
      expect(
        screen.getByTestId('agents-ontology-filter-status')
      ).toBeInTheDocument();
    });
  });

  describe('Filter Interactions', () => {
    beforeEach(() => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue(mockRecords);
    });

    it('filters records by status', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-status')
        ).toBeInTheDocument();
      });

      const statusFilter = screen.getByTestId(
        'agents-ontology-filter-status'
      );
      await user.selectOptions(statusFilter, 'active');

      const count = screen.getByTestId('agents-ontology-count');
      expect(count.textContent).toMatch(/2/);
    });

    it('filters records by search text', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-search')
        ).toBeInTheDocument();
      });

      const searchInput = screen.getByTestId(
        'agents-ontology-filter-search'
      );
      await user.type(searchInput, 'conversation');

      await waitFor(() => {
        const count = screen.getByTestId('agents-ontology-count');
        expect(count.textContent).toMatch(/1/);
      });
    });

    it('shows no-results message when filters match nothing', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-search')
        ).toBeInTheDocument();
      });

      const searchInput = screen.getByTestId(
        'agents-ontology-filter-search'
      );
      await user.type(searchInput, 'zzz-no-match-zzz');

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-no-results')
        ).toBeInTheDocument();
      });
    });

    it('shows and clears filter button when filters are active', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-search')
        ).toBeInTheDocument();
      });

      // Apply a filter
      const searchInput = screen.getByTestId(
        'agents-ontology-filter-search'
      );
      await user.type(searchInput, 'screen');

      // Clear button should appear
      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-clear-filters')
        ).toBeInTheDocument();
      });

      // Click clear
      await user.click(
        screen.getByTestId('agents-ontology-clear-filters')
      );

      // All records should show again
      await waitFor(() => {
        const count = screen.getByTestId('agents-ontology-count');
        expect(count.textContent).toMatch(/3/);
      });
    });
  });

  describe('Detail Drawer', () => {
    beforeEach(() => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue(mockRecords);
    });

    it('opens detail drawer when card is clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-card-agents-ontology-001')
        ).toBeInTheDocument();
      });

      await user.click(
        screen.getByTestId('agents-ontology-card-agents-ontology-001')
      );

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-drawer')
        ).toBeInTheDocument();
      });

      expect(
        screen.getByTestId('agents-ontology-drawer-title')
      ).toBeInTheDocument();
    });

    it('closes detail drawer when close button is clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-card-agents-ontology-001')
        ).toBeInTheDocument();
      });

      // Open drawer
      await user.click(
        screen.getByTestId('agents-ontology-card-agents-ontology-001')
      );
      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-drawer')
        ).toBeInTheDocument();
      });

      // Close drawer
      await user.click(
        screen.getByTestId('agents-ontology-drawer-close')
      );
      await waitFor(() => {
        expect(
          screen.queryByTestId('agents-ontology-drawer')
        ).not.toBeInTheDocument();
      });
    });

    it('closes detail drawer when backdrop is clicked', async () => {
      const user = userEvent.setup();
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-card-agents-ontology-001')
        ).toBeInTheDocument();
      });

      // Open drawer
      await user.click(
        screen.getByTestId('agents-ontology-card-agents-ontology-001')
      );
      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-drawer')
        ).toBeInTheDocument();
      });

      // Click backdrop
      await user.click(
        screen.getByTestId('agents-ontology-drawer-backdrop')
      );
      await waitFor(() => {
        expect(
          screen.queryByTestId('agents-ontology-drawer')
        ).not.toBeInTheDocument();
      });
    });
  });

  describe('Keyboard Navigation', () => {
    beforeEach(() => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue(mockRecords);
    });

    it('opens drawer on Enter key press on a card', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-card-agents-ontology-001')
        ).toBeInTheDocument();
      });

      const card = screen.getByTestId(
        'agents-ontology-card-agents-ontology-001'
      );
      fireEvent.keyDown(card, { key: 'Enter', code: 'Enter' });

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-drawer')
        ).toBeInTheDocument();
      });
    });
  });

  describe('Accessibility', () => {
    beforeEach(() => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue(mockRecords);
    });

    it('has ARIA roles on key elements', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getByRole('main')).toBeInTheDocument();
      });

      expect(screen.getByRole('region', { name: /page header/i })).toBeInTheDocument();
    });

    it('filter bar has accessible label', async () => {
      renderComponent();

      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-filter-bar')
        ).toBeInTheDocument();
      });

      const filterBar = screen.getByTestId('agents-ontology-filter-bar');
      expect(filterBar).toHaveAttribute('aria-label');
    });

    it('each card has accessible button role', async () => {
      renderComponent();

      await waitFor(() => {
        expect(screen.getAllByRole('button').length).toBeGreaterThan(0);
      });
    });
  });

  describe('i18n Hook', () => {
    it('uses useLiterals hook with agents_ontology.list_view scope', async () => {
      vi.mocked(
        agentsOntologyViews.getDefaultAgentsOntology
      ).mockResolvedValue([]);

      renderComponent();

      // Component should render without errors (useLiterals called)
      await waitFor(() => {
        expect(
          screen.getByTestId('agents-ontology-title')
        ).toBeInTheDocument();
      });
    });
  });
});
