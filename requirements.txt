# EVA Model API — runtime dependencies
# Pin versions to avoid breaking changes
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.3

# Cosmos DB (optional — only needed when COSMOS_URL is set)
azure-cosmos==4.7.0
aiohttp==3.10.11
# Managed-identity / passwordless auth for Cosmos (PROD-WI-3 / COS-2).
# Uncomment when wiring DefaultAzureCredential in api/store/cosmos.py.
# azure-identity==1.17.1

# Redis (optional — only needed when REDIS_URL is set)
redis==5.0.7

# HTTP client (used by tests and health probes)
httpx==0.27.0

# Testing dependencies
pytest-asyncio==0.23.5
